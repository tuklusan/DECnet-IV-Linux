<!-- ============================================================================ -->
<!-- Copyright (c) 2026 Supratim Sanyal of SANYALnet Labs. -->
<!-- Proprietary rights reserved except as expressly licensed herein. -->
<!-- -->
<!-- DECnet-IV-Linux -->
<!-- This file is governed by the SANYALnet Labs Non-Commercial License in the -->
<!-- root LICENSE file. Non-Commercial use is permitted; Commercial Use and use -->
<!-- for AI/ML model training are prohibited unless separately authorized. -->
<!-- -->
<!-- Attribution is required: "Based on original work by Supratim Sanyal of -->
<!-- SANYALnet Labs." See LICENSE for full terms, warranty disclaimer, termination, -->
<!-- patent, trademark, and governing-law provisions. -->
<!-- ============================================================================ -->

# Scratch Resume Index

## Current checkpoint

Annotated tag `PHASE-4-COMPLETE` is verified on closure commit `571333bfd7aaa8b2fcc88715c1d61442af151f3c`; the temporary tag workflow has been removed.

Phase 4 is complete on exact protocol candidate `6c185b6d01f8a57ee9f0ea6a6c37d7112ddb77d2`. Phase 5 is complete on exact protocol candidate `2d54dbf252120b42a5c2a60fa202371382af5dc9`. Phase 6 is complete on exact protocol candidate `c11045a70e7deb59870fff5f8bc22213e0df2bc0`; Phase 7 is active on `main`. Phase 3 remains frozen at tag `PHASE-3-COMPLETE` on commit `ae1bcb82a1539ccadda0661664205e360bd760b7`.

The final Phase 3 acceptance set was green: Repository Policy `35343324826`, Build Bootstrap `35343357536`, Project State Gate `35343359444`, External Reference Baselines `35343361139`, Python QEMU VM Lab `35343362922`, and Independent Ethernet Interoperability run `35343364812`. The interoperability matrix passed all eight amd64/ARM64 Route20/PyDECnet routing/endnode role jobs.

Temporary Phase 3 Route20 crash instrumentation and the temporary tagging workflow are gone. Only reusable infrastructure remains: the fixed Route20 pin, PyDECnet peer, exact-source candidate/reference image derivation, direct-QEMU lab, Route20 guest 60-second post-boot settle policy, bounded readiness/failure handling, packet capture and compact evidence.

## Stable infrastructure

- Route20: `a9ef7c0b7f875f0dd2e8abaf11213798a8e4474c` (native VDE Ethernet, default open args, modern/legacy libvdeplug ABI).
- PyDECnet live: `8d93c2a546317c67aba0adf9433f5f3efdf1f85c` (native VDE Ethernet, default open args, modern/legacy libvdeplug ABI; mature MULTINET retained).
- PyDECnet tests: `9a844987bf3a1450632dee8d37e60a23a453bad3`.
- LinuxDECnet: `ff39eef045d1e4b7b72a3d40111e89c07a473398`.
- SIMH: `5b73b1032b52d19bf80752ea4d9cbbdc92e7b5e0`.
- Foundations: `outer-v2-<arch>-<foundation-fingerprint>`.
- VM runtime: fresh disposable qcow2 overlays under short `/tmp/dniv-*` paths.
- Ordinary lab addressing: area 31, nodes 70-79.
- Route20 reference guests: reach `multi-user.target`, idle 60 seconds, then start the peer.
- Route20 READY bounds: amd64 180s, ARM64 600s.
- Host-TAP PyDECnet: starts on the architecture-matched runner and requires its `DECnet/Python is running` application marker within 60 seconds.
- E1 controller bounds: amd64 300s, ARM64 360s.
- Local VDE2 transport proof: green, run `35440502997`; cross-runner VDE2 is still unproven.
- Local MULTINET TCP proof: green, run `35440787624`; Area-31 remote integration is still planned.
- Remote branch invariant: only `refs/heads/main`.
- Acceptance concurrency is isolated by run/scenario instead of globally by architecture: E1-E4, independent references and interop suites can use separate hosted runners concurrently. Exact-SHA and parent-run binding are unchanged, so parallelism does not weaken candidate identity.

## Phase 4 closure

Exact protocol candidate `6c185b6d01f8a57ee9f0ea6a6c37d7112ddb77d2` closed Phase 4 on 2026-09-18. Repository Policy `35393330920`, Build Bootstrap `35393363966`, Project State Gate `35393366433`, External Reference Baselines `35393368425`, E1 `35393370633`, E2 `35393372626`, E3 `35393374453`, E4 `35393376465`, and Independent Ethernet Interoperability `35393378666` all completed successfully. E1-E4 passed on amd64 and ARM64 and the Route20/PyDECnet interoperability matrix was green.

Phase 4 delivered routing codecs/checksums, adjacency-backed L1/L2 route state, metrics/aging, triggered and periodic routing advertisements, deterministic best-route selection, short/long data forwarding, visit-count loop prevention, return-to-sender behavior, L1 destination-0 attached-area semantics, multi-LAN convergence and multi-area forwarding.

## Next action

This section is an append-only chronology. Historical `Next:`/`next` statements describe the state at that entry and are superseded by later entries; the final entries below define the current resume point.

Phase 5 is active. NSP wire/state primitives now have a kernel connection foundation: validated CI/CD/CR/CC/RUN/DI transitions, bounded connection allocation with generation-spaced local link IDs, bounded retransmit queues, contiguous transmit sequencing, cumulative prefix ACK retirement and due-retransmit lookup. Local Routing Layer delivery now enters NSP on endnodes and routers; CI/RCI creates or matches incoming links, remote node/link identity is checked, and expected receive sequence advances modulo 4096. Outbound NSP now has deterministic local long-data encapsulation, router route-table selection, endnode router-adjacency selection, loopback delivery and a validated NSP transmit entry point. CI/CD receive mapping now learns the remote link identifier from CC/reject traffic; data/other sequence space starts at one; cumulative ACK retirement is restricted to the outstanding contiguous window and NAK is not retired as ACK. NSP data and other-data subchannels now have independent transmit/receive sequence spaces, retransmit selection and cumulative ACK handling, including cross-subchannel XACK retirement regardless of optional ACK-field position. The follow-up build regression from stale local cross-channel temporaries is corrected on main. Kernel NSP timers now enforce connection-establishment expiry, response retransmission limits and inactivity keepalive generation; valid RUN traffic rearms inactivity. Outbound logical-link operations now include CI generation with RCI retry, new/duplicate incoming CI acknowledgment, CC accept, DI reject/disconnect with disconnect confirmation, immediate data/other ACKs and sequenced data transmission. NSP now has bounded per-link out-of-order and ready receive queues, contiguous reorder drain, data-window backpressure, Link Service XON/XOFF/interrupt-credit handling, interrupt transmission and dequeue metadata for upper layers. The Phase 5 logical-link/receive-queue build follow-up removes a userspace unit assertion against the kernel-private MSS constant; protocol behavior is unchanged. NSP negative acknowledgements now follow the Digital NSP 4.0.1 rules: a valid data NAK cumulatively acknowledges NUMBER and immediately retransmits from NUMBER+1, while an other-data NAK for the outstanding other-data sequence retransmits that message without retiring it. Cross-subchannel XNAK uses the same rules after channel selection. Phase IV delayed acknowledgement is implemented with the NSP 4.0.1 suggested 3-second ACK delay for in-order packets that permit delay; duplicates force an immediate cumulative ACK, while future out-of-order packets stay cached without acknowledging missing sequence space. The Linux-facing socket compatibility UAPI is now defined with classic DECnet protocol numbers, sockaddr_dn, connect/disconnect/access/link option layouts and DSO constants, with host unit coverage. A bounded NSP normal-data reassembly API now preserves BOM/EOM message boundaries, sequence-ordered segments and the 65023-byte DECnet/Linux message ceiling while keeping interrupt delivery independent. Exact-SHA acceptance of the E4 convergence-race fix at `c96b715a1e4adcfb8d36f2675d4538c98ffe2e94` completed green: Repository Policy `35417101451`, Build Bootstrap `35417116930`, Project State Gate `35417117771`, External Reference Baselines `35417118818`, E1 `35417119749`, E2 `35417120797`, E3 `35417121525`, E4 `35417122358`, and Independent Ethernet Interoperability `35417123229`; all eight interop jobs passed. The native `AF_DECnet` / `SOCK_SEQPACKET` family is now registered against NSP with validated `sockaddr_dn` bind/getname, outbound object-number/name connect selectors, blocking/nonblocking connect, record segmentation/reassembly, poll readiness and NSP-to-socket waiter notification. Linux 6.8 build of the initial socket-family commit exposed an unavailable `copy_to_iter_full` helper; the follow-up uses the supported `copy_to_iter` byte-count contract. The next build caught a cleanup typo that removed `order` from the sequence helper instead of the outer receive path; this follow-up restores the helper local and removes only the unused receive local. Exact-SHA acceptance of the socket-family build follow-up at `dff43abd34cb77cb65fd03d92c1e49fab1e55304` is green: Repository Policy `35420342660`, Build Bootstrap `35420378234`, Project State Gate `35420379715`, External Reference Baselines `35420380754`, E1 `35420381818`, E2 `35420382772`, E3 `35420383606`, E4 `35420384814`, and Independent Ethernet Interoperability `35420385869`; all amd64/ARM64 jobs passed. The next candidate adds direct independent NSP/socket proof: every PyDECnet interoperability role now drives native `AF_DECnet` / `SOCK_SEQPACKET` through built-in object 25 (MIRROR) with records spanning single-segment, MSS-boundary and multi-segment sizes, and packet-capture validation requires bidirectional NSP traffic. The first MIRROR-test commit preserved the intended content but accidentally cleared executable bits on the lab entry scripts; Repository Policy run `35422200657` caught the mode regression before acceptance dispatch. This follow-up restores the executable modes without changing protocol behavior. Exact-SHA acceptance of `5f8fbac5311dc160365a08020be7bce3f681cf4e` exposed one ARM64 PyDECnet false-readiness race in interop job `105842036682`: the candidate had an UP routing adjacency, but the reference harness had emitted `DNIV-REF-READY` while PyDECnet was still importing/initializing, so the first MIRROR socket connect returned `ENOENT`. The reference harness now captures PyDECnet startup output and does not publish READY until the pinned peer itself logs `DECnet/Python is running`; timeout/exit paths preserve the captured PyDECnet diagnostics. Candidate protocol behavior is unchanged. The first exact-SHA rerun of this readiness fix showed the ARM64 controller still cut the reference VM off before PyDECnet could reach that application-ready marker: prior evidence places the marker near guest uptime 435 seconds, while the harness still allowed only 360 wall-clock seconds for reference readiness. The ARM64 independent-reference READY bound is therefore raised to a bounded 600 seconds; amd64 remains 180 seconds. This changes only the TCG startup allowance, not protocol behavior or candidate-side convergence limits. Repository Policy run `35424062828` correctly rejected that harness-only bound change because its dedicated readiness regression still encoded the previous 360-second ARM64 contract. The regression is updated with the new 600-second value and a 660-second excessive-bound ceiling, preserving a narrow bounded allowance around the observed application-ready time. Next rerun exact-SHA acceptance, then complete inbound listen/accept, interrupt/OOB delivery, classic DECnet socket options/access/connect data, stream mode, socket lifecycle negatives and dedicated independent NSP socket interoperability. Do not reopen Phase 4 without concrete regression evidence.

Exact-SHA acceptance of `7d5be13d35d4cc15e06511ad4fedf99f483f1034` proved the raised ARM64 PyDECnet readiness bound and its regression guard, and the E1 ARM64 retry completed green. Independent Ethernet Interoperability run `35424157142` then exposed a real candidate routing defect in ARM64 PyDECnet L1 job `105847098820`: after the broadcast-router adjacency reached UP, PyDECnet's initial self-destination L1 vector entry was infinity, and the coalesced route key treated that learned entry as the separately maintained direct adjacency route, withdrawing the direct path to 31.71. The native MIRROR connect therefore returned `ENOENT` before NSP could transmit. Packet-capture ordering confirms the self-infinity vector arrived after two-way adjacency formation and before PyDECnet later advertised its self metric as zero. This follow-up preserves the direct adjacent-router path by ignoring the advertising router's own destination slot in learned L1/L2 vector processing; focused route-metric unit coverage locks the rule.

Next: rerun exact-SHA acceptance for this corrective candidate. Only after it is fully green, continue Phase 5 with inbound listen/accept, interrupt/OOB delivery, classic DECnet socket options/access/connect data, stream mode, socket lifecycle negatives and dedicated independent NSP socket interoperability.

Exact-SHA acceptance of `5a79c95bbddcdce27507acc55d211c5f70fa7ce4` passed policy/build/state/reference and E1-E4 on both architectures, but Independent Ethernet Interoperability `35425733618` failed only ARM64 PyDECnet routing on two attempts. PCAP proves the direct-route fix works: the candidate emits the valid NSP CI to 31.71. PyDECnet then has an approximately 33-second frame-silent interval while processing the candidate's initial full routing-vector burst, longer than the lab MIRROR client's 20-second timeout. This is reference convergence/readiness, not a candidate protocol failure. The harness now requires PyDECnet adjacency to remain continuously UP for eight seconds before starting MIRROR; the readiness policy regression locks the check before `dnmrr`.

Next: rerun exact-SHA acceptance for this harness correction. Only after the full matrix is green, continue Phase 5 with inbound listen/accept, interrupt/OOB delivery, classic DECnet socket options/access/connect data, stream mode, socket lifecycle negatives and dedicated independent NSP socket interoperability.

Repository Policy `35434792500` caught a Python syntax error in the new readiness regression: the generated source had a literal `\\n` between `REFERENCE_IMAGE` and `CANDIDATE_SMOKE`. The follow-up fixes only that formatting defect; protocol behavior and the stable-adjacency lab guard are unchanged.

ARM64 PyDECnet routing in `35434888383` still timed out after the eight-second stable-adjacency guard. The guard is increased to forty continuous seconds (160 checks at 250 ms, bounded by 960 checks / 240 s), which exceeds the observed roughly 33-second PyDECnet processing blackout. Candidate protocol behavior is unchanged.

PyDECnet settle guard raised from 40 to 60 continuously-UP seconds (240 quarter-second samples, still bounded by 960 samples / 240 s) to match the lab rule. Candidate protocol behavior is unchanged. VDE2 and MULTINET will be proven independently, not in the same acceptance run, before either is used for distributed scaling.

Transport reconciliation: serial/synchronous datalink scope is removed. VDE2 is the rootless/distributed Ethernet fabric and MULTINET TCP is the user-space Internet point-to-point transport. They are deliberately proven in separate workflows before combination. Test scripts are persistent at `tests/lab/prove-vde2.sh` and `tests/lab/prove-multinet.sh`; the configurable Area-31 gateway is `userspace/dnmultinet/dnmultinet.py`. The HECnet path is optional interoperability evidence and never substitutes for local candidate acceptance.

Next: finish exact-SHA acceptance, then VDE2 proof, then MULTINET proof, then resume Phase 5 inbound socket/listener work.

Repository Policy `35436873179` caught missing `queue: max` declarations in the new VDE2 and MULTINET workflow concurrency blocks. The follow-up changes workflow policy only; protocol and transport behavior are unchanged.

VDE2 proof `35436873611` failed because foreground `vde_switch` exits on EOF in the noninteractive runner after dependencies/fork build succeeded. The proof now uses VDE daemon mode with per-run socket/PID discovery and cleanup.

VDE2 proof `35436975623` reached daemon startup but the harness incorrectly required `-sock` to be a socket inode; VDE2 creates a communication directory there. The endpoint readiness check now accepts the real VDE2 path form.

VDE2 proof `35437051337` found that modern libvdeplug exports `vde_open_real`, not a callable `vde_open` symbol. Both VDE-enabled forks are corrected with modern ABI plus legacy fallback and repinned. The reference baseline is now explicitly scoped to the maintained PyDECnet module list so removed transport scope does not re-enter acceptance. Next rerun VDE2 independently, then MULTINET independently, then return to Phase 5 socket/listener work.

VDE2 proof `35440198290` got through modern ABI symbol resolution but libvdeplug returned `EINVAL` when given a zeroed open-args structure. Both fork adapters now pass NULL open args, and the proof first validates the switch `ctl` socket plus a native C/libvdeplug frame round trip before exercising PyDECnet. Rerun VDE2 alone; do not start MULTINET until it is green.

Repository Policy `35440503013` caught the missing canonical project header on `tests/reference/pydecnet-in-scope-tests.txt`. The follow-up fixes only that policy defect; the in-scope module list is unchanged.

VDE2 proof `35440502997` is green: real libvdeplug frame delivery, PyDECnet adapter and Route20/PyDECnet adjacency all passed. Proceed with MULTINET as a separate proof; only after both are green may they be combined for Area-31 distributed lab routing.

MULTINET `35440600180`: all 40 upstream MULTINET tests passed. Live proof false-failed because it watched Ethernet adjacency events instead of point-to-point circuit events. Harness now watches 4.10 Circuit up / 4.8 Circuit down and will be rerun independently.

MULTINET proof `35440787624` is green: upstream MULTINET suite plus live TCP point-to-point circuit establishment, down detection and reconnect recovery. VDE2 and MULTINET are independently proven. Next run exact-SHA project acceptance, then resume Phase 5 socket/listener work; Area-31 combination follows only as an additional interoperability surface.

External Reference Baselines `35440924133` exposed a harness parser defect: the canonical header in the PyDECnet module list was treated as test names, yielding `ValueError: Empty module name`. The workflow now filters comments/blank lines and requires a non-empty module array before running unittest. Rerun exact-SHA acceptance.

Acceptance `47c95e0` exposed only an ARM64 harness budget issue: PyDECnet READY and candidate adjacency UP are present, but the fixed 420-second candidate-ready controller timeout expires during the required 60-second stability polling under TCG, before MIRROR or any protocol failure marker. Default ARM64 candidate-ready bound raised to 720 seconds; amd64 remains 420 and explicit override remains supported. Rerun exact-SHA acceptance before Phase 5 listener work.

Repository Policy `35443472854` caught an undefined `INTEROP` name in the new readiness regression. Replace it with the existing `SCRIPT` text variable and rerun exact-SHA acceptance; no lab/protocol behavior changes.

Repository Policy `35443572981` caught that the prior typo fix over-rewrote literal `DNIV-INTEROP` / `DNIV_INTEROP_TIMEOUT_SECONDS` strings in the regression. Restore those literals; keep only the Python text variable as `SCRIPT`. Rerun exact-SHA acceptance.

Acceptance `35443697753` showed the 720-second ARM64 budget still expires inside the fixed 60-guest-second PyDECnet stability loop after both reference READY and candidate UP. Replace elapsed guest-time settling with a protocol-visible readiness condition: snapshot hello count after UP, require a subsequent peer hello while still UP, then run MIRROR. No candidate protocol behavior changes.

Acceptance `71dfd825ca251761e2f2f19edfd9355d738a72ef` still fails ARM64 PyDECnet L1 after fresh-hello readiness. PCAP shows valid CI + three RCIs from the candidate and zero PyDECnet unicast replies across the full 721-second capture, while reference multicast routing and raw probes continue; amd64 gets CC in 27.7 ms. Root cause is the ARM64 TCG reference NIC receive path depending on promiscuous delivery to a DECnet logical MAC different from its 52:54 hardware MAC. The PyDECnet reference NIC now uses the DECnet logical MAC directly; Route20 keeps the distinct hardware-MAC pcap path. Probe evidence is keyed by the DNIV payload marker. Rerun exact-SHA acceptance before Phase 5 listener work.


Exact-SHA acceptance of `2f87ff696b52109c97bb0302b172105bb3a0e901` completed seven of eight independent interop jobs green in run `35446947352`; only ARM64 PyDECnet routing failed at the first L1 MIRROR connect. The candidate emitted the same valid CI bytes as the successful amd64 run, then three RCIs, while the reference continued emitting routing/hello traffic and raw unicast probes but no NSP reply. Matching the QEMU NIC to the DECnet logical MAC was therefore insufficient by itself. The PyDECnet reference guest now explicitly enables promiscuous mode on its Ethernet interface before opening the pcap handle, removing dependence on libpcap/driver timing for unicast capture under ARM64 TCG. Candidate protocol behavior is unchanged. Rerun exact-SHA acceptance before Phase 5 listener work.

Exact-SHA acceptance of `ad2be6f12ea275b0d46ed7814893b19a2312cae0` completed all acceptance jobs except ARM64 PyDECnet routing in Independent Ethernet Interoperability run `35449155556`, job `105913181915`. Explicit guest-interface promiscuous mode did not change the failure mode: the candidate reached adjacency UP and emitted the same valid MIRROR CI bytes as the successful amd64 case, while the ARM64 PyDECnet reference stayed alive and continued hello/routing and diagnostic probe traffic but emitted no protocol unicast reply. This isolates the failure to the independent reference's ARM64 guest virtio/libpcap receive path rather than candidate DECnet behavior. The interop harness now runs the exact pinned PyDECnet peer directly on the architecture-matched hosted runner using PyDECnet's native Linux TAP backend on the host bridge; Route20 keeps the independent reference-VM path. PyDECnet still gates on its own running marker and packet-capture validation still requires bidirectional NSP traffic. The raw diagnostic-probe minimum remains a Route20/reference-VM check and is no longer required for the direct-TAP PyDECnet path.

Next: run exact-SHA acceptance for the direct-TAP reference harness. Only after the full acceptance matrix is green, resume Phase 5 inbound listen/accept work, then interrupt/OOB delivery, classic DECnet socket options/access/connect data, stream mode, lifecycle negatives and independent socket interoperability.

Repository Policy run `35452003085` rejected the direct-TAP harness candidate only because the permanent interop-readiness regression still required two literal guest `DNIV-REF-READY` waits and guest PyDECnet promiscuous-mode setup. The regression now validates both bounded generic readiness waits, the pinned PyDECnet host-TAP configuration, its application-ready marker, logical TAP MAC assignment and Route20-only diagnostic-probe minimum. Protocol and direct-TAP harness behavior are unchanged.

Repository Policy run `35452161060` found the updated readiness regression's regex did not match the shell's literal `$ref1_log` / `$ref2_log` calls. The guard now checks both exact bounded wait call strings once each while retaining the hardcoded-timeout rejection. Harness and protocol behavior are unchanged.

Independent Ethernet Interoperability run `35452263422` proved the first direct host-TAP experiment reaches two-way routing adjacency, but amd64 PyDECnet roles job `105921360755` timed out on MIRROR. Artifact `10587625738` shows 775 DECnet frames: PyDECnet continuously emits router hellos/routing updates, the candidate emits four unicast NSP attempts to the PyDECnet logical MAC, and PyDECnet emits no unicast NSP reply. The direct-TAP harness had assigned the TAP netdev itself that same DECnet logical MAC. On a Linux bridge that address is a local FDB entry, so frames to it are consumed by the host bridge path rather than delivered to the TAP userspace queue. The harness now leaves the TAP Linux MAC distinct while PyDECnet continues using its DECnet logical source/destination filtering internally. Candidate protocol behavior is unchanged.

Next: rerun exact-SHA acceptance for the corrected host-TAP bridge receive path. Do not resume Phase 5 listener/accept until the full acceptance matrix is green.

Exact-SHA acceptance of `2acf029b387f165f191129c0e74d6354645994bf` proved the corrected direct host-TAP bridge path: amd64 PyDECnet routing job `105923668238` passed both L1 and L2 with bidirectional NSP (`candidate_nsp=18`, `reference_nsp=29` in each scenario). The amd64 PyDECnet roles job `105923668264` then failed only because the candidate smoke harness still required the old reference-guest raw probe stream after the candidate NIC MAC-change test. The host-TAP PyDECnet path deliberately has no guest probe process. For PyDECnet, the post-MAC-change check now repeats native MIRROR over NSP, which directly proves bidirectional protocol unicast survives the NIC hardware-MAC change; Route20 retains the raw-probe counter check. Candidate protocol behavior is unchanged.

Next: rerun exact-SHA acceptance. Resume Phase 5 listener/accept only after the full matrix is green.

Acceptance run `35453716148` showed the post-MAC-change MIRROR replacement was too strong for L2 timing: amd64 PyDECnet roles passed, but L2 job `105925203104` completed initial MIRROR and fresh post-change hello, then timed out on a second immediate MIRROR while the capture showed repeated CI attempts and no reply. This does not reproduce the original receive-path defect; initial bidirectional NSP is already proven, and the MAC-change test's purpose is lower-layer unicast receive continuity. The direct-TAP path now restores that original proof with an independent host-side `dnraw` probe loop on the Linux bridge. Route20 keeps its guest probe loop. Candidate smoke again requires a post-change raw-unicast receive delta for both references, and pcap validation requires at least three marked probes for both paths while preserving strict Route20 source-MAC checking. Candidate protocol behavior remains unchanged.

Next: exact-SHA acceptance of the host raw-probe path; resume Phase 5 listener/accept only after the complete matrix is green.

Exact-SHA acceptance of `5a878e49954053021772953278726c8e2b74985a` is green. Repository Policy `35454712409`, Build Bootstrap `35454733908`, Project State Gate `35454735310`, External Reference Baselines `35454736674`, the four amd64/ARM64 E1-E4 Python QEMU VM Lab runs `35454737703`, `35454738620`, `35454739608`, and `35454740654`, plus Independent Ethernet Interoperability `35454741869` all completed successfully. The interoperability run passed all eight Route20/PyDECnet amd64/ARM64 routing and role jobs.

Phase 5 listener/accept groundwork now preserves the complete valid incoming CI/RCI Session Control payload in the bounded NSP connection record while the connection is in CR, exposes a read-only CR snapshot API carrying local/remote logical links and remote node identity, and notifies the socket layer after successful initial or retransmitted CI acknowledgement. This is the minimum transport boundary needed for socket-layer destination end-user matching without changing NSP wire behavior. Next: add listener registration/backlog matching by destination object selector, then create and accept child sockets with CC/RUN completion.

Exact-SHA acceptance of `31a5700eeeb573ee28af3c23ce0a89d8bef6b129` is green: Repository Policy `35457158310`, Build Bootstrap `35457183047`, Project State Gate `35457183916`, External Reference Baselines `35457184858`, E1 `35457185708`, E2 `35457186450`, E3 `35457187442`, E4 `35457188280`, and Independent Ethernet Interoperability `35457189126` all completed successfully; all eight Route20/PyDECnet interop jobs passed.

Phase 5 inbound socket support now adds a bounded listener registry and backlog keyed by bound DECnet object number or object name. Incoming CR links are matched from the preserved Session Control CI payload, duplicate RCI notifications are de-duplicated, malformed/no-listener/full-backlog requests are rejected with Phase IV Session/NSP reasons, listener poll reports pending accepts, and `accept()` creates a child socket bound to the existing CR link, decodes the remote end-user selector/node, sends CC and waits for RUN while leaving the parent listener active. Extended source end-user formats 2/4 are decoded for LinuxDECnet compatibility; destination matching remains object-number/name formats 0/1. Next: build/accept this exact candidate, then add focused inbound independent socket interoperability before moving to interrupt/OOB and classic socket options.

Exact-SHA acceptance of `ae6f9fbb26b7ec86583eb33968d95f98f9a9e706` is green: Repository Policy `35460530834`, Build Bootstrap `35460553986`, Project State Gate `35460554931`, External Reference Baselines `35460556110`, E1 `35460557133`, E2 `35460558100`, E3 `35460559292`, E4 `35460560083`, and Independent Ethernet Interoperability `35460561051` all completed successfully; all eight Route20/PyDECnet jobs passed.

The next Phase 5 candidate adds direct inbound socket interoperability. Each PyDECnet matrix role now enables the pinned peer's Unix API and drives two independent connections into the candidate: numeric object 240 and named object `DNIVTEST`. The candidate test process binds/listens on both native `AF_DECnet` / `SOCK_SEQPACKET` sockets, validates the remote PyDECnet node and source object `PYDNIV` through `getpeername()`, echoes application records, observes orderly disconnect and leaves the normal routing/MIRROR/fault sequence intact. The candidate image builds this test binary from repository source, and policy guards require the direct-TAP API/listener proof. Session access-field validation also accepts the full UAPI-defined 40-byte field bound instead of the accidental 39-byte limit. Next: exact-SHA acceptance; if green, proceed to interrupt/OOB delivery and classic socket options/access/connect data.


Exact-SHA interoperability run `35462953985` exposed a harness-only failure in every PyDECnet job: the new Unix API socket path lived under the deeply nested per-run artifact directory and exceeded the platform AF_UNIX pathname limit (`OSError: AF_UNIX path too long`). Route20 jobs were unaffected. The direct-TAP PyDECnet API socket now uses a short per-session hashed `/tmp/dniv-api-*.sock` path with cleanup, while all inbound socket protocol checks remain unchanged. Next: exact-SHA acceptance of this narrowly corrective successor; stop when the complete acceptance set is green.


Repository Policy run `35464168645` caught that the prior corrective commit accidentally changed `tests/lab/run-interop.sh` from executable to non-executable while constructing the multi-file commit. Restore only the executable mode; the short PyDECnet AF_UNIX socket-path correction is unchanged. Next: exact-SHA acceptance; stop when fully green.


Exact-SHA interoperability run `35464251242` exposed a second harness-only PyDECnet API mismatch: pinned PyDECnet `DictConnector.connect()` accepts the source end-user selector as `localuser`, not `srcname`; the latter raised `TypeError` after the candidate listener became ready, leaving every PyDECnet job blocked until the 75-minute workflow timeout. The inbound driver now passes `localuser=PYDNIV`, preserving the intended source object identity and changing no candidate DECnet protocol behavior. Next: exact-SHA acceptance; stop when fully green.


Exact-SHA interoperability run `35469380899` proved the `localuser` correction on amd64 PyDECnet routing and the first endnode scenario, then exposed a narrowly scoped readiness race in `router-endnode`: the PyDECnet endnode returned transient Session Control reason 39 (`Destination unreachable`) immediately after the candidate L1 router reported its endnode adjacency UP. The inbound driver now retries only reason 39 for at most 20 seconds (0.5-second interval), remaining inside the candidate listener's 30-second receive bound; all other rejects still fail immediately. This changes only independent-reference readiness handling, not candidate protocol behavior. Next: exact-SHA acceptance; stop when fully green.


Exact-SHA interoperability runs `35469380899` and `35469759657` isolated a pinned-PyDECnet endnode reference defect in the `router-endnode` role. Packet evidence shows PyDECnet sends a valid CI to the candidate router; the candidate returns ACK-CONN and CC on the wire, after which PyDECnet endnode replies DC/NoLink reason 41 and reports Session reason 39. The same candidate listener handshake is green when PyDECnet is a router, including both numeric and named selectors. The prior reason-39 retry is removed because it cannot correct the reference NSP state loss. The interoperability harness now excludes only the inbound-listener subtest for `router-endnode`; all other `router-endnode` routing/NSP/fault-recovery checks remain, and inbound AF_DECnet proof remains mandatory for the other PyDECnet roles. Candidate protocol behavior is unchanged. Next: exact-SHA acceptance; stop when fully green.


Exact-SHA acceptance of `6aa5eec808e45c0477bb7ea87b9ff76e0bc0859b` is green: Repository Policy `35470535535`, Build Bootstrap `35470553067`, Project State Gate `35470553922`, External Reference Baselines `35470554959`, E1-E4 VM Lab runs `35470555830`, `35470556808`, `35470557705`, `35470558603`, and Independent Ethernet Interoperability `35470559403`. All eight Route20/PyDECnet interop jobs passed. Phase 5 inbound listener/accept and the direct PyDECnet numeric/name listener proof are therefore accepted, with only the pinned PyDECnet endnode inbound-listener reference-defect case excluded as documented.

Next Phase 5 work: interrupt/OOB delivery; classic socket options/access/connect data; stream mode; lifecycle negatives; broader independent socket interoperability.

Planned distributed work remains ordered behind its dependencies. The VDE2 proof currently covers one host only; prove a real cross-runner switch join before distributed scale depends on it. The future Area-31 workflow must be repository-tracked, use `MULTINET_REMOTE_HOST`, `MULTINET_REMOTE_PORT`, `VAX_ADDR`, `VAX_USERNAME`, and `VAX_PASSWORD` only as runtime secrets, check their presence before remote activity, and never emit values. The MULTINET peer is one Area-31 area router and the VAX target is another area router reachable through it. Preserve paired Linux/VAX scripts under `tests/lab` as routing/NICE counters, NSP/Session, login, DAP/FAL, PHONE, mail, task/object and later application tests become available.

Documentation reconciliation corrected stale/cross-document status and continuity wording, including explicit local-only VDE2 proof status and the planned secret-backed Area-31/VAX test path. The zero-gap documentation audit restarts from this tree; protocol behavior is unchanged.

Repository Policy run `35474499903` caught that the documentation reconciliation had renamed the required `## Next action` continuity heading. The canonical heading is restored in both continuity files; the append-only chronology rule remains unchanged. The zero-gap documentation audit restarts from this corrected tree.


Exact-SHA acceptance of documentation-reconciled main `0f041277efbaa9633b56aeef500b04b89fa6d088` is green: Repository Policy `35474807386`, Build Bootstrap `35474819482`, Project State Gate `35474820666`, External Reference Baselines `35474821742`, E1-E4 VM Lab `35474823040`, `35474824226`, `35474825388`, `35474826191`, and Independent Ethernet Interoperability `35474827016`; all eight interop jobs passed.

Phase 5 now advances to interrupt/OOB socket delivery. The candidate maps NSP interrupt data to `MSG_OOB`, enforces the 1-16 byte bound and remote interrupt credit, replenishes one remote interrupt credit when userspace consumes OOB, exposes `POLLPRI`/`EPOLLRDBAND` and `SIOCATMARK`, and extends the direct PyDECnet numeric/name listener proof to bidirectional interrupts plus repeated peer interrupt after credit replenishment. Next after exact-SHA acceptance: classic DECnet socket options/access/connect data.


Exact-SHA acceptance of `c24ecd22042c110ae72d39ac9965b0d0b5e7093a` is green: Repository Policy `35476621265`, Build Bootstrap `35476639827`, Project State Gate `35476640927`, External Reference Baselines `35476642259`, E1-E4 VM Lab `35476643544`, `35476644595`, `35476645585`, `35476646632`, and Independent Ethernet Interoperability `35476647856`; all eight interop jobs passed. Phase 5 interrupt/OOB delivery is accepted.

Phase 5 now advances to the classic socket-option subset: `DSO_CONACCESS`, `DSO_CONDATA`, and read-only `DSO_LINKINFO`. Outbound CI carries access/connect data, inbound accepted sockets expose PyDECnet access/connect data, listener connect data is returned in CC, outbound sockets capture peer CC data, and the PyDECnet harness plus pcap validator require both directions on wire. Next after exact-SHA acceptance: disconnect data/deferred accept controls where needed by classic userspace, then stream mode and lifecycle negatives.


Classic-option candidate `43c6b96bf014702a6d572429509cb68e6760ea0b` passed policy/build/state/reference and all E1-E4 amd64/ARM64 runs, but Independent Ethernet Interoperability `35487016251` failed the four PyDECnet jobs before inbound-listener testing; all Route20 jobs passed. The common failure is a harness expectation bug: PyDECnet MIRROR sends CC accept data `ff ff`, confirmed on retained ARM64 L1 pcap, while `dnmrr` incorrectly required zero-length `DSO_CONDATA`. Update the assertion to require the two-byte MIRROR value. The pinned PyDECnet Session API connect keyword is `data`, not `conndata`; fix the inbound driver and permanent policy guard at the same time. Candidate protocol behavior is unchanged. Next: exact-SHA acceptance.


Exact-SHA acceptance of classic-option corrective candidate `89799302cb0c164cbd8291fd744ef6463567d78b` is green: Repository Policy `35488506272`, Build Bootstrap `35488525088`, Project State Gate `35488526182`, External Reference Baselines `35488527090`, E1-E4 Python QEMU VM Lab `35488528135`, `35488529268`, `35488530262`, `35488531164`, and Independent Ethernet Interoperability `35488532137` all completed successfully; all eight Route20/PyDECnet interoperability jobs passed. The classic `DSO_CONACCESS`, `DSO_CONDATA`, and read-only `DSO_LINKINFO` subset is accepted on main. Next Phase 5 work: disconnect data/deferred accept controls where required by classic userspace, then stream mode, lifecycle negatives, and broader independent socket interoperability.


Phase 5 deferred-accept and outbound disconnect-data support is now implemented against the pinned LinuxDECnet socket contract. `DSO_ACCEPTMODE` accepts `ACC_IMMED` or `ACC_DEFER`; deferred `accept()` returns the CR child without sending CC, `DSO_CONACCEPT` sends the configured CC data and waits for RUN, and `DSO_CONREJECT` sends DI using configured `DSO_DISDATA`. Normal shutdown/release also transmits configured disconnect reason/data. The direct PyDECnet inbound proof uses immediate numeric accept and deferred named accept, so both paths are exercised against an independent peer. Inbound peer disconnect-data capture remains next before stream mode and lifecycle negatives.


Exact-SHA acceptance of deferred-accept candidate `0499c273de9e4eb068b5a899327bfe3f25621082` is green: Build Bootstrap `35493677400`, Project State Gate `35493678586`, External Reference Baselines `35493679708`, E1-E4 Python QEMU VM Lab `35493680575`, `35493681592`, `35493682418`, `35493683113`, and Independent Ethernet Interoperability `35493684110` all completed successfully. Deferred accept/reject and outbound disconnect-data controls are accepted.

The next Phase 5 candidate preserves peer Disconnect Initiate reason/data through the NSP closed state and exposes it through `DSO_DISDATA`; the PyDECnet inbound proof now disconnects both listener sessions with `py-disconnect`, and native userspace requires the exact payload after EOF. Next after exact-SHA acceptance: stream-mode semantics and lifecycle negatives.


Candidate `e3c516f572ec4238263165f76e69719f1c82627e` failed Build Bootstrap `35507899224` on x86_64 with the concrete compiler error `dniv_sock_getsockopt: ret undeclared` in the new `DSO_DISDATA` snapshot path. The corrective successor only declares the existing local return variable; protocol behavior is unchanged. Next: exact-SHA acceptance of the corrective successor.


Exact-SHA acceptance of disconnect-data corrective candidate `0c71473a90c8c601f8a6c2dc4ec55e9ac80e23df` is green: Repository Policy `35507949425`, Build Bootstrap `35507964672`, Project State Gate `35507965960`, External Reference Baselines `35507967011`, E1-E4 Python QEMU VM Lab `35507968181`, `35507969353`, `35507970535`, and `35507971766`, and Independent Ethernet Interoperability `35507973008` all completed successfully. Peer Disconnect Initiate reason/data preservation and `DSO_DISDATA` retrieval are accepted.

Phase 5 now adds native `SOCK_STREAM` compatibility over NSP. Stream sockets preserve unread bytes across short reads, support `MSG_WAITALL` across NSP record boundaries, reject application `MSG_EOR`, and retain the existing interrupt/OOB path. The direct PyDECnet MIRROR proof exercises a 4096-byte response through 257-byte reads and a 200-byte `MSG_WAITALL` read spanning two peer records. Next after exact-SHA acceptance: lifecycle negatives and broader socket stress/interoperability.


The initial SOCK_STREAM commit `f8aed0844f5d1bf19e61143d5f156d7bc76100d9` accidentally cleared executable mode on the candidate-image and interoperability entry scripts while constructing the multi-file tree. This corrective successor restores only those executable bits; stream/socket behavior is unchanged. Next: exact-SHA acceptance.


Exact-SHA acceptance of SOCK_STREAM corrective candidate `65b6857eae25d38f260e7185580d6a88c2efb49b` is green: Repository Policy `35511887065`, Build Bootstrap `35511906913`, Project State Gate `35511908045`, External Reference Baselines `35511909266`, E1-E4 VM Lab `35511910105`, `35511911060`, `35511912053`, and `35511913169`, and Independent Ethernet Interoperability `35511914361`; all eight interop jobs passed. SOCK_STREAM is accepted.

Next Phase 5 candidate: socket lifecycle negatives and independent connection churn, including deterministic pre-connect errors, nonblocking connect completion, unsupported half-shutdown, full shutdown/send-after-shutdown, and repeated PyDECnet MIRROR sessions. If exact-SHA acceptance is green, proceed to poll/epoll, fork/dup/process-exit and concurrent-session stress.


Exact-SHA acceptance of socket lifecycle/stress candidate `dd8107fe0967cad04c048175d9843e9057efd2bc` is green: Repository Policy `35513958617`, Build Bootstrap `35514007595`, Project State Gate `35514009043`, External Reference Baselines `35514010307`, E1-E4 VM Lab `35514011498`, `35514012520`, `35514013577`, and `35514014546`, and Independent Ethernet Interoperability `35514015412`; all eight interop jobs passed. Lifecycle negatives and 16-cycle independent MIRROR churn are accepted.

Next Phase 5 candidate adds poll/epoll readiness, dup and fork descriptor lifetime, process-exit cleanup, and eight concurrent PyDECnet MIRROR sessions. If exact-SHA acceptance is green, continue with higher-volume concurrent data/window pressure and listener backlog/accept stress.


Exact-SHA acceptance of descriptor/process/concurrency candidate `749c1eca73d808759bc03969f597f14a4bf479a1` is green: Repository Policy `35515817724`, Build Bootstrap `35515835527`, Project State Gate `35515836391`, External Reference Baselines `35515837603`, E1-E4 VM Lab `35515838672`, `35515839711`, `35515840906`, and `35515842015`, and Independent Ethernet Interoperability `35515843122`; all eight interop jobs passed. Descriptor/process/concurrency socket stress is accepted.

Next Phase 5 candidate adds sustained multi-link data/window pressure: eight simultaneous PyDECnet MIRROR sessions, each carrying repeated 4096-byte records with exact echo validation before close. If exact-SHA acceptance is green, proceed to inbound listener backlog/accept stress and close/reset races.


Exact-SHA acceptance of sustained data/window-pressure candidate `30fda8f27f172f7904d6d0bef42c8aeaa605d7ad` is green: Repository Policy `35519500865`, Build Bootstrap `35519522589`, Project State Gate `35519523777`, External Reference Baselines `35519524801`, E1-E4 VM Lab `35519525762`, `35519526703`, `35519527663`, and `35519528509`, and Independent Ethernet Interoperability `35519529402`; all eight interop jobs passed. Sustained multi-link data/window pressure is accepted.

Next Phase 5 candidate adds inbound listener backlog/accept stress: four simultaneous PyDECnet Session connects are queued before native userspace accepts them, then all four are accepted and echoed exactly. If exact-SHA acceptance is green, proceed to backlog overflow/object-busy rejection and close/reset races.


Infrastructure detour is active. Acceptance is now tiered as `fast`, `consolidated`, and `full`; ordinary owner acceptance issues default to fast, nightly scheduled acceptance is full, and only full acceptance can promote a phase/release. Fast keeps dual-architecture build/E1 plus focused independent interop; consolidated adds references/E4 and broader peers; full retains the complete E1-E4/reference/interop matrix. Interop scenario pairs are split into independent parallel jobs to reduce wall time without deleting assertions. The Phase 5 backlog candidate `5924e18523cf8c2fc569b8ab68eaea5e812fc981` investigation remains locked exactly where it was: x64 PyDECnet jobs `106106370256` and `106106370339` failed, no failure logs have been analyzed, and no protocol/harness corrective work has been attempted. End the infra detour before resuming that investigation.


Infrastructure correction: GitHub rejected the first tiered `interop.yml` because a job-level `if` referenced `matrix.*` before matrix expansion. The corrected version uses a dynamic `fromJSON` include matrix selected from `PROFILE` and `SCOPE`; coverage policy is unchanged.


Infrastructure policy follow-up: Repository Policy `35523266827` correctly stopped dispatch because `workflow_budget_gate.py` could not see scenarios inside the dynamic JSON matrix. The gate now parses both literal YAML and JSON matrix scenario fields, with a one-scenario-per-job maximum; regression coverage is updated accordingly.


Infrastructure test correction: Repository Policy `35523382490` caught the stale two-scenario policy-test fixture. The fixture now models one legal scenario and verifies that two scenarios are rejected; production workflow behavior is unchanged.


Infrastructure detour closure: acceptance tiering is implemented and policy-validated on main `45b17abc86837cb2777fad628480af8342a1552f`. Repository Policy `35523481269` and its dispatcher passed. The emitted fast profile was exactly build + state + E1 on amd64/arm64 + x64 Route20/PyDECnet L1 interop; those E1/interop children were externally cancelled before completion and are not acceptance evidence. Full interop is now one scenario per job (14 jobs), nightly scheduled acceptance is full, and only full can promote a phase/release.

End the infrastructure detour and resume the frozen Phase 5 backlog investigation at candidate `5924e18523cf8c2fc569b8ab68eaea5e812fc981`. First inspect failed x64 PyDECnet jobs `106106370256` and `106106370339`; no failure logs were analyzed and no corrective protocol/harness work was performed during the detour. Fix only proven defects, then iterate with the fast acceptance profile unless a consolidated/full checkpoint is warranted.

Frozen backlog-failure investigation completed for candidate `5924e18523cf8c2fc569b8ab68eaea5e812fc981`. Both failed x64 PyDECnet jobs (`106106370256`, `106106370339`) reached the new four-connection listener test and failed with candidate-generated Session reason 6 (object busy), while one accepted connection was also torn down before echo. The concrete kernel defect is duplicate incoming CI/RCI handling: NSP correctly recognized an existing remote logical link and re-ACKed the duplicate, but then re-notified Session Control as though it were a fresh connection indication. Once accept dequeued a CR link, a retransmitted CI/RCI could therefore requeue that already-owned link and consume backlog slots. Duplicate CI/RCI now re-ACKs only; the original connection indication remains the sole upper-layer notification. Protocol behavior outside duplicate-connect indication delivery is unchanged. Next: exact-SHA fast acceptance of this corrective successor, then continue Phase 5 backlog overflow/object-busy rejection and close/reset races if green.

Corrective candidate `f87e654077130f8f4b4a36110c3007e9e18b1ce1` removed duplicate CI/RCI upper-layer re-notification but fast socket acceptance still failed in x64 PyDECnet L1 interop run `35524297201`, job `106113666583`. Packet capture proves all four distinct CIs were initially ACKed; the native listener later accepted/connected three links, but the third child closed before echo and listener teardown then rejected the fourth queued link with reason 6. The remaining failure is therefore downstream of queue admission, not a four-entry backlog overflow. The backlog harness now reports exact recv/send return values and errno at the failure point so the next exact-SHA run can distinguish receive-state from transmit-state failure without changing protocol behavior.

Diagnostic successor `08fd5f68df1aa6db8d279ac34480f93eda803062` isolated the remaining backlog failure in x64 PyDECnet L1 run `35524772529`, job `106114913039`: the second accepted child returned `recv() = -1, errno=EPERM` even though the peer's valid sequence-1 data record was on the wire. This is a socket-security initialization defect, not NSP sequencing: accepted children were attached with `sock_init_data()` but never passed through the modern Linux accepted-socket security graft/clone hooks. The accept path now calls `security_sock_graft()` and `security_sk_clone()` after child allocation/attachment, matching current Linux accepted-socket initialization requirements while leaving DECnet protocol state unchanged. The backlog diagnostic newline is also corrected. Next: exact-SHA fast socket acceptance; if green, advance to explicit backlog-overflow/object-busy rejection and close/reset race coverage.

Fast socket run `35525149713` on `7d87e5d40a5ba15149332f24403ecf02d6747828` reproduced the same `recv()=-1/EPERM` failure, so the speculative accepted-socket security graft/clone change did not affect the defect and has been removed. A narrowly scoped object-241 kernel trace now records entry and exact return from the DECnet `recvmsg` callback. The next exact-SHA run will determine whether EPERM is produced inside the protocol callback or before it in the generic socket/security path; no protocol behavior is changed by this diagnostic successor.

Exact-SHA diagnostic run `35525468345`, job `106116739880`, on `c12d950326f163ea42ad71135510199ea003b5f9` again returned userspace `recv()=-1/EPERM`, but none of the object-241 `dniv_sock_recvmsg` entry traces fired. The denial is therefore definitively before the DECnet protocol callback in Linux's generic `sock_recvmsg()` LSM hook. Comparing the native accept path with current Linux socket cloning shows the concrete initialization defect: accepted child `struct sock` security state must be cloned from the listening socket before the child is grafted to the accepted `struct socket`. The prior experiment grafted before cloning, unlike the kernel's normal clone-then-graft ordering. The DECnet accept path now performs `security_sk_clone(listener, child)`, initializes the child socket, then performs `security_sock_graft(child, newsock)`. Protocol state and wire behavior are unchanged. Next: exact-SHA fast socket acceptance; remove the temporary object-241 recv trace once this is proven green.

Fast socket acceptance for corrective candidate `e89a1c8a0184ad170331266a3516ba940926b8d4` is green: Repository Policy `35525699661`, Build Bootstrap `35525717748`, Project State Gate `35525719171`, E1 Python QEMU VM Lab `35525720775`, and Independent Ethernet Interoperability `35525722276` all completed successfully. The four-entry PyDECnet inbound listener queue passed exactly. The root cause of the frozen `5924e185...` failures was accepted-socket LSM state initialization: the child security state must be cloned from the listener before grafting the child to the accepted socket. Temporary recv tracing is removed in the successor.

Phase 5 now adds the mandatory negative side of backlog handling. Object 242 listens with backlog two while three PyDECnet Session clients connect concurrently; exactly two must be accepted and echoed and exactly one must be rejected with Session reason 6 (object busy). No product behavior is changed by this successor beyond retaining the accepted-socket security fix. Next after exact-SHA fast acceptance: close/reset race coverage.

Repository Policy run `35526264084` caught a harness packaging defect in `e8e9b04fec6404c7201d279c3c97b7510c589793`: the tree update accidentally dropped executable mode bits from the modified interop scripts. No protocol code ran under that candidate. This corrective successor restores the established `100755` modes for `dniv-interop-smoke.sh`, `run-interop.sh`, and `pydecnet-backlog.py`; backlog-overflow test semantics are otherwise unchanged.

Fast interop run `35526382699` on `7301d5d4f4b72ed138ce14b1a99e274f71bbbac0` reproduced the intermittent second-child `EPERM` before the new overflow stage. This showed that the prior green run was not sufficient to declare the listener stress accepted. The queue harness was also mixing two concerns: it accepted, echoed, and closed each child while other connection requests remained queued. That close-while-pending behavior belongs to the next close/reset-race item, not the backlog-admission proof. The native backlog server now first accepts every queued child, then performs echo validation, and only then closes children. The accepted-socket LSM calls are also restored to the exact ordering used by current in-tree AF_ALG (`sock_init_data`, `security_sock_graft`, then `security_sk_clone`). This successor therefore isolates backlog admission/overflow from close races rather than hiding them; close/reset races remain explicitly next.

Fast acceptance for `591c73d50e149d4b58ed93270af8c2254d13986d` is fully green: Repository Policy `35526637563`, Build Bootstrap `35526655922`, Project State Gate `35526657237`, Independent Ethernet Interoperability `35526659585`, and E1 Python QEMU VM Lab `35526658388` on both x86_64 and ARM64. The independent PyDECnet proof includes a four-entry listener queue plus the mandatory negative backlog case: backlog two with three concurrent requests yielded exactly two accepted links and one Session reason-6 object-busy rejection.

Phase 5 now isolates the previously observed close-while-pending race as its own interoperability scenario instead of conflating it with backlog admission. Object 243 queues four concurrent PyDECnet clients; the native server deliberately accepts, echoes, and closes each child one at a time while later requests remain pending. All four clients must complete without LSM denial, spurious object-busy rejection, stale-link reuse, or cross-connection corruption. This is harness-only coverage; no protocol behavior changes in this candidate. Next after exact-SHA fast acceptance: peer-reset/disconnect races and then consolidated Phase 5 checkpointing.

The isolated close-while-pending scenario on `23cb7952d81d0d56c8d2d0050fc968ca4b26f4de` failed in x64 PyDECnet interoperability run `35527721091`: the first accepted object-243 child returned `recv()=-1/EPERM`, after the queue and overflow scenarios had both passed. This exposes a concrete accepted-socket initialization ordering defect. The child was passed through `security_sock_graft()` and `security_sk_clone()` while `newsk->sk_protocol` was still zero; `DNPROTO_NSP` was assigned only afterward. Linux security hooks must see the final socket family/protocol identity. The accept path now assigns `PF_DECnet` and `DNPROTO_NSP` immediately after `sk_alloc()`, before `sock_init_data()` and the security graft/clone hooks. No NSP wire behavior changes. Next: exact-SHA fast socket acceptance retaining the isolated close-race scenario.

The protocol-identity-before-hooks successor `d052b0c37e31f52e898a9bd5c4305e3bce0da4bd` still failed the isolated close-while-pending test in x64 interop run `35527962650`, again with the first accepted child denied `recv()=-1/EPERM`. Thus zero protocol identity was not the cause. The remaining controlled difference from the earlier green accepted-child experiment is security clone/graft ordering. For DECnet's independently allocated accepted child, cloning the listener's `sk_security` after graft leaves the already-grafted socket associated with the pre-clone child security state. The accept path now sets final DECnet identity, clones `sk_security` from the listener, initializes the child socket, and only then grafts it. This matches the required causal ordering for this independently allocated network child rather than AF_ALG's family-specific pattern. The close-race test remains unchanged and is the acceptance discriminator.

The clone-before-graft experiment `0aadf54a1352b1c0dde6019482a69574c4fa2ec1` still failed the isolated close-while-pending test in x64 run `35528243005`; child 0 completed and child 1 then hit `recv()=-1/EPERM`. This proves manual `sk_alloc()` plus selected security hooks is insufficient for a normal accepted network child. Linux's canonical network-socket cloning path is `sk_clone_lock()`, which copies the complete listener socket state while preserving a separately allocated security blob and cloning LSM, cgroup, filter, namespace, uid, and other per-socket state coherently. DECnet accepted children now use `sk_clone_lock()`, reset all DECnet listener/private queue state, graft the fully cloned child to `newsock`, unlock it, and drop the extra protocol-side clone reference because DECnet does not hash accepted children. The isolated close-race remains unchanged as the discriminator.

Candidate `40f055eb49ad6f08ca302adf86e9683bc0eefa39` was rejected before protocol execution: Build Bootstrap `35528599091` failed on both architectures because `sk_clone_lock()` is GPL-only and this project's module license is deliberately non-GPL. That experiment is fully reverted.

Upstream AppArmor documents the relevant accepted-socket contract directly: sockets created by `sock_alloc()` have no `struct sock` during `socket_post_create`; accept cases are initialized later by the `sock_graft` LSM hook. Manually invoking `security_sk_clone(listener, child)` is therefore not the generic network accept path and can copy listener peer-security state into an unrelated child. DECnet now allocates the child with `sk_alloc()`, sets final family/protocol, attaches it with `sock_init_data()`, and invokes only `security_sock_graft()` for accepted-socket LSM setup. This uses exported non-GPL interfaces and leaves protocol/wire behavior unchanged. The close-while-pending test remains the discriminator.

Graft-only candidate `4b2498f38c3da884f9cccc8ba9bab1e7a2fa9c50` built cleanly and E1 passed on x86_64/ARM64, but x64 PyDECnet interoperability run `35528808340` again reproduced the isolated close-while-pending failure: child 0 completed, child 1 returned `recv()=-1/EPERM`, and listener teardown then rejected remaining pending clients. Queue and overflow scenarios still passed first. The earlier inference that EPERM was necessarily before `dniv_sock_recvmsg()` relied on `pr_info` output that may be suppressed by the VM console log level, so that inference is no longer treated as proven.

This diagnostic successor makes no protocol change. For object 243 only, the kernel emits warning-level accept/recvmsg entry and return records including family/type/protocol/state, while the userspace server records `SO_DOMAIN`, `SO_TYPE`, and `SO_PROTOCOL` for every accepted child before recv. The next exact-SHA fast interop run will establish whether EPERM is returned before the DECnet callback and whether child socket identity changes across close/reaccept.

Diagnostic candidate `31c04a18b87e755ff1b30fc4857438ff16cf8d5e` did not reach the VM: interop run `35530445339` failed while compiling `dnbacklog.c` because the strict C11 build does not expose glibc's `SO_PROTOCOL` and `SO_DOMAIN` feature macros. The Linux ABI values (38 and 39) are now supplied under `#ifndef` guards in the test harness only. No kernel or protocol behavior changes.

Exact-SHA fast acceptance for diagnostic candidate `42fb2c52cf16fbe10d3d9be8e65fb3994a9f4532` is green: Repository Policy `35530548031`, Build Bootstrap `35530565830`, Project State Gate `35530566952`, Independent Ethernet Interoperability `35530569203`, and E1 Python QEMU VM Lab `35530568009` on both architectures. The successful close-race run showed all four accepted descriptors consistently as domain 12 (AF_DECnet), type 5 (SOCK_SEQPACKET), protocol 2 (NSP), with fd 4 deliberately reused after each close. Because the defect is intermittent, one green four-child sample is not sufficient to close the investigation.

The next diagnostic strengthens only the discriminator: one object-243 listener now survives four successive four-client batches (16 accept/echo/close/reaccept cycles) while keeping backlog four, warning traces are raised to error console priority so callback entry/exit cannot be hidden by console log level, and the server records its task security label plus seccomp/no-new-privileges state. No protocol behavior changes.

Two independent fast runs on diagnostic stress candidate `4babe093db6c4f1bb56d2084c157b39065cbf57c` were green: interop runs `35531069533` and `35531426687`, each completing 16 close/accept/echo/reaccept cycles, plus green x86_64/ARM64 E1, build, and state gates. Error-priority kernel tracing proved every successful child entered `dniv_sock_recvmsg()` with AF_DECnet/SOCK_SEQPACKET/NSP identity and returned the expected 9-byte payload; the server process was unconfined with Seccomp=0 and NoNewPrivs=0. This excludes AppArmor/seccomp policy as the source of the intermittent EPERM and confirms the earlier failures are timing-sensitive.

A concrete socket-lifecycle mismatch remains in the implementation and is now corrected. DECnet attached a freshly allocated accepted child with `sock_init_data(newsock, newsk)` and then called the LSM graft hook directly. Linux's canonical accepted-network-socket attachment is `sock_graft()`, which updates the socket/wait-queue association under `sk_callback_lock` and invokes the LSM graft hook as part of that atomic attachment. The child is now fully initialized while unattached with `sock_init_data_uid(NULL, newsk, SOCK_INODE(newsock)->i_uid)`, then attached using `sock_graft(newsk, newsock)`. All diagnostic logging and per-child identity probes are removed; the 16-cycle close/reaccept stress remains, so timing is no longer artificially stretched. Next: repeated exact-SHA fast acceptance.

The first no-diagnostic acceptance run for canonical graft candidate `64a30eb6fa23b8759cfb2d64f63e8e2f9fd0c788` failed immediately in close-race round 1 (interop `35532259656`) with `recv()=-1/EPERM` on child 0. This exposed a concrete consequence of the new unattached initialization: `sock_init_data_uid(NULL, ...)` intentionally cannot copy `socket->type`, so the child `sk_type` remained zero. Generic LSM receive mediation matches family/type/protocol and can therefore reject the malformed AF_DECnet/type-0/NSP socket before the DECnet recvmsg callback. The accepted child now explicitly copies `newsock->type` into `newsk->sk_type` before `sock_graft()`. This retains the atomic canonical graft while restoring the socket identity proven by the prior diagnostics. Next: repeated no-diagnostic exact-SHA fast acceptance; any residual EPERM after this point belongs to the original race rather than the type-zero regression.

No-diagnostic canonical-graft candidate `95102f695155c2b44f364392a644f79934aef43a` still failed close-race round 1 in x64 interop run `35532556612` with userspace `recv()=-1/EPERM`, after queue and overflow had passed. Restoring `sk_type=SOCK_SEQPACKET` therefore fixed the type-zero regression but did not remove the original intermittent failure.

The next diagnostic is deliberately failure-only to avoid the timing perturbation that made prior verbose diagnostics green. Object 243 emits no accept/entry/success logging. It logs only if `dniv_sock_recvmsg()` itself is about to return a negative value. Since every visible DECnet receive path returns named errno values other than EPERM, a reproduced userspace EPERM with no matching kernel negative-return record will prove the rejection occurs before the protocol callback in generic `security_socket_recvmsg()`; a matching record will identify the exact protocol return instead.

Failure-only diagnostic run `35532853384` on `304baa4e5e58e83d891076000f3271ea2ed7f4a6` reproduced the defect without success-path logging and proved it is inside the DECnet protocol callback: `dniv_sock_recvmsg()` itself logged `ret=-1` for object 243 immediately before userspace received EPERM. This overturns the earlier LSM hypothesis. The callback source has no intentional `-EPERM` return, so the next diagnostic tags the small number of assignments that can feed a negative return: pre/post-allocation socket-state checks, `dniv_nsp_recv_message()`, wait interruption, post-wake link status, and copy-to-user. Logging remains failure-only; successful timing is unchanged apart from integer stage assignments.

The stage-tag candidate `8899d1eeae6a298e99bbc35af616a02866df83e1` failed to compile because a broad edit inserted the diagnostic assignment into `dniv_wait_running()`. That stray assignment is removed and stage 20 is now placed only at the receive wait in `dniv_sock_recvmsg()`. No protocol behavior changes.

Corrected stage run `35533900407` on `ab6411443ee4030349c68a21771f108223da45d4` reproduced the failure and localized it exactly: `dniv_sock_recvmsg()` returned `-1` at stage 20, the `wait_event_interruptible_timeout()` assignment. The NSP receive call itself was not the source. Since that wait normally returns zero, positive remaining jiffies, or `-ERESTARTSYS`, the literal `-1` strongly suggests the timeout value entering the wait may itself be `-1` when the wait condition is already satisfied. The next failure-only diagnostic records both the wait input and `sk_rcvtimeo`; no success-path logging is added.

Root cause established from interop run `35534273593` on `6f10ee87d4491bed64bcaaff5002eb4d8ac59882`: the failing receive entered `wait_event_interruptible_timeout()` with both `timeo` and `sk_rcvtimeo` equal to `MAX_SCHEDULE_TIMEOUT` (`LONG_MAX`), then `dniv_sock_recvmsg()` returned literal `-1`. Several socket paths stored the wait macro's `long` return value in an `int ret`. On 64-bit kernels, a successful wake with an infinite timeout can return `LONG_MAX`; conversion to 32-bit int yields `-1`, which userspace reports as EPERM. Timing-heavy diagnostics masked the defect by allowing data to arrive before the wait.

The fix separates protocol errno (`int ret`) from wait results (`long wait_ret`) in every affected DECnet socket path: normal and OOB send flow-control waits, stream receive, seqpacket/OOB receive, and listener accept. `dniv_wait_running()` already used a long result and required no change. Failure diagnostics are removed. This fixes the reproduced close/reaccept failure and the same latent truncation hazard in blocking send, stream receive, and accept. Exact-SHA fast acceptance must now prove the 16-cycle no-delay close/reaccept stress and x86_64/aarch64 E1.

Exact-SHA fast acceptance of the wait-width fix `31284e3437010ea2c7bc1ae41a0a9554ce8ea471` proved the Phase 5 socket defect fixed in independent PyDECnet interoperability run `35534785016`: normal backlog passed, the backlog=2/3-client negative case produced exactly one reason-6 rejection, and all 16 close/echo/reaccept cycles passed without diagnostic timing. Build and Project State gates were green. ARM64 E1 also passed. x86_64 E1 run `35534783548` failed for a separate harness race: DN70 observed DN71's expired adjacency transition directly from UP with expires=0 to INIT when DN71 restarted, never sampling a moment where the adjacency row was absent. The harness therefore failed to set `seen_expired`; DN71 later completed its own recovery proof and powered off, after which DN70 recorded that second disappearance and waited for a recovery that could never occur.

E1 now treats an INIT transition after the established-UP phase as valid evidence that the prior adjacency expired and the peer restarted, matching the kernel state transition actually observed on x86_64. A physically absent adjacency remains valid evidence too. This is test-harness-only; no protocol behavior changes. Next: exact-SHA fast acceptance of the socket fix plus corrected E1 discriminator.

Exact-SHA fast acceptance for `9c303ed5d461503c4f0443f260386922bd2c03ae` is fully green: Repository Policy `35535312705`, Build Bootstrap `35535336273`, Project State Gate `35535337506`, Independent Ethernet Interoperability `35535339578`, and E1 Python QEMU VM Lab `35535338581` on both x86_64 and ARM64. The PyDECnet run again passed the four-entry backlog case, the exact object-busy overflow negative case, and all 16 no-delay close/reaccept cycles. The 64-bit wait-result truncation defect and the unrelated E1 restart-discriminator race are therefore resolved at fast acceptance depth.

Phase 5 now advances to peer abort/reset isolation. A new object-244 interop server accepts an ABORTER and SURVIVOR child on the same listener for eight successive rounds. Pinned PyDECnet aborts one running logical link with Session reason 9 and disconnect data while the sibling remains active; Linux must return EOF on the aborted child, preserve the reason/data through DSO_DISDATA, continue lossless echo on the sibling, and then capture that sibling's orderly reason-0 disconnect data. This directly tests that peer abort/teardown cannot corrupt another accepted child or listener state.

Exact-SHA fast acceptance for peer-abort isolation candidate `a4741f85d8f65481792c6f507f1090b2b6f4f587` is fully green: Repository Policy `35536040433`, Build Bootstrap `35536091528`, Project State Gate `35536092812`, Independent Ethernet Interoperability `35536095581`, and E1 Python QEMU VM Lab `35536094108` on x86_64 and ARM64. The PyDECnet reset scenario passed eight rounds of reason-9 abort/disconnect-data capture while a sibling accepted link remained lossless, followed by orderly sibling disconnect-data capture. No cross-child corruption or listener damage was observed.

Phase 5 now tests listener teardown with pending connections. Object 245 receives three concurrent PyDECnet requests on a backlog-four listener. Linux accepts one child, closes the listener while two requests remain pending, and must reject exactly those two with Session reason 6 while the already accepted child remains usable for echo. This distinguishes listener ownership from accepted-child lifetime and directly exercises `dniv_listener_unregister()` under live pending state.

Exact-SHA fast acceptance for listener-close candidate `6ea6195ed8451d09e21f16af5ebcdeedb828722c` is fully green: Repository Policy `35537665524`, Build Bootstrap `35537685972`, Project State Gate `35537687056`, Independent Ethernet Interoperability `35537688924`, and E1 Python QEMU VM Lab `35537688053` on x86_64 and ARM64. The independent PyDECnet listener-close case produced exactly one accepted child and two reason-6 pending rejections, while the accepted child remained usable. Prior backlog, overflow, 16-cycle close/reaccept, and eight-round peer-abort isolation cases also remained green.

The final close/reset-race discriminator before a consolidated Phase 5 checkpoint is simultaneous termination. Object 246 accepts four live PyDECnet children; two abort with reason 9 and distinct disconnect data while two perform orderly reason-0 disconnects at the same barrier. Linux must observe EOF and the correct per-link reason/data on every child without cross-link contamination, then accept a fresh fifth connection on the same listener and complete echo plus orderly disconnect.

Exact-SHA fast acceptance for simultaneous-termination candidate `35ba4cbeb020f944b2e19c6ba808595228496f1b` is fully green: Repository Policy `35538239304`, Build Bootstrap `35538290607`, Project State Gate `35538292068`, Independent Ethernet Interoperability `35538294648`, and E1 Python QEMU VM Lab `35538293448` on x86_64 and ARM64. The independent PyDECnet termination-race case passed with two simultaneous reason-9 aborts, two simultaneous orderly disconnects, correct per-link disconnect data, no cross-link contamination, and a fresh fifth connection succeeding on the same listener. The complete listener/backlog/close/reset race tranche is therefore fast-green.

This closes the current related Phase 5 socket-lifecycle tranche and moves to a consolidated socket checkpoint before further NSP transport work. The next protocol work after that checkpoint is loss/retransmission/timer behavior rather than additional listener lifetime variants.
Consolidated socket-checkpoint interoperability run `35538768718` exposed a real immediate-post-accept race on both ARM64 PyDECnet scenarios while both x86_64 PyDECnet scenarios and both Route20 L1 scenarios passed. PyDECnet received CC for all four object-246 children, but the fourth Linux `accept()` returned `ECONNREFUSED`; the peer then saw recovery rejected with Session reason 6 after the failed server closed its listener. The race is in `dniv_sock_accept_impl()`: after successfully sending CC it waits to sample NSP RUN, but a fast peer disconnect can advance CC -> RUN -> CLOSED before that waiter runs. The accepted child was then incorrectly discarded even though its terminal reason/data remained available in the NSP connection. The socket accept path now treats that retained CLOSED state after successful local CC as an accepted-then-terminated child, returns the socket, and lets normal `recv()` EOF plus `DSO_DISDATA` semantics expose the peer termination. Other accept failures still fail normally. Exact-SHA consolidated acceptance is required next.


Exact-SHA consolidated socket acceptance for `dc62cdb2ab1494069fe8d2d1c546a18d0ae8a279` is fully green: Repository Policy `35540170709`, Build Bootstrap `35540191816`, Project State Gate `35540192698`, External Reference Baselines `35540193641`, VM Lab `35540194520` and `35540195378`, and Independent Ethernet Interoperability `35540196173`. All six consolidated interop jobs passed, including both formerly failing ARM64 PyDECnet simultaneous-termination cases and their recovery connection.

Phase 5 resumes at NSP loss/retransmission/timers. DNA NSP V4.0.1 sections 2.6.3.1 and 7.1 require timed Data Segment retransmission to remain subject to current normal-data flow-control permission. The implementation previously honored XOFF for new sends but not for expired retransmit entries, so a timed DATA retransmission could violate withdrawn permission and consume retry budget. DATA retransmission is now gated by `data_xon`; while XOFF is active it neither transmits nor increments retry count, while OTHER retransmission remains eligible. An expired DATA entry becomes eligible on the next timer pass after XON. Shared unit coverage locks the gating rule. Next: exact-SHA fast socket acceptance, then deterministic live packet/ACK-loss interoperability coverage.


Exact-SHA fast acceptance of XOFF-gated DATA retransmission candidate `c344fe2e639833f9c0c4e9783e53b8cdc0672da0` is green: Repository Policy `35541472035`, Build Bootstrap `35541493006`, Project State Gate `35541493864`, E1 `35541494883` on both architectures, and x64 PyDECnet L1 interop `35541495840`.

The next Phase 5 candidate adds a deterministic live NSP retransmission discriminator. A native object-25 MIRROR connection sends one unique marked record while the host drops only PyDECnet unicast toward the candidate for eight seconds. The tc action counter must prove actual injection; PCAP must show the marked candidate data at least twice and a post-fault reference response. Multicast hellos/routing stay live. No product protocol behavior changes in this test-only successor. Next: exact-SHA fast socket acceptance, then retry-exhaustion/recovery coverage.


Exact-SHA fast acceptance for live NSP loss candidate `900b11f553790286cb26b6d8517255c3bc488e32` is green: Repository Policy `35542070424`, Build Bootstrap `35542088254`, Project State Gate `35542089304`, E1 `35542090329` on both architectures, and x64 PyDECnet L1 interop `35542091425`. PCAP proves one marked application send was retransmitted after deterministic reference-to-candidate unicast loss and then completed after fault removal.

Retransmit-limit semantics are corrected next. The old timer path erased the connection on retry exhaustion, losing the normative/independent-reference reason 39 and making sockets observe clean EOF. Exhaustion now retains a CLOSED connection with node-unreachable reason 39 until socket release. Socket receive/send status maps that terminal condition to `EHOSTUNREACH`, poll includes `EPOLLERR|EPOLLHUP`, and `DSO_DISDATA` can retrieve reason 39. Normal peer disconnects still produce EOF. Next: exact-SHA fast socket acceptance, then deterministic live exhaustion/recovery proof.


Exact-SHA fast acceptance for retransmit-exhaustion semantics candidate `9f486435c5c35576c6ae2ce17b41b1a06a8a419c` is green: Repository Policy `35542589965`, Build Bootstrap `35542611007`, Project State Gate `35542612321`, E1 `35542613688` on both architectures, and x64 PyDECnet L1 interop `35542615041`.

The next test-only successor holds deterministic reference-to-candidate unicast loss through the complete five-attempt NSP response budget. Native userspace requires `EHOSTUNREACH`, `DSO_DISDATA` reason 39, `POLLERR|POLLHUP`, five marked candidate transmissions on pcap, a nonzero injector counter, and a successful fresh MIRROR connection after fault removal. Next: exact-SHA fast socket acceptance, then connection-establishment/control retransmission and timer-boundary coverage.


Exact-SHA fast acceptance for established-link retry exhaustion candidate `e803482a0cac568e44708fcdd1fa6d54e38bb5ef` is green: Repository Policy `35543151094`, Build Bootstrap `35543165444`, Project State Gate `35543166349`, E1 `35543167217` on both architectures, and x64 PyDECnet L1 interop `35543168107`. The five-attempt loss sequence, node-unreachable socket semantics and fresh-link recovery are all proven.

The next test-only successor exercises connection-control retry exhaustion: reference-to-candidate unicast is blackholed before a marked MIRROR `connect()`, which must return `EHOSTUNREACH`; pcap must contain one CI plus four RCIs, followed after fault removal by a successful fresh marked connection and echo. Next: exact-SHA fast socket acceptance, then delayed-ACK/NAK/flow-control timer edges.


Exact-SHA fast acceptance for connection-control retry candidate `2c646d82a0f6a7c49df17ebafae67785185a383f` is green: Repository Policy `35544499145`, Build Bootstrap `35544514739`, Project State Gate `35544515810`, E1 `35544516934` on both architectures, and x64 PyDECnet L1 interop `35544517875`. PCAP proved the initial CI, four RCIs, and fresh-link recovery data.

The next Phase 5 correction removes an exact-only ACK_OTHER special case. Other-Data ACKNUM is cumulative just like Data ACKNUM, and both pinned PyDECnet and LinuxDECnet retire acknowledged Other-Data through the cumulative number. The common wrap-safe ACK/NAK path now applies to both channels, preventing lost early ACKs from leaving stale interrupt/link-service retransmit entries. Next: exact-SHA fast socket acceptance, then sequencing/resource and malformed-ACK negatives.


Exact-SHA fast acceptance for cumulative ACK_OTHER candidate `d62bf7cb5c86ca5cee2f0c199872978ead6828c6` is green: Repository Policy `35544925880`, Build Bootstrap `35544944645`, Project State Gate `35544945676`, E1 `35544946708` on both architectures, and x64 PyDECnet L1 interop `35544947674`.

Phase 5 receive buffering now fixes a concrete bounded-cache deadlock. The old global 32-entry limit could be filled entirely by unacknowledged future segments, after which the missing expected segment was rejected and the cache could never advance; it could also let normal-data backlog block Other-Data. DNA permits future segments to be discarded instead of cached and requires retention/admission of the missing lower segment ahead of cached higher segments. Bounds are now per subchannel. Full-cache future arrivals are left unacknowledged, while an expected arrival reclaims the farthest cached future on that subchannel before admission. Next: exact-SHA fast socket acceptance, then malformed ACK-field and sequence-boundary negatives.


Exact-SHA fast acceptance for receive-cache candidate `551029224a621c5f11b3ae3a280c51e14b965ca0` is green: Repository Policy `35545504071`, Build Bootstrap `35545526646`, Project State Gate `35545527462`, E1 `35545528555` on both architectures, and x64 PyDECnet L1 interop `35545529422`.

A harness audit invalidates the earlier listener-close proof: its special close-listener-with-pending-requests block was unreachable because it was nested under mutually exclusive `close_race` mode. Listener-close is now its own path: accept exactly one child, close the listener while two requests remain pending, prove the accepted child still echoes, and let PyDECnet require the other two outcomes to be reason-6 object-busy rejects. No product behavior changes. Next: exact-SHA fast socket acceptance; fix any concrete product failure exposed by the corrected discriminator before continuing NSP timer work.


Exact-SHA fast acceptance for repaired listener-close coverage `dfd249b02fe0b72b87d088523a9603630a641290` is green: Repository Policy `35545967240`, Build Bootstrap `35545980316`, Project State Gate `35545981269`, E1 `35545982418` on both architectures, and x64 PyDECnet L1 interop `35545983392`. The object-245 path really ran: accepted=1 and busy=2.

Connection-response timeouts are corrected next. CI/CC no longer lose their retry-limit/node-unreachable semantics merely because delayed timer work crosses the generic 30-second deadline. An unanswered inbound CR now emits DI reason 38 through the reliable control retransmit path; an outbound CD response timeout retains CLOSED reason 38 locally. Terminal pre-accept links are removed immediately from listener pending rings so timeout or peer abort cannot leak backlog capacity. Next: exact-SHA fast socket acceptance, then bounded timer-specific evidence.


Exact-SHA fast acceptance for connection-response timer semantics `ad754ae503ba0bf7d0fa9b7d877db6c148dae547` is green: Repository Policy `35546873484`, Build Bootstrap `35546897584`, Project State Gate `35546898681`, E1 `35546899879` on both architectures, and x64 PyDECnet L1 interop `35546901103`.

The next test-only successor adds one consolidated/full-only timer proof on x64 PyDECnet L1. Object 247 leaves the first CI in CR past 30 seconds; the peer must receive reason 38 and pcap must contain candidate DI reason 38. A second connection to the backlog-one listener must then accept and echo `DNIV-CR-TIMEOUT-RECOVER`, proving terminal pre-accept cleanup released the pending slot. Fast runs do not execute this 30-second timer case. Next: consolidated exact-SHA acceptance, then DC/no-resources and malformed ACK/sequence negatives.


Consolidated run `35547989271` found a concrete E4 harness race, not a timer-proof protocol failure: ARM64 L1A self-terminated after the fixed 600-second router sleep before both endpoints completed, and the controller reported `E4 guest exited early: L1A`. Transit-router lifetime is controller-owned, so E4 L1/L2 guests now stay alive indefinitely after READY and are stopped by controller QMP teardown. The new x64 PyDECnet timer proof on `4263ee43307c5c6842bcb8b2f342a05f5d27949d` itself passed reason 38 plus fresh connection recovery and PCAP evidence. Next: repeat exact-SHA consolidated acceptance.


Exact-SHA consolidated acceptance for `8cf0e121ebe273447c500381ebe44ee374c440cc` is green: policy `35549058759`, build `35549108369`, state `35549109446`, references `35549110505`, E1 `35549111412` both architectures, E4 `35549112413` both architectures, and six-job interop `35549113555`.

The next Phase 5 parser correction rejects trailing non-ACK bytes in ACK_DATA/ACK_OTHER while preserving the Phase IV/PyDECnet rule that an optional ACK word with invalid QUAL is ignored after consuming that word. Unit negatives cover both. Next: exact-SHA fast socket acceptance, then reserved-port No Resources/No Link receive-dispatch semantics.


Exact-SHA fast acceptance for `37f82031790e7da584940259703f94074204a5fc` is green: policy `35550252765`, build `35550274983`, state `35550276263`, E1 `35550277558` both architectures, and x64 PyDECnet L1 interop `35550278702`.

Reserved-port semantics are corrected next: CI/RCI allocation failure sends DC reason 1 (No Resources); unmapped or wrong-source Data/Interrupt/Link-Service sends DC reason 41 (No Link), while bad-mapped ACK remains ignored. Received DC reasons are retained in CLOSED state until socket release instead of immediately erasing the connection, preserving rejection/termination status for userspace. Next: exact-SHA fast socket acceptance, then bounded independent No Resources/No Link wire proof and sequence-boundary negatives.


Exact-SHA fast acceptance for `b6932bc3bbb1589a9d7022cb714deb16e1170d7d` is green: policy `35550914584`, build `35550933006`, state `35550934357`, E1 `35550935699` both architectures, and x64 PyDECnet L1 interop `35550937333`.

Pinned PyDECnet receive-dispatch semantics add CC and DI to the No-Link response set. Candidate unknown-destination and wrong-source mapping now send DC reason 41 for CC, DI, Data, Interrupt and Link Service; ACK/DC remain excluded. Next: exact-SHA fast socket acceptance, then independent reserved-port wire proof and sequence-boundary negatives.


Exact-SHA fast acceptance for complete NSP No-Link receive dispatch `e80644dd485ea12815ab87e57904e57dd9ca9339` is green: policy `35551483227`, build `35551507570`, state `35551508723`, E1 `35551509771` on both architectures, and x64 PyDECnet L1 interop `35551510921`.

The next test-only successor adds an x64 PyDECnet L1 reserved-port wire proof after ordinary socket tests: one unknown-link CC must yield DC reason 41, and 320 unique valid CIs must exhaust the 256-entry table and yield DC reason 1. Candidate progress is tied to a >=260 non-hello receive delta, while PCAP independently requires both response reasons. Unit tests add exact half-range and wrap-boundary negative sequence cases. Next: exact-SHA fast socket acceptance, then flow-control/delayed-ACK and malformed receive-state negatives.


Reserved-port proof candidate `dda5f6d747ea026d4735280ed1ae2ef0a2e12bb8` failed only interop job `106189987949`: 320 CIs were injected, but the live PyDECnet peer was receiving candidate rejects and confirming them, so slots recycled before persistent table exhaustion and no reason-1 DC appeared in PCAP. The harness now isolates candidate-to-peer unicast on TAP egress only during the synthetic burst, requires a nonzero tc match counter, preserves bridge capture of candidate responses, and removes the filter immediately after the candidate observes the burst. No product change. Next: exact-SHA fast socket acceptance.


The peer-isolation rerun `25a3e47fedc5b87ecac6d09aef795ce340e2f2be` exposed the preceding injector defect: synthetic short-data used the host bridge MAC, so the candidate Routing Layer correctly rejected it before NSP because data source identity must map to an UP DECnet adjacency. The injector now uses the canonical DECnet MAC derived from the embedded PyDECnet source node. Combined with the TAP-egress reply isolation, this should both admit the synthetic CIs and prevent the live peer from recycling connection slots during the burst. No product change. Next: exact-SHA fast socket acceptance.


Exact-SHA fast acceptance for reserved-port wire proof `6c54859389257f7337c044f79c29b1a1e2ec8f35` is green: Repository Policy `35554033965`, Python QEMU VM Lab `35554063023`, and x64 PyDECnet L1 Independent Ethernet Interoperability `35554064466`. The corrected injector reached NSP with a canonical DECnet source MAC; reply isolation held slots long enough to produce both DC reason 41 (No Link) and DC reason 1 (No Resources). Next: flow-control/delayed-ACK and malformed receive-state negatives.


Exact-SHA fast acceptance for continuity commit `a16acc77b63ac2faf6b21a1bd53f44cceca323b1` is green: Repository Policy `35555005673`, Build Bootstrap `35555026456`, Project State Gate `35555027572`, E1 `35555028576` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35555029675`.

The next Phase 5 correction tightens delayed-ACK semantics against the pinned PyDECnet reference. Once a delayed ACK is pending, additional DLY data no longer restart the holdoff deadline, preventing sustained traffic from postponing the ACK indefinitely. Only normal Data Segment bit 12 is treated/emitted as DLY; Interrupt and Link Service sequence high bits are ignored rather than creating a delayed-ACK request, matching their fixed `dly=0` reference semantics. Shared unit coverage locks the non-sliding holdoff and other-data high-bit behavior. Next: exact-SHA fast socket acceptance, then remaining malformed receive-state negatives.


Build Bootstrap `35555522237` rejected delayed-ACK candidate `e8a21b8230d0e07dbc7b5b986574c66ff993da9e` on x64 unit coverage before kernel build: the new other-data high-bit vectors set bit 15, which is the ACK-present discriminator and therefore correctly entered optional-ACK decoding instead of the sequence field. The vectors now exercise only reserved sequence bits 12-14 (`0x70` high byte), leaving bit 15 clear. Product code is unchanged. Next: exact-SHA fast socket acceptance.


Exact-SHA fast socket acceptance for `33385c8abce1533ab602bb191404e19961b740df` is green: Repository Policy `35555625293`, Build Bootstrap `35555646942`, Project State Gate `35555647917`, E1 `35555648936` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35555649884`.

Receive-state review found a loss-recovery gap for duplicate Connect Confirm. The pinned PyDECnet RUN handler explicitly acknowledges a retransmitted `ConnConf`; the candidate instead rejected CC once it had entered RUN. If the first data ACK of CC were lost, that could leave the peer retransmitting CC until failure. CC receive handling now distinguishes first acceptance (CI/CD), duplicate re-ACK (RUN), and invalid states; duplicate CC sends ACK_DATA(0) without replacing accept data or changing state. Shared state-action unit coverage locks the valid/duplicate/invalid matrix. Next: exact-SHA fast socket acceptance, then continue receive-state loss negatives.


Exact-SHA fast acceptance for duplicate-CC recovery candidate `3a7bc799b43fb14ad390aa4b9bed2288b6305ec4` is green: Repository Policy `35556210489`, Build Bootstrap `35556235889`, Project State Gate `35556237313`, E1 `35556239238` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35556240811`.

Pinned PyDECnet restarts RUN inactivity on every received packet before handling a duplicate Connect Confirm. The candidate now does the same while re-ACKing duplicate CC, so loss of the original CC acknowledgement cannot leave an otherwise active link carrying a stale inactivity deadline. First CC acceptance still records accept data exactly once; duplicate CC remains idempotent apart from ACK/rearm. Next: exact-SHA fast socket acceptance, then continue receive-state loss negatives.


Exact-SHA fast socket acceptance for inactivity-rearm candidate `136aedce9aa49f0344d2d082cc9c9057575507ff` is green: Repository Policy `35557146528`, Build Bootstrap `35557164398`, Project State Gate `35557165658`, E1 `35557167128` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35557168496`.

Receive-state cross-check found duplicate CI/RCI handling was still too broad. Pinned PyDECnet only re-ACKs an already-mapped incoming CI while the connection is CR or CC; once that link has advanced to RUN or a terminal state, the unexpected duplicate is discarded. Route20 agrees that RUN/default states do not emit a new Connect Ack. The candidate previously sent ACK_CONN for any remote-link match, including RUN/CLOSED. Duplicate CI/RCI re-ACK is now restricted to CR/CC, with all other mapped states discarded without Session re-notification or wire response. Shared unit coverage locks the state matrix. Next: exact-SHA fast socket acceptance, then continue receive-state loss negatives.



Exact-SHA fast acceptance for duplicate-CI mapping candidate `1d39475fd258f877063809f26b0cc6c7a9072bef` is green: Repository Policy `35557627099`, Build Bootstrap `35557644703`, Project State Gate `35557646170`, E1 `35557647643` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35557648866`.

Receive-state mapping review found stale Connect Ack handling differed from the pinned PyDECnet NSP port-mapping rules. ACK_CONN is valid only while an outbound connection is in CI; once the connection has moved to CD, RUN, or any other state, the packet must be silently discarded. The candidate previously returned `EINVAL` for these stale/duplicate ACK_CONN packets. It now drops them without changing state or producing a response, while CI still clears the outstanding connect control and enters CD. Shared unit coverage locks the one-valid-state matrix. Next: exact-SHA fast socket acceptance, then continue receive-state and flow-control loss negatives.


Exact-SHA fast acceptance for stale-ACK_CONN discard candidate `2958449c9c37d7c8a3fec2141bf08f65e3a05595` is green: Repository Policy `35558135839`, Build Bootstrap `35558155273`, Project State Gate `35558156428`, E1 `35558157531` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35558158493`.

Malformed-control review against the pinned PyDECnet packet decoder and LinuxDECnet DC receive path found that fixed-format control packets still accepted unparsed trailing bytes. Connect Confirm and Disconnect Initiate now require their declared image field to consume the entire packet, and Disconnect Confirm now requires its exact seven-byte wire length. CI/RCI remain variable-payload messages and are unchanged. Unit negatives lock trailing-byte rejection for CC, DI and DC. Next: exact-SHA fast socket acceptance, then continue receive-dispatch silent-drop and flow-control negatives.


Exact-SHA fast acceptance for malformed-control candidate `ef929e81326dbce0ea9dda270159429dd0a98dc9` is green: Repository Policy `35558671026`, Build Bootstrap `35558692168`, Project State Gate `35558693029`, E1 `35558693935` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35558694959`.

Control-message compatibility review found that the empty Session Control I field is optional on the wire. Pinned PyDECnet's tolerant I decoder accepts a fixed-header-only Connect Confirm or Disconnect Initiate as an empty field, and LinuxDECnet likewise only parses the field when bytes remain. The candidate previously rejected those valid empty forms. CC now accepts its 9-byte fixed header and DI its 7-byte fixed header/reason as empty Session Control data, while retaining exact declared-length consumption, the 16-byte bound, and exact-length DC validation. Unit coverage locks both omitted-I forms. Next: exact-SHA fast socket acceptance, then continue receive-dispatch silent-drop and flow-control negatives.


Exact-SHA fast acceptance for omitted-control-I candidate `b91e122e97a77944358297c0bb507f2d736e684b` is green: Repository Policy `35559162597`, Build Bootstrap `35559181671`, Project State Gate `35559182650`, E1 `35559183636` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35559184617`.

Socket flow-control review found the normal-data writability gate counted all NSP retransmit entries against the 20-segment Data window. Other-Data traffic therefore reduced the effective Data window and could leave a blocking writer asleep after a Data ACK even though `dniv_nsp_send_data()` itself had room to send. Connection snapshots now expose per-subchannel outstanding counts, and send admission plus poll/wait readiness use the Data count for the 20-segment window while still enforcing the shared 64-entry retransmit bound and XON state. Unit coverage locks the mixed Data/Other case, the Data-window limit, the global limit and XOFF. Next: exact-SHA fast socket acceptance, then add outbound DLY selection and continue flow-control/loss negatives.

Exact-SHA fast socket acceptance for data-window readiness candidate `0d916012d551943e3f20f03c4b8ca1a0c7b87694` is green: Repository Policy `35592768107`, Build Bootstrap `35592803268`, Project State Gate `35592806080`, E1 `35592808968` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35592812174`.

Pinned PyDECnet Phase IV transmit behavior requests delayed acknowledgement only while the Data queue is at most half full before enqueue. Native outbound Data now sets DLY at that same boundary: with the 20-segment window, existing outstanding counts 0 through 10 request delayed ACK, while 11 and above request immediate ACK. The wire flag is chosen before queueing so retransmissions preserve the original packet image. Unit coverage locks the boundary. Next: exact-SHA fast socket acceptance, then continue flow-control/loss negatives and clean-shutdown ordering.

Exact-SHA fast socket acceptance for outbound-DLY candidate `80f31de7ebf970e648878c7cac51746da3ac0715` is green: Repository Policy `35593564409`, Build Bootstrap `35593599143`, Project State Gate `35593601067`, E1 `35593603024` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35593605238`.

Clean local disconnect now follows the pinned PyDECnet ordering when normal Data remains unacknowledged: RUN is retained, the disconnect reason/data is saved, new Data/Interrupt transmission is blocked, delayed receive ACKs are suppressed, and DI is deferred until the Data retransmit queue is fully acknowledged. Socket writability is suppressed during that drain and inactivity keepalive generation is paused. Unit coverage locks the defer and delayed-ACK shutdown predicates. Next: exact-SHA fast socket acceptance, then add a deterministic live clean-shutdown/loss proof and continue NSP flow-control negatives.

Exact-SHA fast socket acceptance for clean-disconnect ordering candidate `1dadf0026e05d96eb8e4ae9fe528079970a0b4b1` is green: Repository Policy `35594478614`, Build Bootstrap `35594516941`, Project State Gate `35594519476`, E1 `35594522083` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35594524504`.

The next test-only successor adds deterministic live clean-shutdown loss proof against PyDECnet MIRROR. It drops reference-to-candidate unicast after a tagged Data send is queued and the application closes, holds the fault long enough to require retransmission, then restores the path and requires fresh MIRROR recovery. PCAP validation binds the tagged Data and subsequent DI by NSP source link and requires at least one retransmission before DI, proving local close drains outstanding normal Data before disconnect. No product behavior changes. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

Fast acceptance of live clean-drain proof candidate `ede2ac119f4d182b0e09ff798659c9bf182672cf` failed only x64 PyDECnet interoperability run `35595756498` during candidate-image preparation. The image script asserted that `dndrain` existed but the compile stanza had not been added, so the proof never ran and no protocol behavior was exercised. This follow-up adds the missing build/install command; product code and test semantics are unchanged. Next: exact-SHA fast socket acceptance.

Exact-SHA fast socket acceptance for live clean-drain proof candidate `d6bb4d9b74128caaff36d40582f5ccea5c6f3d74` is green: Repository Policy `35595967423`, Build Bootstrap `35596006568`, Project State Gate `35596008127`, E1 `35596009930` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35596011706`. The tagged loss proof confirms that local close retransmits unacknowledged normal Data before DI and recovers cleanly afterward.

Malformed optional-ACK review found that ACK words with the present bit set but reserved qualifier values 4-7 were consumed and silently ignored on Data/Interrupt/Link-Service packets. Optional ACK decoding now reports that malformed condition to the packet parser, which rejects the packet instead of continuing at the following sequence field. Existing regression coverage that previously documented the permissive behavior now requires rejection. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

The optional-ACK qualifier change in `fe9056d156809512cf78e0b420ba1391bddb905e` was superseded before acceptance: pinned PyDECnet intentionally consumes ACK-present words with reserved QUAL 4-7 and ignores their value, exactly matching the candidate's prior behavior. That tolerance is restored. The same fixed-format review found a real parser gap instead: Link Service accepted trailing bytes after LSFLAGS/FCVAL, while the pinned PyDECnet fixed layout has no payload. Link Service now requires exact packet consumption, with a trailing-byte negative vector. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

Exact-SHA fast socket acceptance for Link Service fixed-length candidate `4c129fc36c0213b6d0828a342f4541c618fe8d4a` is green: Repository Policy `35597317720`, Build Bootstrap `35597360915`, Project State Gate `35597364000`, E1 `35597366693` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35597369540`.

NSP segment-size negotiation now follows the pinned PyDECnet behavior instead of always transmitting at the local 563-byte MSS. Incoming CI and first CC record min(peer SEG_SIZE, local MSS) on the connection; normal-data send enforces that negotiated bound; socket record segmentation uses it; and DSO_LINKINFO reports it. Zero peer segment size remains unusable rather than being silently promoted. Shared unit coverage locks smaller-peer, larger-peer and zero boundaries. Next: exact-SHA fast socket acceptance, then add independent small-segment negotiation proof and continue NSP flow-control/loss negatives.

Exact-SHA fast socket acceptance for negotiated-segment candidate `451affd5a89dc743833fb46cc765336ea2b17dec` is green: Repository Policy `35598667492`, Build Bootstrap `35598709272`, Project State Gate `35598711510`, E1 `35598714232` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35598716636`.

Socket-scope PyDECnet L1 interoperability now turns the pinned peer into a protocol-valid 128-byte NSP segment-size oracle without modifying its tracked source: a runtime-only `sitecustomize.py` lowers PyDECnet's imported MSS before NSP loads. PCAP validation requires a reference CC advertising 128, rejects any candidate Data payload above that negotiated limit, and requires multiple full 128-byte candidate segments so the existing 563/564/4096-byte MIRROR records prove real segmentation rather than merely accepting the option. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

Small-segment proof candidate `ce46f19e2cb8946e224b74288da838719dbc1eb6` built green and passed E1 on both architectures, but x64 PyDECnet interoperability run `35599580242` failed only the clean-drain PCAP postcondition. Exact packet evidence shows the candidate did the safe thing: tagged Data on local link 42 was retransmitted five times and no DI was sent because the small-segment PyDECnet peer never acknowledged the duplicate after the injected reply-loss window. The validator incorrectly required DI to eventually appear. It now enforces the actual ordering invariant instead: at least one Data retransmission is mandatory, and any DI for that link before a retransmission is a failure; absence of DI when the peer never acknowledges is valid. No product behavior changes. Next: exact-SHA fast socket acceptance.

Exact-SHA fast socket acceptance for clean-drain proof correction `8567fe8ac9c834d6de43c60112ff45c538584a18` is green: Repository Policy `35600584379`, Build Bootstrap `35600584505`, Project State Gate `35600586681`, E1 `35600588856` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35600591119`.

The next socket-scope PyDECnet proof exercises live normal-data XOFF/XON rather than only unit predicates. A fresh MIRROR connection emits a tagged Data segment; a host raw-frame injector derives that connection's NSP link pair from the live frame, sends in-sequence Link Service XOFF and XON packets as the reference node, and requires the nonblocking candidate send to return EAGAIN while XOFF is active and a blocking send to resume after XON. The injector also observes the resumed tagged Data on the wire. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP loss/flow-control negatives.

Fast socket acceptance of live XOFF/XON proof candidate `7572f3e1a277ff0098cfde26ba135f762b65f5fa` failed only x64 PyDECnet interoperability run `35601855043`. The candidate-side nonblocking send remained writable because the injector never saw the tagged candidate frame: an AF_PACKET socket bound to the Linux bridge does not receive the QEMU TAP ingress frame used for discovery, although bridge transmission works for injection. Packet capture confirms no injected Link Service frame was emitted. The injector now sniffs the candidate TAP, where QEMU ingress is directly visible, while retaining a separate bridge-bound socket for reference-identity XOFF/XON transmission and capture visibility. Product code is unchanged. Next: exact-SHA fast socket acceptance.

The first TAP-sniff correction candidate `4137a8f9cc25b5a6967ae600be641950e657c161` still failed only the live flow-control proof in x64 PyDECnet run `35602424316`; no Link Service injection appeared in the PCAP, so product flow control was not exercised. The remaining harness defect is packet-socket protocol demultiplexing on TAP ingress: the custom EtherType socket saw no QEMU-injected frame. The sniffer now opens ETH_P_ALL and filters DECnet in its parser, while the separate transmit socket remains scoped to EtherType 0x6003. Explicit flushed injector milestones make subsequent evidence unambiguous. Product code is unchanged. Next: exact-SHA fast socket acceptance.

Fast socket acceptance of TAP-capture candidate `6844dd79c5fddd8834c1fcd16b96fd68ea6be770` failed only x64 PyDECnet interoperability run `35603117115`. The live flow injector opens AF_PACKET/SOCK_RAW sockets and was invoked without privilege; the runner therefore never injected XOFF/XON, while the candidate correctly remained writable. The orchestration now runs only that raw-socket injector under sudo, matching the existing reserved-port raw injector. Product code and proof semantics are unchanged. Next: exact-SHA fast socket acceptance.

Fast socket acceptance of candidate `39084fc71829380393dd7bc1f1b9e7ae18274807` failed only x64 PyDECnet interoperability run `35603708003`. Repository inspection found the decisive harness gap: the candidate-side `dnflow` probe and workflow selector were present, but the host-side `inject-nsp-flow.py` orchestration block had never landed in `run-interop.sh`. Therefore no XOFF/XON packet could be sent. The missing bounded orchestration is now inserted after clean-drain recovery, using privileged bridge sniff/injection and requiring both FLOW-READY and FLOW-PASS markers. Product code is unchanged. Next: exact-SHA fast socket acceptance.

Static review of live flow-control candidate `7ce48ebc2978a45f6f1ab22c29cf95344acf7012` found a deterministic harness error before acceptance completed: the XON transmission still referenced the removed single-socket variable `sock` instead of the split transmit socket `send`. XOFF could be injected, but the injector would raise NameError four seconds later and never release the blocked candidate writer. The one stale identifier is corrected; product code and protocol semantics are unchanged. Next: exact-SHA fast socket acceptance.

Fast socket acceptance for live XOFF/XON candidate `f0f700e836f67360f1505b4595bede323cd0911c` proved the new flow-control scenario end to end in PyDECnet run `35604824026`: the injector observed tagged Data, sent XOFF, the candidate blocked, XON resumed transmission, and the injector observed the resumed record. The run later failed only the older clean-drain PCAP assertion. Exact timestamps show its 4-second reply-drop window ended before the candidate's 5-second retransmission deadline plus one-second timer granularity; PyDECnet's retransmitted echo then supplied the ACK first. The clean-drain fault is extended to 7 seconds so at least one candidate Data retransmission is forced deterministically before replies can recover. Product code is unchanged. Next: exact-SHA fast socket acceptance.


Exact-SHA fast socket acceptance for deterministic clean-drain candidate `1b3c29d74dc9ecb686a10c621c8a011810240d53` is green: Repository Policy `35605567036`, Build Bootstrap `35605622749`, Project State Gate `35605626090`, E1 `35605629674` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35605633210`. The clean-drain proof now deterministically includes a candidate retransmission before recovery; the live XOFF/XON proof also remained green.

Flow-control negative coverage now extends that live connection with sequence-order checks. After XOFF sequence 1, the injector sends future XON sequence 3 before the candidate's nonblocking blocked check; only in-order XON sequence 2 may release the writer. It then injects stale duplicate XOFF sequence 1, waits for it to settle, and requires a second tagged nonblocking candidate record to remain writable. The injector requires both resumed tags. This proves future Link Service state is cached without early application and stale duplicates cannot reapply flow state. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

Exact-SHA fast socket acceptance for ordered flow-control candidate `67324f617646d61c4d22d1ea8c38eac985853226` is green: Repository Policy `35607362462`, Build Bootstrap `35607414708`, Project State Gate `35607417241`, E1 `35607419674` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35607421967`. Live evidence shows future XON was held until sequence closure and stale duplicate XOFF did not reapply flow state.

The same live flow proof now covers the timeout interaction required by Phase IV flow control. During the fresh MIRROR connection, the harness drops all real PyDECnet replies while raw Link Service injection remains available. XOFF is held across the candidate's five-second Data response deadline; the injector fails on any tagged Data retransmission during that interval. After in-order XON closes the sequence gap, the already-due tagged Data must retransmit, while the blocked new record and post-stale-XOFF record must also appear. The fault filter must match at least one real reference reply, preventing a vacuous proof. This matches the pinned PyDECnet rule that an expired Data retransmit remains unsent while flow is XOFF and is eligible again after XON. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

Static review of XOFF retransmit candidate `0d89cb7426c2ea691d14763a2cf4a65514252a0b` found a deterministic orchestration race before acceptance completed: the new reference-reply drop was installed immediately after FLOW-READY, but the candidate intentionally waits two seconds before starting `dnflow`, so the filter could suppress the MIRROR connection handshake itself rather than only its Data acknowledgments. The flow probe now publishes FLOW-CONNECTED after the independent peer has accepted the logical link and pauses three seconds; only then does the host install the reply-drop filter and start the raw Link Service injector. This makes the fault window begin after connection establishment and before the tagged Data is sent. Product behavior and proof assertions are unchanged. Next: exact-SHA fast socket acceptance.

Repository Policy `35608428544` on XOFF retransmit candidate `0d89cb7426c2ea691d14763a2cf4a65514252a0b` stopped before dispatch because the multi-file tree update accidentally reset `tests/lab/run-interop.sh` from executable to regular-file mode. The harness contents were not implicated. The executable bit is restored while retaining the post-connect fault-window correction. Product behavior and test semantics are unchanged. Next: exact-SHA fast socket acceptance.

Exact-SHA fast socket acceptance for XOFF retransmit candidate `44f8dbe137ac5a4e6a1430e7869e110d89d74b50` is green: Repository Policy `35608774196`, Build Bootstrap `35608813361`, Project State Gate `35608815868`, E1 `35608818460` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35608821041`. The live proof holds XOFF across the response deadline, observes no forbidden retransmission, then observes the due retransmission after XON.

The next socket-scope loss negative proves that an out-of-range cumulative Data ACK cannot falsely drain the retransmit queue. A fresh MIRROR connection sends tagged Data while real PyDECnet replies are dropped. A raw reference-identity injector derives the live link pair and Data sequence, sends an ACK seven sequence numbers ahead of the only outstanding segment, and requires the tagged Data to retransmit on its normal response timer. The fault filter must match real reference replies, then the path is restored and the same logical link must receive the correct MIRROR echo. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP ACK/NAK and flow-control negatives.

Exact-SHA fast socket acceptance for out-of-range ACK candidate `bafd77e440f78d843f19b572f4bed4a6ee6eaba2` is green: Repository Policy `35610134250`, Build Bootstrap `35610181088`, Project State Gate `35610184033`, E1 `35610187754` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35610191317`. Live evidence shows forged cumulative ACK 8 against the sole outstanding Data sequence 1 was ignored and the tagged segment retransmitted on its normal timer.

The same fault window now adds explicit NAK behavior. After the timer retransmission proves the forged future ACK did not drain state, the injector sends a Data NAK acknowledging sequence zero, i.e. immediately before the first outstanding sequence one. The candidate must retransmit the tagged Data again within 2.5 seconds, well before another normal five-second response timeout. This locks the intended Phase IV NAK fast-retransmit path without relying on the independent peer to originate a NAK. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP ACK/NAK and flow-control negatives.

Exact-SHA fast socket acceptance for NAK candidate `b20b605229ae384645f2c725569a9e3984cff669` is green: Repository Policy `35611234764`, Build Bootstrap `35611286702`, Project State Gate `35611289909`, E1 `35611293373` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35611296994`. The live loss window proves a Data NAK for sequence zero forces prompt retransmission of outstanding sequence one.

ACK/NAK negative coverage now extends the same live outstanding Data segment across the Phase IV cross-subchannel qualifiers. An explicit ACK_OTHER carrying XACK with a forged future cumulative ACK must not drain the Data retransmit queue; the next tagged retransmission must arrive on the normal response timer rather than immediately. ACK_OTHER carrying XNAK for the sequence immediately before the outstanding Data must then force another prompt retransmission. This proves cross-subchannel routing, cumulative range validation, and NAK fast retransmit together. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP sequence-wrap, ACK/NAK and flow-control negatives.

Fast socket acceptance of cross-channel ACK candidate `c87526d40c67a87c916f43878f036f4728ecb15c` failed only x64 PyDECnet interoperability run `35612571928`. The injector itself proved all four requested behaviors, but stacking the already-accepted normal future-ACK timer retransmit and normal NAK retransmit ahead of the new cross-XACK/XNAK pair consumed the candidate's five-attempt retransmit budget. After the fault filter was removed, the next timer therefore closed the link with destination-unreachable before a real reference ACK could recover it. The cross-channel proof is now isolated on a fresh connection: forged future ACK_OTHER/XACK must survive to the normal timer, then ACK_OTHER/XNAK must force prompt retransmit, leaving retry headroom for reference-path recovery. Product code is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP sequence-wrap and flow-control negatives.

Exact-SHA fast socket acceptance for isolated cross-channel ACK candidate `292f13708bdc9c3c26854d5ef45ea189d6831695` is green: Repository Policy `35613301589`, Build Bootstrap `35613361594`, Project State Gate `35613364677`, E1 `35613367237` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35613370335`. The fresh-link proof confirms forged future ACK_OTHER/XACK is ignored until the normal timer and ACK_OTHER/XNAK forces prompt retransmission while preserving recovery headroom.

The next socket-scope independent-peer proof drives one MIRROR logical link across the 12-bit NSP sequence rollover. With the fast socket peer advertising 128-byte segments, 130 verified 4096-byte records produce more than 4096 Data segments in each direction while retaining one connection. Every record is content-checked after reassembly; success therefore requires transmit sequencing, cumulative acknowledgments, receive ordering, reassembly, and reply sequencing to survive 4095-to-0 rollover against pinned PyDECnet. Product behavior is unchanged. Route20 VDE2 support requested for later distributed work is already present in the pinned fork at `a9ef7c0b7f875f0dd2e8abaf11213798a8e4474c` and remains the configured Route20 reference. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.

Exact-SHA fast socket acceptance for live sequence-rollover candidate `87b3b776dbd31253572bf63c0bcf1ea12f7dfaa9` is green: Repository Policy `35628701199`, Build Bootstrap `35628743562`, Project State Gate `35628746258`, E1 `35628749288` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35628752458`. The single MIRROR link exchanged 130 verified 4096-byte records with the peer advertising 128-byte segments, forcing normal-data sequence space through the 4095-to-0 rollover in both directions.

The cross-subchannel ACK loss proof now covers the exact modulo half-space boundary without spending another retransmit attempt. Before waiting for the one normal response timeout, the injector sends both the already-proven future ACK_OTHER/XACK at +7 and an ACK_OTHER/XACK exactly +2048 from the sole outstanding Data sequence. Neither may drain the retransmit queue or trigger an early retransmission; the same tagged segment must reappear only on the normal timer, after which XNAK must still force prompt retransmission. This locks the Phase IV 12-bit half-space ambiguity at the live packet path as well as in unit arithmetic. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP flow-control/loss negatives.


Exact-SHA fast socket acceptance for half-space ACK candidate `685b1f2b0d0df1448239bfd0f2240ca25ae9c220` is green after an exact-SHA rerun: Repository Policy `35631426475`, Build Bootstrap `35631471540`, Project State Gate `35631474220`, E1 `35631476753` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35631479293`. The first E1 attempt `35630924104` was invalidated before candidate execution on x86_64 by a transient runner APT repository HTTP 403; the unchanged rerun passed both architectures. Live interoperability proved both +7 future and exact +2048 half-space cross-XACKs leave the sole outstanding Data segment queued until its normal response timer, followed by prompt XNAK retransmission.

Other-Data loss coverage now adds a fresh MIRROR logical link carrying a tagged outbound NSP Interrupt. The host drops the independent PyDECnet ACK_OTHER, requires the candidate to retransmit the identical Interrupt only after its normal response timer, then injects a valid reference-identity ACK_OTHER for that sequence and watches through another response interval to prove retransmission stops. The fault filter must match at least one real peer reply, and the same logical link must complete normal MIRROR traffic after the fault is removed. This exercises Other-Data retransmit timing and ACK recovery independently of normal Data XOFF state. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP loss/flow-control negatives.


Exact-SHA fast socket acceptance for Other-Data loss candidate `eb1e70514ad3fee75d4bb40680a8f8bb0069585d` is green: Repository Policy `35632947071`, Build Bootstrap `35632991993`, Project State Gate `35632995040`, E1 `35632998357` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35633001550`. The live proof dropped the real ACK_OTHER, observed the tagged Interrupt retransmit only after the response timer, injected a valid ACK_OTHER, observed no further retransmit through another response interval, restored the independent path, and completed normal MIRROR traffic on the same logical link.

Interrupt flow-control coverage now separates ACK recovery from credit and exercises Other-Data sequence ordering. On a fresh MIRROR link the candidate sends one Interrupt, then must return EAGAIN for a second nonblocking Interrupt even after PyDECnet has acknowledged the first: ACK_OTHER is not a credit grant. The injector sends future Interrupt Request Link Service sequence 2 and requires the blocking second send to remain held, then sends missing sequence 1; sequence closure must release both the second Interrupt and an immediate third Interrupt using the cached second credit. Finally a stale duplicate sequence-1 grant with delta 100 must not recreate credit: a fourth nonblocking Interrupt must remain EAGAIN. Normal MIRROR data must still work afterward. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP loss/flow-control negatives.


Exact-SHA fast socket acceptance for interrupt-credit ordering candidate `1393a6da15ecc91bd701dffed2473e73127f1560` is green: Repository Policy `35635574606`, Build Bootstrap `35635622913`, Project State Gate `35635625856`, E1 `35635628952` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35635631736`. The live proof confirms ACK_OTHER does not replenish Interrupt credit, a future Interrupt Request grant remains cached until the missing Other-Data sequence arrives, sequence closure releases both credits, and a stale duplicate grant cannot recreate credit.


The live ACK-range fault now also covers spoofed connection identity without adding another retransmit cycle. While the real PyDECnet reply path is suppressed and one Data segment is outstanding, the injector sends a numerically valid ACK for that exact sequence from the correct DECnet node identity but with an incorrect NSP source link. The candidate must ignore it just like the future and half-space cross-ACKs: the tagged Data must remain queued until the normal response timer. This directly exercises PP-06 malformed/spoofed connection-ID handling on an established logical link while preserving retry headroom for XNAK and path recovery. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP loss/flow-control negatives.


Exact-SHA fast socket acceptance for spoofed-link ACK candidate `7435ee7a3fb688f0c37ae27b02160e8403c4b691` is green: Repository Policy `35639044766`, Build Bootstrap `35639090640`, Project State Gate `35639092916`, E1 `35639095518` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35639098368`. Live evidence shows a valid ACK number from the correct DECnet node but wrong NSP source link was ignored; the tagged Data remained outstanding until its response timer and the subsequent cross-XNAK fast retransmit still succeeded.


Spoofed-identity ACK coverage now includes all established-link identity axes in the same bounded loss window. Alongside the already-proven wrong NSP source-link ACK, the injector sends a valid ACK number to a non-existent local destination link and another valid ACK using the correct live link pair but a different DECnet source node/MAC identity. None may drain the retransmit queue or trigger early retransmission; the tagged Data must remain outstanding until the normal response timer, after which the existing XNAK proof still forces prompt retransmission. This closes PP-06 established-link spoof rejection for node identity plus both NSP connection IDs without consuming extra retry budget. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP loss/control negatives.


Unknown-link packet behavior is now proved alongside established-link spoof rejection. The injector sends a syntactically valid complete Data segment from the correct remote node identity but with deliberately unused source and destination NSP link IDs. The candidate must answer with Disconnect Confirm reason 41 (No Link), addressed back to those probe link IDs, while leaving the real connection and its outstanding tagged Data untouched. This covers the Phase IV reserved-port/no-link response path without risking the live peer connection. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue NSP lost-control and timer negatives.


Exact-SHA fast socket acceptance for unknown-link response candidate `983c8a0714c264f2d031cc4589f0c50ffd7a55e1` is green: Repository Policy `35641889672`, Build Bootstrap `35641930405`, Project State Gate `35641933003`, E1 `35641935798` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35641938325`. The established live link survived all spoofed ACKs; the synthetic unknown-link Data packet received Disconnect Confirm reason 41 (No Link) on the probe link IDs, and the real outstanding Data still followed its timer/XNAK recovery path.

Lost Connect Confirm acknowledgement now has a deterministic independent-peer proof. A deferred native listener accepts a PyDECnet CI but waits four seconds before issuing DSO_CONACCEPT, allowing the host to arm a reference-to-candidate unicast drop and raw observer before the first CC. PyDECnet receives the CC but its ACK_DATA(0) is dropped; the observer must see an identical candidate CC retransmission no earlier than the response timeout. After the fault is removed, the same logical link must enter RUN and complete an echoed record plus orderly peer disconnect. This turns the already-accepted duplicate-CC state logic into live loss/recovery evidence. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue lost disconnect/control and timer negatives.


Exact-SHA fast socket acceptance for lost-CC-ACK candidate `11519c7ea94c08eebe99cdf23629808063292d12` is green: Repository Policy `35643319482`, Build Bootstrap `35643360112`, Project State Gate `35643362852`, E1 `35643365745` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35643368217`. The raw observer saw the identical Connect Confirm retransmitted after 5.119 seconds while peer acknowledgements were dropped; after path restoration the same connection entered RUN, echoed data, and disconnected cleanly.

Disconnect control loss now has the matching live proof. A candidate MIRROR connection first exchanges normal data, then the host drops PyDECnet-to-candidate unicast before userspace calls SHUT_RDWR. The observer must see the identical Disconnect Initiate retransmitted only after the normal response timeout while at least one real peer confirmation is dropped. The socket remains open long enough for a later retransmitted DI to receive a confirmation after path restoration, then a fresh MIRROR connection must succeed. This proves DI control retransmission and stack recovery rather than merely application close ordering. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue control exhaustion, keepalive/inactivity and remaining PP-06 negatives.


Exact-SHA fast socket acceptance for lost-Disconnect-Confirm candidate `4c2e3f8f3cd40ee5c86bf398afbfd37a23f47ed8` is green: Repository Policy `35645429917`, Build Bootstrap `35645490482`, Project State Gate `35645493211`, E1 `35645496009` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35645498585`. The live proof observed an identical Disconnect Initiate retransmission only after the response interval while peer confirmations were dropped, then restored the path and verified a fresh MIRROR connection.


Disconnect-control exhaustion now extends the accepted transient loss proof. On a fresh MIRROR link, the host suppresses every PyDECnet-to-candidate unicast confirmation before native userspace calls SHUT_RDWR. The candidate must consume its five-attempt control retransmission budget, terminate the link with local reason 39 (node unreachable), expose that reason through DSO_DISDATA plus POLLERR|POLLHUP, and then recover on a fresh MIRROR connection after the fault is removed. This distinguishes terminal DI retry exhaustion from the previously accepted one-retry recovery path. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then keepalive/inactivity and remaining PP-06 negatives.


Exact-SHA fast socket acceptance for Disconnect Initiate exhaustion candidate `b0beba6fa23aa20d4d4e79f4f9cf28cb97050ea0` is green: Repository Policy `35648395182`, Build Bootstrap `35648447878`, Project State Gate `35648450806`, E1 `35648453673` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35648456285`. With every peer confirmation suppressed, the native link consumed its five-attempt DI control budget, exposed reason 39 through DSO_DISDATA with POLLERR|POLLHUP, and recovered on a fresh MIRROR connection after fault removal.


Reserved-port No Link coverage now spans every Phase IV packet class for which the pinned PyDECnet reference maps an unknown connection to its reserved port. In the existing bounded ACK-range fault window, the raw reference-identity injector sends syntactically valid Data, Connect Confirm, Disconnect Initiate, Interrupt, and Link Service packets using deliberately unused NSP source/destination link IDs. Each must receive Disconnect Confirm reason 41 addressed to the probe link pair; the established real connection must remain intact and still complete its timer/XNAK recovery. ACK-only and disconnect-confirm packets remain silent-discard classes and are covered separately by the spoofed-ACK path. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then keepalive/inactivity coverage.


Exact-SHA fast socket acceptance for reserved-port class candidate `95fb4106dc9519cdb1825540e46dc4a6a2e6a57a` is green: Repository Policy `35649532570`, Build Bootstrap `35649575197`, Project State Gate `35649577829`, E1 `35649580393` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35649583348`. Every valid unknown-link Data, CC, DI, Interrupt, and Link Service probe received Disconnect Confirm reason 41 while the established connection completed timer/XNAK recovery.

PP-06 malformed established-link coverage now injects three parser-invalid packets into that same bounded live loss window using the correct peer node and connection IDs: ACK_DATA with reserved ACK qualifier 4, Link Service with illegal flow modifier 3, and an empty Interrupt. None may acknowledge the outstanding tagged Data, change Data flow state, queue usable Other-Data, or otherwise damage the live connection; the original tagged Data must remain outstanding until its normal response timer and the subsequent valid cross-XNAK must still force prompt retransmission. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then keepalive/inactivity coverage.


Exact-SHA fast socket acceptance for malformed-packet candidate `8e62c06b7c5aed75edea12396ab2a6a9ce4999e5` is green on the exact rerun: Repository Policy `35653129828`, Build Bootstrap `35653180798`, Project State Gate `35653183863`, E1 `35653186498` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35653189580`. The earlier E1 run `35652189222` completed both candidate labs but failed only while finalizing the ARM64 evidence artifact with an external HTTP 403; the unchanged rerun passed. The live malformed ACK, Link Service, and empty-Interrupt probes were inert and the established link completed timer/XNAK recovery.

NSP inactivity keepalive is now independently testable without changing the production default. The module exposes writable `nsp_inactivity_seconds` with default 300 seconds; zero safely falls back to the default. The fast socket proof temporarily sets four seconds for one otherwise-idle MIRROR link, requires two candidate no-change Link Service packets on the same Other-Data subchannel with consecutive sequence numbers and no premature firing, then verifies normal MIRROR data on that same connection and restores 300 seconds. Consecutive sequence numbers rather than a retransmitted sequence prove PyDECnet acknowledged the first keepalive and the candidate restarted its inactivity interval. Next: exact-SHA fast socket acceptance, then continue remaining NSP/PP-06 negatives.


Exact-SHA fast socket acceptance for inactivity-keepalive candidate `b2ab90a1a6dd6141114dbc0d0d5d51134583f916` is green: Repository Policy `35654640026`, Build Bootstrap `35654680497`, Project State Gate `35654682738`, E1 `35654684891` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35654687240`. The live idle link emitted two acknowledged no-change Link Service keepalives on consecutive Other-Data sequence numbers at the shortened test interval, then completed normal MIRROR traffic on the same connection. Next: continue remaining NSP/PP-06 negatives.


PP-06 full Data-window backpressure now has a live independent-peer proof. On a fresh MIRROR link the host suppresses every PyDECnet-to-candidate unicast reply while userspace submits eight one-segment records, filling the bounded NSP Data window. An immediate ninth nonblocking record must return EAGAIN rather than overrun the retransmit window. After the reverse path is restored, the blocked ninth record must resume on the same logical link. This complements the existing XOFF zero-window proof and distinguishes protocol flow control from socket-buffer behavior. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue remaining NSP/PP-06 negatives.


The first full-window live proof failed because the test encoded an eight-segment window while the implementation's declared bounded Data window is 20 segments. The candidate correctly accepted the ninth segment. The test is corrected to fill all 20 outstanding Data slots, require the 21st nonblocking send to return EAGAIN, then require recovery after peer replies resume. Product behavior is unchanged.


- Exact-SHA fast socket acceptance for full-window backpressure candidate `2f4d54c26d3330badada1f4e9703b5384162403b` is green: Repository Policy `35661733498`, Build Bootstrap `35661771870`, Project State Gate `35661773813`, Python QEMU VM Lab `35661776097` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35661778430`. The corrected live proof filled all 20 outstanding Data slots, observed nonblocking backpressure on the 21st send while peer replies were suppressed, then resumed and completed after replies were restored.


PP-06 EINTR/signal handling now has live socket coverage. A native listener blocks in `accept()` with no pending connection and must return `EINTR` on a non-restarting SIGALRM. A connected MIRROR socket then blocks in `recv()` with no pending record and must likewise return `EINTR`; the same logical link must immediately complete a normal request/reply afterward, proving interruption does not corrupt socket or NSP state. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue remaining PP-06 negatives.


The first signal-interruption acceptance attempt failed before runtime in x64 interoperability run `35663764985`: strict C11 compilation hid POSIX `sigaction`/`sigemptyset` declarations because the test lacked a POSIX feature-test macro. `dnsignal.c` now requests POSIX.1-2008 before headers. This is test-only; product behavior is unchanged. The failed run remains recorded rather than being treated as a protocol failure.


Exact-SHA fast socket acceptance for signal-interruption candidate `4a7f2ce6616b7937ac006a7628ecb0d5f588add6` is green: Repository Policy `35663912204`, Build Bootstrap `35663951694`, Project State Gate `35663953798`, Python QEMU VM Lab `35663955669` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35663957177`. The live proof confirms blocking `accept()` and `recv()` return `EINTR` on a non-restarting signal and the interrupted connected socket immediately remains usable for normal MIRROR traffic.


PP-06 receiver-stall and many-client fairness now have a live independent-peer proof. One native MIRROR link sends 20 records and intentionally leaves every reply unread, creating a receiver-stalled session with queued application data. While that link remains stalled, a second MIRROR connection must connect and complete an independent request/reply. The first socket must then drain all 20 replies in order. This proves one blocked consumer does not starve an independent valid session within the declared connection/window limits. Product behavior is unchanged. Next: exact-SHA fast socket acceptance, then continue remaining PP-06 negatives.


Exact-SHA fast socket acceptance for receiver-stall fairness candidate `ed5a1ecaf1eb6668de751f71b18e33dbe8ffbc6d` is green: Repository Policy `35665443669`, Build Bootstrap `35665485064`, Project State Gate `35665487460`, Python QEMU VM Lab `35665489800` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35665492465`. The live proof kept 20 MIRROR replies unread on one connection while a second independent session connected and completed a request/reply, then drained all stalled replies in order.


PP-06 syscall-fuzz coverage now includes a deterministic invalid-call corpus in the live socket lifecycle test. It exercises short sockaddr lengths, wrong address family, conflicting object number/name selectors, unsupported sockaddr flags, invalid node-address lengths, remote addresses missing node identity, short option buffers, invalid accept mode, oversized access-field lengths, and unknown NSP socket options. Each call must fail with the implementation's declared errno while leaving the socket usable for the existing lifecycle/connect/churn proof. Product behavior is unchanged. The already-accepted reserved-port proof also directly demonstrates bounded NSP connection-table exhaustion by requiring Disconnect Confirm reason 1 (No Resources) after synthetic CIs fill the 256-entry table. Next: exact-SHA fast socket acceptance, then continue remaining PP-06 negatives.


Static review of the new syscall-negative corpus found one test-ordering mistake before acceptance completed: `sendmsg()` validates unsupported message flags before connection state, so `MSG_PEEK` must return `EOPNOTSUPP`, not `ENOTCONN`, on an unconnected DECnet socket. The expectation is corrected to match the socket implementation; product behavior is unchanged.


Exact-SHA fast socket acceptance for receiver-stall fairness candidate `ed5a1ecaf1eb6668de751f71b18e33dbe8ffbc6d` is green: Repository Policy `35665443669`, Build Bootstrap `35665485064`, Project State Gate `35665487460`, Python QEMU VM Lab `35665489800` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35665492465`. The live proof confirms an unread 20-record MIRROR reply queue on one link does not prevent a second independent MIRROR session from connecting and completing traffic, and the stalled link then drains cleanly in order.


Exact-SHA fast socket acceptance for syscall-negative candidate `9c3b402bc4de7f2023a03c0592bbd32aa2233df8` is green: Repository Policy `35666650101`, Build Bootstrap `35666687690`, Project State Gate `35666689839`, Python QEMU VM Lab `35666691798` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35666693616`. The deterministic invalid-call corpus returned the declared errno values without damaging subsequent socket lifecycle/churn behavior.


Phase 5 full promotion acceptance is green on exact candidate `2d54dbf252120b42a5c2a60fa202371382af5dc9`: Repository Policy `35668700953`, Build Bootstrap `35668731691`, Project State Gate `35668733943`, External Reference Baselines `35668736098`, E1 `35668737804`, E2 `35668739614`, E3 `35668741529`, E4 `35668743712`, and full Independent Ethernet Interoperability `35668745743`. E1-E4 passed on x86_64 and ARM64; the 14-job Route20/PyDECnet interoperability matrix passed. Phase 5 exit criteria are satisfied: reliable bidirectional logical links, reconnect/recovery, deterministic loss/retransmission, flow control, sequence wrap, socket lifecycle/concurrency, bounded resource pressure, malformed-input negatives, signal interruption, receiver-stall fairness and full socket ABI interoperability are accepted. Phase 6 is next: Session Control and NICE/NML network management.


Phase 5 closure commit `fd2e603853ae913bd458e6c27e9b81650fa34ffb` itself passed full exact-SHA acceptance: Repository Policy `35674017637`, Build Bootstrap `35674044043`, Project State Gate `35674045083`, External Reference Baselines `35674046333`, E1 `35674047724`, E2 `35674048935`, E3 `35674050217`, E4 `35674051615`, and Independent Ethernet Interoperability `35674053420`; all x86_64/ARM64 jobs and the full 14-job Route20/PyDECnet matrix were green.

Phase 6 starts with a read-only management-state surface suitable for NICE/NML: the management UAPI enumerates current best routing entries (level, destination, next hop, interface, cost, hops), and `dnctl routes` provides a local scripted consumer. Remote NICE/NML encoding and object-19 dispatch follow. Phase 7 scope also records a future minimal DECnet-native web server based on a pinned, license-compatible tiny HTTP implementation.


The first Phase 6 route-management acceptance attempt on `7135e72aa994d1af44064b70dfff45ecd61e9d05` failed only at kernel compilation in Build Bootstrap `35675753048` on both architectures. The new local variable name `current` collided with the Linux kernel `current` macro (`get_current()`), producing strict-prototype and lvalue errors in `dniv_route_get_index()`. The enumerator variable is renamed to `cursor`; management semantics are unchanged. The failed build is retained as evidence.


Exact-SHA fast acceptance for corrected Phase 6 route-management candidate `efa76ee675dd6a9e9c8c514f0710287f93cd3124` is green: Repository Policy `35675873484`, Build Bootstrap `35675894607`, Project State Gate `35675896016`, Python QEMU VM Lab `35675897212` on x86_64 and ARM64, and Independent Ethernet Interoperability `35675898672`. The read-only best-route enumeration and `dnctl routes` management consumer are accepted.

Phase 6 management state now extends to live NSP logical links. A new read-only `DNIV_IOC_GET_LINK` enumerates bounded connection records and reports local/remote logical-link IDs, remote node, protocol state, negotiated segment size, Data/Other-Data outstanding counts, receive queue depth, interrupt credit, XON/shutdown state and terminal reason. Snapshotting is performed under the existing NSP lock, and `dnctl links` is the first scripted local consumer. NSP wire behavior is unchanged. Next: exact-SHA fast acceptance, then build the object-19 Session Control/NICE boundary on these management snapshots.


Exact-SHA fast acceptance for NSP-link management candidate `84a098031d0c8ba90278be94267d29a0cd33ba3e` is green: Repository Policy `35676893762`, Build Bootstrap `35676965236`, Project State Gate `35676966599`, Python QEMU VM Lab `35676967767` on x86_64 and ARM64, and Independent Ethernet Interoperability `35676968922` against PyDECnet L1 and Route20 L1. The read-only logical-link management UAPI and `dnctl links` consumer are accepted.

Phase 6 now adds the first repository-owned Phase IV NICE codec boundary. It parses the standard five-byte READ NODE executor request used by the pinned PyDECnet reference, validates the function/entity/selector form, and emits a standards-shaped successful NodeReply containing the executor address/name plus Identification parameter 100. Focused host tests lock the exact request bytes `14 20 00 00 00`, executor entity encoding and reply parameter framing. This is pure management-message codec work; Session object 19 dispatch follows after exact-SHA acceptance.


The first NICE-codec acceptance attempt on `40e7bf5726cc66a7190cd62bceb8519358c3a3da` failed only in Build Bootstrap `35677719942` on both architectures. The codec produced the correct 30-byte reply (11-byte success/entity prefix plus the 19-byte Identification parameter), while the new host test incorrectly asserted 26 bytes. The expected length is corrected to 30; protocol/codecs are unchanged and the failed build remains recorded.


Phase 6 now exposes NICE through the standard Session Control object 19 boundary. New userspace daemon `dnnml` binds native `AF_DECnet` object 19, negotiates NICE version 4.0.0 in Session connect data, reads the live executor identity from the kernel management UAPI, and answers the accepted READ NODE executor request with the repository-owned NICE codec. Independent PyDECnet now opens object 19, verifies the version accept data, sends the exact five-byte executor request, and validates the returned address/name plus Identification parameter. This is the first end-to-end remote NICE/NML path; additional node/circuit/link information and counters follow. Next: exact-SHA fast acceptance, then extend NICE READ NODE information from current management snapshots.


Object-19 acceptance attempt `35678774415` failed only in the x64 PyDECnet L1 interoperability job before any NICE request was sent. The candidate reached adjacency and NSP readiness, started `dnnml`, and published `DNIV-INTEROP-NML-READY`; the controller was still waiting for a later NSP loss milestone because the host NICE query had been inserted after the existing loss/timer orchestration. The host NICE exchange is now scheduled immediately after candidate launch/readiness, before later socket-fault stages. Product, Session Control and NICE codec behavior are unchanged.


Exact-SHA fast acceptance for object-19 orchestration correction `fd3eeffb0a80312b6a2f525ba8a33b689b6e481e` is green: Repository Policy `35679662549`, Build Bootstrap `35679691988`, Project State Gate `35679693606`, Python QEMU VM Lab `35679695309` on x86_64 and ARM64, and Independent Ethernet Interoperability `35679696797`. The pinned PyDECnet object-19 query completed before later fault orchestration and validated NICE version negotiation, executor identity and Identification parameter end-to-end.

Phase 6 NICE executor reads now distinguish information classes instead of returning characteristics for every request. READ NODE summary/status returns executor state plus the live active-link count derived from the management link snapshot; characteristics retains Identification; unsupported classes return a NICE error rather than misleading data. Unit coverage locks the summary/status selector bytes and parameter encoding. The unit Makefile duplicate Phase 6 target and clean omission are corrected. Next: exact-SHA fast acceptance, then add executor counter and circuit/adjacency management information.


Exact-SHA fast acceptance for NICE executor status candidate `d9419187e80f59f2b8fc13d3df3f5480920e8f14` is green: Repository Policy `35680946527`, Build Bootstrap `35680972608`, Project State Gate `35680973956`, Python QEMU VM Lab `35680975464` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35680976771`. Summary/status codec changes and the live object-19 characteristics path remain green.

Phase 6 now adds a native outbound NICE management client, `dnnice`. It connects through `AF_DECnet` to standard NML object 19, negotiates NICE 4.0.0 connect/accept data, sends READ NODE executor summary, parses the successful node entity plus state/active-link prefix, and prints the remote executor identity. Fast PyDECnet interoperability invokes it before the local `dnnml` listener proof, giving bidirectional management evidence: candidate queries independent NML and independent PyDECnet queries candidate NML in the same exact-SHA run. Next: exact-SHA fast acceptance, then expand NICE adjacency/circuit information and counters.


Exact-SHA fast acceptance for native NICE client candidate `b439d8fb4f9794f6d9d691f50ff1571c66b65d68` is green: Repository Policy `35681976488`, Build Bootstrap `35682008528`, Project State Gate `35682010347`, Python QEMU VM Lab `35682012132` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35682013882`. The candidate successfully queried the pinned PyDECnet NML object 19 using NICE 4.0.0 and READ NODE executor summary, while the reciprocal PyDECnet-to-candidate object-19 query remained green.

The candidate NML server now keeps an accepted management session open for multiple NICE requests instead of processing exactly one request and closing the child socket. The independent PyDECnet proof uses one negotiated object-19 session to read executor characteristics and then executor status, requiring State=On and a nonzero live Active Links parameter from the kernel management snapshot. This exercises repeated Session/NICE operations and validates the new status information on the live wire. Next: exact-SHA fast acceptance, then add circuit/adjacency information and management counters.


Exact-SHA fast acceptance for repeated NICE management-session candidate `040424959fd4067f3b4d5035d10c0a984dac4fad` is green: Repository Policy `35682678214`, Build Bootstrap `35682702215`, Project State Gate `35682703575`, Python QEMU VM Lab `35682704849` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35682706361`. One negotiated object-19 session successfully served executor characteristics followed by status with State=On and live Active Links.


Phase 6 NICE codec coverage now includes standard READ CIRCUIT status framing. The repository-owned codec parses the Phase IV entity-class 3 request form used by the pinned PyDECnet reference and emits a CircuitReply with the named circuit, State=On and parameter 810 Block Size. This establishes the deterministic wire boundary needed to map live adjacency/circuit snapshots into object-19 replies next; no kernel or Session behavior changes in this increment. Next: exact-SHA fast acceptance, then wire live circuit/adjacency status through `dnnml` and reciprocal PyDECnet validation.


The first NICE circuit-codec acceptance attempt failed only the ARM64 E1 VM job `106605185228` in run `35683454049`. Both guests completed the required expiry/restart/recovery sequence; DN70 emitted PASS and DN71 reached RECOVERED at guest uptime 360.98 seconds, but the controller's 360-second ARM64 E1 bound expired roughly one second before DN71 could emit its final PASS marker. x86_64 E1, policy/build/state, and x64 PyDECnet interoperability were green. The ARM64 E1 controller bound is raised narrowly from 360 to 375 seconds, preserving the bounded fast profile while covering the observed TCG tail. Product and NICE protocol behavior are unchanged; the failed run remains recorded. Next: rerun exact-SHA fast socket acceptance.


Exact-SHA fast acceptance for NICE circuit-codec candidate `cf19a74a95ba75849f26141c4d63e28df6e64cb5` is green after the narrowly bounded ARM64 E1 correction: Repository Policy `35684075406`, Build Bootstrap `35684098766`, Project State Gate `35684100061`, Python QEMU VM Lab `35684101313` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35684102599`. The standard READ CIRCUIT request parser and CircuitReply State/Block Size encoding are accepted; the 375-second ARM64 E1 bound also completed green.


Phase 6 now wires READ CIRCUIT status to live kernel adjacency state. The object-19 server maps logical circuit names ETH-N to distinct active kernel interfaces from DNIV_IOC_GET_ADJACENCY, requires an UP adjacency, and reports the live negotiated block size rather than a fixture value. The reciprocal pinned-PyDECnet management proof now reads ETH-0 on the same object-19 session after executor characteristics/status and requires State=On plus a sane live Block Size. Next: exact-SHA fast acceptance, then add adjacency/node information and management counters.


Exact-SHA fast acceptance for live NICE circuit-status candidate `da0b4864741ce5220a2b7d7743631a479a871957` is green: Repository Policy `35685198705`, Build Bootstrap `35685229625`, Project State Gate `35685231343`, Python QEMU VM Lab `35685233059` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35685234915`. The reciprocal object-19 session validated executor characteristics/status plus live `ETH-0` State=On and kernel-adjacency Block Size.


Phase 6 executor management now serves standard READ NODE counters on object 19. The response exposes NICE node counter 608 (Total bytes received) and 610 (Total messages received) from the kernel's live DECnet receive accounting, with standard CTR4 encoding and saturation. The pinned PyDECnet reciprocal session now validates characteristics, status, counters and live circuit status on one NML connection. Next: exact-SHA fast acceptance, then add transmit/per-circuit accounting and richer circuit/adjacency NICE information.


The first executor-counter acceptance attempt failed only x64 PyDECnet interoperability run `35686193367` before the counter request executed: the test script contained a literal `\\n` between the STATUS_REQUEST and COUNTERS_REQUEST assignments, causing Python SyntaxError. Policy/build/state were green and the product codec/server compiled. The harness source now contains a real newline; product/NICE semantics are unchanged. The failed run remains recorded.


The corrective executor-counter acceptance still failed only x64 PyDECnet interoperability run `35686568655` before management traffic because a second literal `\\n` remained in the test script's final formatted print statement. The request declarations were fixed and policy/build/state were green, but Python again stopped at parse time. All literal newline escapes in executable Python source are now removed; product/NICE semantics remain unchanged. Both failed harness runs remain recorded.


Exact-SHA fast acceptance for executor-counter corrective candidate `4512d0ac92c2fda7ee7ba1e4e69babae9c7f30a8` is green: Repository Policy `35686827735`, Build Bootstrap `35686857571`, Project State Gate `35686858893`, Python QEMU VM Lab `35686860051` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35686861521`. One object-19 session successfully validated executor characteristics, status, live node receive counters and live circuit status after the two recorded test-script syntax failures were corrected.


Phase 6 management accounting now covers both directions and circuit scope. A new additive read-only traffic snapshot reports aggregate or per-interface DECnet receive/transmit frames and bytes without changing the existing UAPI structures. Ethernet receive and all successful hello/routing/data transmit paths feed the counters. NICE READ NODE counters now serve standard 608-611 Total bytes/messages received/sent, while READ CIRCUIT counters serve standard 1000/1001 bytes and 1010/1011 data blocks for the live ETH-N interface. The reciprocal pinned-PyDECnet object-19 session validates all eight live counters. Next: exact-SHA fast acceptance, then enrich circuit status with adjacent-node information and continue NICE node/adjacency management coverage.


Exact-SHA fast acceptance for bidirectional/per-circuit NICE traffic-counter candidate `35f15e4d5825109dddb9447a48bde025ef00c0de` is green: Repository Policy `35688413870`, Build Bootstrap `35688437430`, Project State Gate `35688439214`, Python QEMU VM Lab `35688440768` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35688442294`. The reciprocal object-19 session validated node counters 608-611 and live ETH-0 circuit counters 1000/1001/1010/1011 with nonzero receive/transmit values. Next: enrich live circuit status with adjacent-node information and continue NICE node/adjacency management coverage.


Phase 6 live READ CIRCUIT status now includes standard NICE parameter 800 Adjacent node from the same UP kernel adjacency that supplies the circuit block size. The codec emits the pinned-reference CM-1 node-address form, and the reciprocal PyDECnet object-19 proof requires a nonzero live adjacent address before accepting the block-size parameter. Next: exact-SHA fast acceptance, then extend READ NODE beyond the executor using live adjacency/route snapshots.


Exact-SHA fast acceptance for live NICE circuit-adjacent status candidate `4952895102e3e121b233e93e31e0f905860ad94f` is green: Repository Policy `35689217606`, Build Bootstrap `35689242761`, Project State Gate `35689244385`, Python QEMU VM Lab `35689245952` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35689247610`. The reciprocal object-19 session validated live ETH-0 State=On, standard parameter 800 Adjacent node, Block Size, and the accepted circuit counters. Next: extend READ NODE beyond the executor using live adjacency/route snapshots.


Phase 6 READ NODE status now handles a specific live remote node instead of only executor zero. The object-19 server resolves the requested address through the kernel UP-adjacency snapshot and matching route-table entry, then emits standard NICE Reachable state, Routing-IV/Non-Routing-IV type, route Cost/Hops, Circuit and Next-node parameters. The reciprocal pinned-PyDECnet session reuses the live ETH-0 adjacent address, requests that exact node, and validates the route-backed status before continuing circuit counters. Next: exact-SHA fast acceptance, then add multiple-entity known/active/adjacent node reads and management error semantics.


The first live remote READ NODE acceptance attempt failed only x64 PyDECnet interoperability run `35690144066`. The server returned NICE error -1 because the test exposed a representation mismatch in the userspace route lookup: L1 route UAPI entries carry the 10-bit node number in `destination`, with area supplied by `next_hop`, while the new NML code compared `destination` to the full 16-bit area.node address. The lookup now matches L1 node plus area correctly (and retains L2 area matching). Build/policy/state were green; product routing itself was not implicated. The failed run remains recorded.


Exact-SHA fast acceptance for live remote READ NODE corrective candidate `cf5b3fe104c988a393e1fc33956d613bb5df24ff` is green: Repository Policy `35690455115`, Build Bootstrap `35690484236`, Project State Gate `35690486057`, Python QEMU VM Lab `35690487637` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35690489477`. The reciprocal object-19 session successfully read the live adjacent remote node and validated Reachable state, node type, route cost/hops, circuit and next-node fields. Next: add multiple-entity known/active/adjacent node reads and management error semantics.


Phase 6 NICE node management now supports multiple-entity READ NODE status for Known (-1), Active (-2) and Adjacent (-4). The parser accepts standard plural node selectors, the NML server derives live remote entities from route or adjacency snapshots, sorts/deduplicates them, emits the standard multiple-items framing (retcode 2, per-entity success records, retcode -128 terminator), and preserves route-backed Reachable/type/cost/hops/circuit/next-node detail. Valid specific unknown nodes now return NICE -8 Unrecognized component; unsupported permanent reads return -1. The reciprocal PyDECnet session validates all three plural reads include the live adjacent peer plus the two error semantics. Next: exact-SHA fast acceptance, then broaden Session Control object dispatch and remaining NICE/NML management operations.


Exact-SHA fast acceptance for plural NICE node-status candidate `1189d35b2886036b006f456fc0d8812878cfbe34` is green: Repository Policy `35691397184`, Build Bootstrap `35691422005`, Project State Gate `35691423342`, Python QEMU VM Lab `35691424646` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35691426004`. Known, Active and Adjacent multi-entity READ NODE status all included the live adjacent peer, while specific-unknown and unsupported-permanent reads returned the required NICE errors.


Phase 6 NICE circuit management now supports multiple-entity READ CIRCUIT status for Known (-1) and Active (-2). The circuit parser accepts the standard three-byte plural selector, the NML server enumerates live ETH-N circuit snapshots, emits NICE multiple-items framing and per-circuit State/Adjacent-node/Block-size replies, and the reciprocal pinned-PyDECnet session requires ETH-0 to appear in both plural reads. Unsupported plural circuit classes or information classes remain explicit NICE errors. Next: exact-SHA fast acceptance, then continue Session Control/NICE management coverage.


Exact-SHA fast acceptance for plural NICE circuit-status candidate `07ff9eb6fe5a76ffd8b2868e38a552e8c2329029` is green: Repository Policy `35692546939`, Build Bootstrap `35692568618`, Project State Gate `35692570088`, Python QEMU VM Lab `35692570940` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35692572150`. Known and Active multi-entity READ CIRCUIT status both included live ETH-0 using standard multiple-items framing while existing management coverage remained green.


Phase 6 plural circuit coverage now includes READ CIRCUIT counters for Known (-1) and Active (-2), exercising the already-implemented multiple-item counter path against pinned PyDECnet. Each returned live circuit must carry the standard 1000/1001/1010/1011 receive/transmit byte/block counters and ETH-0 must appear with nonzero traffic. Product behavior is unchanged. Next: exact-SHA fast acceptance, then continue Session Control/NICE management operations and negative handling.


Exact-SHA fast acceptance for plural NICE circuit-counter candidate `e954ee76c70c734a61c475a7a9f1b56312749d23` is green: Repository Policy `35693426128`, Build Bootstrap `35693454698`, Project State Gate `35693456129`, Python QEMU VM Lab `35693457418` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35693458896`. Known and Active multi-entity circuit counter reads included live ETH-0 with nonzero standard receive/transmit byte and block counters.


Phase 6 PP-07 negative coverage now injects malformed NICE requests over an established object-19 session: wrong function, truncated node request, unsupported entity class and inconsistent circuit-name length. Each must return NICE -1 without closing or corrupting the management session, after which a valid executor status read must still succeed with State=On. Product behavior is unchanged; this locks malformed-management rejection and session recovery against the independent peer path. Next: exact-SHA fast acceptance, then daemon/session restart and concurrent-management coverage.


Exact-SHA fast acceptance for malformed NICE session-recovery candidate `7d34c5a016b8e79909625d8ee8c165b562e5f962` is green: Repository Policy `35694162276`, Build Bootstrap `35694195705`, Project State Gate `35694197206`, Python QEMU VM Lab `35694198772` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35694200328`. Malformed NICE requests returned -1 without terminating the object-19 session, and a subsequent valid executor status read succeeded.


Phase 6 daemon/session robustness now adds concurrent and restart coverage for object 19. The NML daemon can fork independent management-session workers; normal daemon mode reaps children automatically and bounded `--sessions N` mode waits for every child and propagates failure. The live pinned-PyDECnet proof holds three simultaneous NICE sessions open, requires valid executor status from all three, then restarts the daemon and reruns the full NICE management suite on a fresh session. This covers PP-07 many-client isolation plus daemon/session restart without weakening the existing malformed-request recovery proof. Next: exact-SHA fast acceptance, then continue Session Control/NICE management operations.


Full interoperability run `35713822257` against `d95dc1131ed0c8985bfc25399155b871ea2f59a9` exposed management-coverage gaps outside the earlier x64 L1 fast slice: direct UP adjacencies without a matching route entry could return NICE -8, Known/Active plural node reads could omit a live cross-area adjacency, and one ARM64 restart attempt hit a transient object-19 registration race. The NML snapshot now treats every UP adjacency as a live node, uses route metrics when available with a direct-adjacency cost/hop fallback when they are not, and gives bounded NML lifecycle probes an additional guest second before publishing readiness. Next: exact-SHA fast acceptance, then re-run the affected PyDECnet interoperability roles before continuing Phase 6.


Exact-SHA fast acceptance for the live-node coverage corrective candidate `28b7503965fe8a72723911a9f886db20ed6dc4e6` is green: Repository Policy `35721733232`, Build Bootstrap `35721773410`, Project State Gate `35721775148`, Python QEMU VM Lab `35721777173` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35721779005`. Direct-adjacency fallback, plural live-node coverage and the bounded NML restart allowance all passed the fast gate.

Phase 6 local/remote management usability now extends `dnnice` from a fixed executor summary query to explicit executor summary, status, characteristics and counters queries over object 19. The command validates NML version negotiation and decodes standard identification plus node traffic counters for interactive use. Next: exact-SHA fast acceptance, then add specific remote-node and circuit query modes to the same client.


Exact-SHA fast acceptance for the executor-query client candidate `3943fcd0a20cd552ac2953a25b10ff2028c593c3` is green: Repository Policy `35722657962`, Build Bootstrap `35722715333`, Project State Gate `35722717424`, Python QEMU VM Lab `35722719983` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35722722871`. The expanded `dnnice` builds on both architectures while the existing independent summary query remains interoperable.

The independent PyDECnet path now exercises every implemented interactive executor query from `dnnice`: summary, status, characteristics and counters. The restart-side bounded NML listener allowance is also made consistent with the already-proven two-second initial/concurrent listener allowance; the previous one-second restart sleep was a residual harness inconsistency. Product protocol semantics are unchanged. Next: exact-SHA fast acceptance, then add specific remote-node and circuit query modes to `dnnice`.


Fast interoperability run `35723583203` for `3ab65e446ed08b49df587eefd28c2bb93db1c73d` proved adjacency, NSP, summary and status, then exposed a client-only NICE decoding defect: PyDECnet characteristics replies contain multiple standard parameters and `dnnice` incorrectly required Identification (100) to be the first and only decoded field. PyDECnet's pinned source confirms response parameters are sorted by parameter number, so State (0) precedes Identification and Management Version (101) plus Software Identification (126) may follow. `dnnice` now walks the full typed NICE parameter stream, extracts Identification wherever it occurs, and validates the remainder rather than assuming a one-field reply. Product routing/NSP/NML semantics are unchanged. Next: exact-SHA fast acceptance and continue Phase 6 remote-node/circuit client coverage.


The first characteristics-decoder correction still failed exact-SHA PyDECnet interoperability in run `35725003342`. The remaining defect was CM-n handling: NICE coded-multiple type 0xC1..0xDF encodes a count of nested typed values, not a byte-length prefix. PyDECnet's pinned `CM1.decode` confirms this. The generic `dnnice` value walker now recursively consumes exactly that many nested NICE values, allowing Management Version (CM-3) and other compound parameters to coexist with Identification. This remains a client decoder correction only. Next: exact-SHA fast acceptance.


Fast interoperability run `35726017085` for `df4f954b1526be186ee9b420b50a4fa063fd525f` still reached the PyDECnet characteristics query but rejected its typed parameter stream. The remaining client-only decoder gap was the standard H-n/O-n numeric NICE families (type codes 0x21..0x2f and 0x31..0x3f); the generic value walker had handled DU/DS, coded, ASCII/hex-image and CM-n values but not those fixed-width numeric encodings. The walker now consumes H-n/O-n using the standard low-nibble width while retaining CM-n recursion. Product routing/NSP/NML semantics remain unchanged. Next: exact-SHA fast acceptance, then add specific remote-node and circuit query modes to `dnnice`.


Fast acceptance for `fa20b88c3f8ee268f4049825d8b06d84dbf6dee2` was green in Repository Policy `35727180595`, Build Bootstrap `35727229528`, Project State Gate `35727232423`, and Python QEMU VM Lab `35727234890` on x86_64 and ARM64, but x64 PyDECnet interoperability `35727237104` still failed only the remote characteristics decode after summary/status succeeded. H-n/O-n support was therefore not the remaining cause. The client now emits the bounded malformed characteristics reply bytes on this error path so the next exact-peer run can identify the exact unsupported encoding rather than guessing. Next: run the x64 PyDECnet fast slice, fix the concrete decoded gap, and immediately re-run exact-SHA fast acceptance.


Diagnostic fast interoperability run `35728633258` on `9897cbf93660018f22d4507af337fc9be7c9aaac` exposed the exact remaining characteristics mismatch. PyDECnet's successful NICE reply used detail `0x0000` and an empty counted message before the node entity, while `dnnice`'s characteristics/counter entity helper incorrectly required candidate-server-specific detail `0xffff` and hard-coded the entity at byte 4. The typed parameter stream itself decoded correctly. `dnnice` now parses the standard NICE success header generically—retcode, two-byte detail, counted message—then locates the node entity, accepting both independent-peer and candidate-server forms. Next: exact-SHA fast acceptance; once green, add specific remote-node and circuit query modes.


Fast interoperability run `35729804786` for `759387e564dad30d6009753974dbf13030263574` passed PyDECnet summary, status and characteristics, then exposed the next client-only NICE compatibility gap in executor counters. The preserved LAN capture shows PyDECnet returns the standard full counter set in ascending parameter order and uses the NICE counter type nibble to encode 1-, 2- or 4-byte counters (and mapped variants); parameters 608-611 therefore are not the first fields. `dnnice` now walks the full counter stream, skips unrelated counters using their encoded widths, handles mapped-counter bitmap prefixes, and extracts 608-611 wherever present. Product routing/NSP/NML semantics remain unchanged. Next: exact-SHA fast acceptance, then add specific remote-node and circuit query modes to `dnnice`.


Exact-SHA fast acceptance for the full NICE counter-stream decoder candidate `0bd1ccfa0beb6f70f1ddd9117f6e6a6a14714d10` is green: Repository Policy `35734916006`, Build Bootstrap `35734963015`, Project State Gate `35734965815`, Python QEMU VM Lab `35734968787` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35734971678`. All implemented executor queries now interoperate with the pinned independent peer. Phase 6 management usability now adds explicit remote-node and circuit query modes to `dnnice`: a management target can request a specific node summary/status or a named circuit status/counters, with generic typed NICE decoding for route state and circuit state plus standard counter streams. The independent PyDECnet path now exercises the candidate asking the peer about the candidate node and ETH-0 status/counters. Next: exact-SHA fast acceptance, then extend interactive multiple-entity node/circuit reads or additional Session Control object dispatch.


Exact-SHA fast acceptance for remote-node/circuit client candidate `bbe875eb1ab09e996d3793090e0cb90e97da7942` is green: Repository Policy `35736469661`, Build Bootstrap `35736527756`, Project State Gate `35736530459`, Python QEMU VM Lab `35736532797` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35736536010`. The independent peer successfully answered a specific candidate-node status query plus ETH-0 status and counters. Interactive `dnnice` now also supports standard NICE multiple-item reads for known/active/adjacent nodes and known/active circuits, including circuit counters; it consumes the retcode-2 item stream through the -128 terminator and decodes each entity with the same typed parsers used for specific reads. The independent path requires the candidate to appear in PyDECnet's adjacent-node result and ETH-0 to appear in active circuit status/counters. Next: exact-SHA fast acceptance, then broaden Session Control object dispatch beyond NML/object 19.


Fast interoperability run `35737901590` for `6bc001e2e69784ce1c37d26baa7c7aeb59ed2b5f` passed every specific executor/node/circuit query and then exposed a standards-compatible independent-peer behavior in plural reads: when a plural selector resolves to exactly one entity, pinned PyDECnet collapses the response to one ordinary retcode-1 item instead of sending retcode 2 plus an -128 terminator; it uses multiple-item framing only when two or more replies exist. `dnnice` now accepts both legal response shapes while the candidate NML server's existing multi-item framing remains unchanged. Next: exact-SHA fast acceptance, then broaden Session Control dispatch with standard MIRROR object 25.


Fast interoperability run `35739261965` for `1684c621764cb71a9641be519be510dd57bdb80d` passed all specific executor/node/circuit reads and then failed only the plural adjacent-node client query. Preserved LAN packet evidence shows pinned PyDECnet returned a valid single-item adjacent-node reply containing node type, cost, hops and circuit but no State parameter; adjacency membership itself supplies the active/reachable context. `dnnice` now accepts a remote-node item when it contains any recognized standard status field, and prints State only when present, while still rejecting an empty/malformed item. Next: exact-SHA fast acceptance, then broaden Session Control dispatch with standard MIRROR object 25.


Exact-SHA fast acceptance for sparse adjacent-node status candidate `279cabd631f34674fa7f1d3b862dc4bafed33bd1` is green: Repository Policy `35740654536`, Build Bootstrap `35740711394`, Project State Gate `35740715296`, Python QEMU VM Lab `35740719041` on x86_64 and ARM64, and x64 PyDECnet L1 interoperability `35740722243`. Phase 6 Session Control dispatch now extends beyond NML/object 19 with a native standard MIRROR object 25 daemon. `dnmirror` advertises the standard two-byte maximum-length accept data, serves loop command records with success status/payload reflection, rejects non-loop function codes with failure status, and supports bounded one-session operation for acceptance plus normal sequential service. The pinned PyDECnet peer now connects inbound to candidate object 25 and validates small, 1 KiB, and negative MIRROR records. Next: exact-SHA fast acceptance, then add object-name dispatch/registration and continue Phase 6 Session Control coverage.


Exact-SHA fast acceptance for native MIRROR object 25 candidate `a494979e410cbedbf4adc4f13fa5a4cb5d4d1c2d` is green: Repository Policy `35742145930`, Build Bootstrap `35742206543`, Project State Gate `35742209944`, Python QEMU VM Lab `35742213010` on x86_64 and ARM64, and x64 PyDECnet interoperability `35742216169`. Phase 6 Session Control now also proves object-name registration and dispatch: `dnmirror --name` binds the standard `MIRROR` object name through the native AF_DECnet socket ABI, and the pinned PyDECnet peer connects by name and runs the same positive, 1 KiB and negative MIRROR record checks. Numeric object 25 behavior is retained unchanged. Next: exact-SHA fast acceptance, then continue Session Control coverage toward access control/task dispatch and Phase 6 closure.


Acceptance attempt `35743974462` for `904da60b5e83bef6322bfad98e5d673a3059f8cd` stopped in Repository Policy before protocol execution because the Git-data commit accidentally reset executable modes on three interoperability scripts. This packaging-only defect is corrected by restoring `100755` on `dniv-interop-smoke.sh`, `pydecnet-mirror.py`, and `run-interop.sh`; Session Control and MIRROR protocol content is unchanged. Next: exact-SHA fast acceptance, then continue Phase 6.


Exact-SHA fast acceptance for object-name Session Control candidate `7b70af547096e2d581621064c31724f79cf7e6ce` is green: Repository Policy `35744157479`, Build Bootstrap `35744209681`, Project State Gate `35744212187`, Python QEMU VM Lab `35744214743` on x86_64 and ARM64, and fast independent interoperability `35744217004`. Phase 6 Session Control access-data coverage now exercises deferred object acceptance through the native socket ABI. `dnmirror --require-access` reads inbound DSO_CONACCESS requester/password/account fields before accepting; matching credentials are accepted with normal MIRROR accept data, while a bad password is rejected with standard Session Control reason 34. The pinned PyDECnet peer proves both paths against the named MIRROR object. Next: exact-SHA fast acceptance, then add generic task/object listener coverage and drive Phase 6 toward consolidated/full closure.


Exact-SHA fast acceptance for Session Control access-data candidate `a5582b75dc624c62e97137520d1e7b1fba0a11a2` is green: Repository Policy `35745502566`, Build Bootstrap `35745567580`, Project State Gate `35745570952`, Python QEMU VM Lab `35745574148` on x86_64 and ARM64, and fast independent interoperability `35745577593`. Valid and invalid access-data paths both passed against pinned PyDECnet. Phase 6 now adds a generic native object listener, `dnobject`, which registers either an arbitrary object name or object number through AF_DECnet and serves record-oriented sessions. Independent coverage registers the nonstandard named task `DNIVTASK`; PyDECnet connects by that name with connect data and exchanges multiple records, proving dispatch is not limited to hard-coded standard objects. Next: exact-SHA fast acceptance, then run consolidated/full Phase 6 interoperability and close remaining management/session gaps found there.


Exact-SHA fast acceptance for generic named object candidate `687c0611c61cbcc9263ef4b0fec955c11928fbd2` is green: Repository Policy `35746895127`, Build Bootstrap `35746964625`, Project State Gate `35746967922`, Python QEMU VM Lab `35746971078` on x86_64 and ARM64, and fast independent interoperability `35746974168`. Consolidated interoperability `35747985975` then exposed one architecture/timing-sensitive deferred-accept race in ARM64 PyDECnet endnode coverage: the peer received the Session Control accept successfully and `pydecnet-access` reported pass, but disconnected immediately enough that DSO_CONACCEPT sampled CLOSED instead of RUN and incorrectly returned ECONNREFUSED to the accepting daemon. The deferred-accept socket path now mirrors the already-proven immediate-accept behavior: after a successful CC send, CLOSED observed while waiting for RUN is retained as an accepted socket so userspace sees orderly EOF rather than a false accept failure. Next: exact-SHA fast acceptance, then rerun consolidated Phase 6 acceptance.


Full Phase 6 promotion run `35752114459` on `7bf27c5aa90998c74914289a3a1d34d3576ff100` exposed a concrete NICE client compatibility gap in the router-to-PyDECnet-endnode role. The preserved x64 LAN capture shows the endnode returned a valid specific-node status record for 31.70 containing only standard Active links (parameter 600, DU2) and Delay (601, DU2). This is legal: PyDECnet's NSP layer contributed those fields while its endnode routing layer had no additional route-state fields for that instant. `dnnice` now recognizes and prints Active links and Delay as sufficient standard remote-node status content instead of rejecting the record as malformed. The same full run also produced one ARM64 router-endnode reverse-NML connect reject reason 39 after NML readiness; that remains separately observable and will be re-evaluated after this concrete decoder correction rather than conflated with the x64 failure. Next: exact-SHA acceptance, then repeat the full Phase 6 promotion matrix and address only any reproducible remaining failure.


Full Phase 6 promotion run `35754816703` on `6bccad0cb5f9b0cf42dbb5a46e3d1bbae10d5afe` confirmed the sparse endnode specific-node status correction: both router-to-endnode roles passed that query. The x86_64 PyDECnet router-endnode role then exposed a separate client-only plural-read framing gap. Pinned PyDECnet returned the standard retcode-2 multiple-items header as `02 00 00 00` (retcode, two-byte detail, empty counted message), while `dnnice` only accepted the candidate server's compact one-byte `02` form. `dnnice` now accepts either bounded form before decoding the item stream; candidate NML server and kernel protocol behavior are unchanged. The same run reproduced the ARM64 reverse-NML reason-39 failure. Packet evidence shows the candidate had object 19 listening, received the PyDECnet CI, and replied with Connect Ack plus Connect Confirm; PyDECnet then immediately returned NSP reason 41 (No Link), despite its API reporting Session reason 39 (Destination unreachable). Treat that as a separate reference-side state/routing investigation and do not alter candidate protocol behavior without concrete contrary evidence. Next: exact-SHA fast acceptance of the plural-header correction, then isolate the ARM64 PyDECnet router-endnode state transition before repeating full Phase 6 promotion.


Follow-up review of the same preserved x86_64 PyDECnet plural-read exchange found that the standard end-of-multiple-items control record is likewise sent in full NICE reply-header form as `80 00 00 00`, not only the candidate server's compact one-byte `80`. The plural decoder now applies the same bounded full-or-compact framing rule to both the retcode-2 opener and retcode--128 terminator. This is client-only compatibility handling; candidate NML/kernel behavior is unchanged. Next: exact-SHA fast acceptance, then isolate the reproduced ARM64 PyDECnet router-endnode reason-39/No-Link transition.


The reproduced ARM64 router-to-PyDECnet-endnode reverse-NML reason-39 failure is now concretely isolated to the independent reference. In pinned PyDECnet endnode routing, a send through an existing previous-hop cache entry physically transmitted the CI but returned `None`; endnode `routing.send(..., rqr=True)` interpreted that false result as send failure and immediately returned the same CI to local NSP. PyDECnet therefore closed its outbound NML link as destination-unreachable (Session reason 39) even though the candidate received the CI and correctly returned CA/CC; the later valid CC then found no PyDECnet connection and elicited internal NSP No Link reason 41. Fork commit `8d93c2a546317c67aba0adf9433f5f3efdf1f85c` corrects the cached-send return to `True` and adds a regression assertion. The live PyDECnet pin is advanced to that exact fork commit; candidate routing/NSP/Session behavior is unchanged. Fast acceptance of `3defe1bfa232f4aafd94d5e63bbee62fbf6c5ad3` was green in Repository Policy `35762127955`, Build Bootstrap `35762175317`, Project State Gate `35762178140`, Python QEMU VM Lab `35762181236`, and Independent Ethernet Interoperability `35762184295`. Next: exact-SHA acceptance with the corrected PyDECnet live pin, then repeat full Phase 6 promotion.


Full Phase 6 promotion run `35764358719` on `53582b86110f64ae152da6c702af86faa47b685a` confirmed the corrected PyDECnet cached-send behavior on ARM64 router-to-endnode reverse NML, but the x86_64 router-to-endnode role exposed a separate test-ordering race before Session Control coverage. The candidate had already formed its endnode adjacency and successfully completed specific executor/node/circuit NICE reads against PyDECnet, while the peer's first plural `adjacent nodes` query still returned an empty legal result because the faster x86_64 candidate reached management probing before the PyDECnet endnode had processed the candidate router hello. The same role is green on ARM64, and PyDECnet's endnode circuit records an adjacency only after receiving that router hello. The interoperability harness now bounds reciprocal peer-adjacency convergence for up to 10 seconds before requiring the candidate node in the plural adjacent-node result; malformed/query failures remain errors. Candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then repeat full Phase 6 promotion.


Phase 6 promotion is complete on exact protocol candidate `c11045a70e7deb59870fff5f8bc22213e0df2bc0`. Full acceptance triggered by issue #334 is green: Repository Policy `35767265536`, Build Bootstrap `35767310354`, Project State Gate `35767313766`, External Reference Baselines `35767317083`, Python QEMU VM Lab runs `35767320599`, `35767324333`, `35767327216`, and `35767330590` on x86_64/ARM64, plus Independent Ethernet Interoperability `35767334168` with all 14 Route20/PyDECnet jobs green, including both router-to-PyDECnet-endnode roles. Phase 6 exit criteria are satisfied. Phase 7 is now active; start with the standard DECnet/Linux command environment and preserve exact-SHA acceptance discipline.


Phase 7 userspace work has started with a native `ncp` command. The initial read-only subset provides one-shot and interactive `NCP>` operation, `show`/`list` for executor, specific/plural nodes and circuits, and `tell AREA.NODE` remote targeting. It translates those standard management commands onto the already-accepted native NICE client path without importing GPL LinuxDECnet code; the pinned LinuxDECnet NCP manual is behavior/reference material only. The build runs parser self-tests, candidate images install `/usr/local/sbin/ncp`, and PyDECnet interoperability now proves `ncp tell <peer> show executor status`. Next: exact-SHA fast acceptance, then extend NCP management coverage and begin standard remote-login tooling.


Exact-SHA fast acceptance for the initial native NCP frontend `4db157edddcc5dc821419e8c770a312be681c736` is green: Repository Policy `35771187931`, Build Bootstrap `35771238334`, Project State Gate `35771241038`, Python QEMU VM Lab `35771243516` on x86_64/ARM64, and Independent Ethernet Interoperability `35771246167`. NCP management coverage now adds the standard local administrative forms `set executor name NAME` and `zero executor`, mapped onto the native kernel identity/counter controls via `dnctl`; remote `tell` remains read-only until matching NICE modify/zero server operations exist. Next: exact-SHA fast acceptance, then begin the CTERM/Foundation remote-login client without importing LinuxDECnet GPL source.


Exact-SHA fast acceptance for NCP local management candidate `64e01554433444cb24468dcaa51797a1252126f7` is green: Repository Policy `35774136980`, Build Bootstrap `35774183702`, Project State Gate `35774186645`, Python QEMU VM Lab `35774189937`, and Independent Ethernet Interoperability `35774193323`. Phase 7 remote-login work now begins with a clean native `dnlogin` CTERM/Foundation boundary. The first increment connects to standard CTERM object 42 over AF_DECnet, validates the Foundation bind, emits the Linux bind-accept identity, validates the peer CTERM initiate and returns the client initiate/capability record; `--probe AREA.NODE` exercises that bounded handshake while parser/codec self-tests run in every userspace build. No LinuxDECnet GPL source is imported; its pinned manuals/source and the pinned PyDECnet remote-terminal notes are behavior/protocol references only. Interactive terminal semantics remain next.


Exact-SHA fast acceptance for the initial CTERM/Foundation probe candidate `f5d88aa235608e15ca667d624256f99f68992d7e` is green: Repository Policy `35776943205`, Build Bootstrap `35777004186`, Project State Gate `35777006116`, Python QEMU VM Lab `35777008401` on x86_64/ARM64, and Independent Ethernet Interoperability `35777010829`. The login handshake is now exercised over a real independent PyDECnet NSP/Session path: a bounded PyDECnet object-42 server accepts the candidate connection, sends Foundation BIND and CTERM INITIATE records, and independently validates the candidate bind-accept identity plus CTERM capability reply. The candidate retries only the short object-registration race and requires the full handshake before continuing. Next: exact-SHA fast acceptance, then add interactive CTERM WRITE/START-READ handling and terminal mode.


Fast acceptance of CTERM/PyDECnet proof candidate `7936aa4b6efab331ea7304aaf68a71393f2f3731` passed Repository Policy `35778423274`, Build Bootstrap `35778471238`, Project State Gate `35778474091`, and Python QEMU VM Lab `35778476856`, but Independent Ethernet Interoperability `35778480144` exposed a harness-ordering defect before protocol execution. The candidate correctly emitted `DNIV-INTEROP-CTERM-READY` before starting its NML listener sequence, while the host controller waited for `DNIV-INTEROP-NML-READY` before launching the PyDECnet object-42 server. The candidate therefore exhausted its bounded registration retries with Connection refused. This follow-up launches the host CTERM server at the candidate CTERM-ready marker before NML orchestration; candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then add interactive CTERM WRITE/START-READ handling and terminal mode.


Fast acceptance of harness-order candidate `936c117de4dacb70b9a9a3f2c92856cb8fbcdd41` passed Repository Policy `35780439395`, Build Bootstrap `35780479542`, and Project State Gate `35780482617`; x64 PyDECnet interoperability job `106924475317` in run `35780488366` then exited before the candidate reached CTERM readiness. Moving CTERM ahead of NML made it the first candidate marker, but its inherited 30-second post-convergence wait was too short for candidate boot; the prior first-marker path correctly used the architecture-bounded `timeout_seconds`. This follow-up uses that existing bound only for the initial CTERM-ready wait. Candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then interactive CTERM WRITE/START-READ handling and terminal mode.
\n\nExact-SHA fast acceptance for the corrected CTERM harness candidate `3b1a8d969f71afa6168147e94f7010f36b4edadb` is green: Repository Policy `35781043116`, Build Bootstrap `35781150597`, Project State Gate `35781152926`, Python QEMU VM Lab `35781155832` on x86_64/ARM64, and Independent Ethernet Interoperability `35781158744`. Phase 7 remote login now advances beyond the handshake: native `dnlogin AREA.NODE` enters a terminal session, decodes Foundation COMMON DATA carrying CTERM WRITE and START READ, writes host output, collects terminal input under raw terminal mode, and returns CTERM READ DATA with terminator position. The independent PyDECnet object-42 harness now serves a second live session after the probe, sends WRITE/START-READ, validates the candidate's `phase7<CR>` READ DATA response, then sends final output and disconnects cleanly. `--probe` and `--selftest` remain available. LinuxDECnet remains reference-only; no GPL source is imported. Next: exact-SHA fast acceptance, then extend CTERM control coverage (write-complete, OOB/unread/check-input/characteristics) and access-data login options.\n\n\nBuild Bootstrap `35788574378` for interactive CTERM candidate `e53d0796aa4f1eb9dc02e1762946c6f788fd4db7` failed identically on x86_64 and ARM64 before protocol execution because fortified libc marks `write(2)` as warn-unused-result and `dnlogin` discarded the return value in its optional local-echo path; `-Werror` correctly rejected that. The follow-up checks the exact byte count for both CRLF and single-byte echo writes. CTERM wire behavior is unchanged. Next: exact-SHA fast acceptance, then continue Phase 7 CTERM control and login-option coverage.\n\n\nFast acceptance for CTERM build-fix candidate `d8906804fa8bff24edd67f289a0a40c267a6d42e` passed Repository Policy `35788736038`, Build Bootstrap `35788770130`, and Project State Gate `35788772741`; x64 PyDECnet interoperability job `106951967136` in run `35788776755` then reached the new interactive CTERM exchange and exposed a harness quoting defect. The smoke script fed the literal two-character sequence `\\r` instead of carriage return, so the candidate correctly returned eight ordinary input bytes with terminator position 8; the independent harness expected a seven-byte `phase7<CR>` record with terminator position 6. The shell escape is corrected, along with the same over-escaped diagnostic newlines. Candidate CTERM behavior is unchanged. Next: exact-SHA fast acceptance, then continue Phase 7 CTERM control and login-option coverage.\n\n\nFast acceptance for shell-escape candidate `9aa40686bd0b6e7a7422eeb231ca0582f97ed4f7` again reached live CTERM but x64 PyDECnet interoperability job `106953520596` in run `35789249607` showed the smoke file still contained two literal backslashes before `r`; the prior Git-data rewrite had escaped the replacement itself. Byte-level verification now enforces exactly one backslash character before `r` in the shell source (so `printf` emits carriage return) and likewise one before `n` in diagnostics. Candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then continue Phase 7 CTERM control and login-option coverage.\n

Exact-SHA fast acceptance for interactive CTERM candidate `b0c7c355365c6315200544f61c3a679e3c03a8ca` is green: Repository Policy `35789632001`, Build Bootstrap `35789673574`, Project State Gate `35789676276`, Python QEMU VM Lab `35789678905` on x86_64/ARM64, and Independent Ethernet Interoperability `35789681346`. Native `dnlogin` now adds standard Session Control access-data options `-u USER`, `-p PASSWORD`, and `-a ACCOUNT`, encoded through `SO_CONACCESS` before connecting to CTERM object 42. The independent PyDECnet CTERM harness validates all three fields on the live interactive session before accepting it, while the probe remains credential-free. Next: exact-SHA fast acceptance, then extend CTERM control handling and terminal characteristics.


Fast acceptance for access-data candidate `26971adfc9368f1727498fb82a1f660cebfb7d24` passed Repository Policy `35790707667`, Build Bootstrap `35790754254`, and Project State Gate `35790756444`; x64 PyDECnet interoperability job `106958397196` in run `35790760961` then exposed a reference-observation mistake in the new test, not a candidate wire failure. Pinned PyDECnet deliberately exposes inbound username/account fields to an API object only when that object enables authentication; object 42 is bound with authentication off, so its API Connect event correctly contains empty access fields even when Session Control access data was present on the wire. The live CTERM harness therefore no longer asserts fields PyDECnet intentionally hides. `dnlogin -u/-p/-a` remains implemented through the already-proven native `SO_CONACCESS` path, while interactive CTERM behavior is still independently exercised. Next: exact-SHA fast acceptance, then extend CTERM control handling and terminal characteristics.


Exact-SHA fast acceptance for the access-data/reference-observation correction `29ad2d1b69f51a66c8e5e5ed92dbdb7a24aeae75` is green: Repository Policy `35791165994`, Build Bootstrap `35791198107`, Project State Gate `35791199993`, Python QEMU VM Lab `35791201684` on x86_64/ARM64, and Independent Ethernet Interoperability `35791203343`. CTERM interactive control coverage now adds CHECK INPUT/INPUT COUNT and explicit WRITE COMPLETE. Native `dnlogin` answers CHECK INPUT from the local terminal/pipe byte count via FIONREAD and already-supported WRITE completion is now independently exercised. The PyDECnet object-42 harness requests write completion, validates the exact completion record, queries queued input and requires the expected seven bytes before issuing START READ. Next: exact-SHA fast acceptance, then add CTERM terminal-characteristics handling and additional control messages.


Exact-SHA fast acceptance for CTERM control candidate `97f7ab4597a54f395d976e00fcdbc1b1bcacb988` is green: Repository Policy `35791997034`, Build Bootstrap `35792035306`, Project State Gate `35792037556`, Python QEMU VM Lab `35792039554`, and Independent Ethernet Interoperability `35792041561`. CTERM now adds READ CHARACTERISTICS/CHARACTERISTICS for a first useful terminal subset: physical character size, logical line width/page length, and handler normal-echo state. Native `dnlogin` derives geometry from TIOCGWINSZ when available and echo from termios, with conventional 80x24/8-bit defaults for non-TTY pipes. The independent PyDECnet object-42 harness requests and validates all four selectors before START READ. Next: exact-SHA fast acceptance, then add CLEAR INPUT/UNREAD/OOB and broader characteristic selectors.


Build Bootstrap `35793464970` for CTERM terminal-characteristics candidate `af57567ff9d3587619189720ebb0cce9151a4feb` failed before protocol execution because the new READ CHARACTERISTICS/CHARACTERISTICS handlers referenced message constants that were added to the PyDECnet harness but omitted from the C client constant block. This follow-up adds only those two protocol constants; behavior is otherwise unchanged. Next: exact-SHA fast acceptance, then continue broader CTERM controls.


Fast acceptance for CTERM characteristics build-fix candidate `c680618716c485545c1ad7cae2375ba8f7c05fef` passed Repository Policy `35793631849`, Build Bootstrap `35793664515`, and Project State Gate `35793666201`; x64 PyDECnet interoperability job `106967845923` in run `35793669863` then proved the candidate emitted the requested four characteristics correctly but exposed a harness length mistake. The response is 17 bytes, not 16: two-byte CHARACTERISTICS header plus 4-byte character-size tuple, 4-byte line-width tuple, 4-byte page-length tuple, and 3-byte normal-echo tuple. The harness now validates the full 17-byte body and the echo value byte. Candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then continue broader CTERM controls.


Exact-SHA fast acceptance for CTERM characteristics candidate `d161cc85d22c1a117a9d739473cd3182d41a32d0` is green: Repository Policy `35794136759`, Build Bootstrap `35794167365`, Project State Gate `35794169070`, Python QEMU VM Lab `35794170984`, and Independent Ethernet Interoperability `35794172759`. CTERM control coverage now adds host UNREAD and CLEAR INPUT. Native `dnlogin` answers UNREAD with an empty READ DATA record carrying reason flag 6, and clears pending local input with TCIFLUSH for terminals or bounded FIONREAD draining for pipes. The independent PyDECnet object-42 harness issues CLEAR INPUT after the normal read, then UNREAD and validates the exact empty READ DATA control reply before final output. Next: exact-SHA fast acceptance, then OOB input handling and broader characteristics.


Exact-SHA fast acceptance for CTERM UNREAD/CLEAR INPUT candidate `4d52cfb0dcc6dded4fee8680170e9f9bf65df2c1` is green: Repository Policy `35795111428`, Build Bootstrap `35795140483`, Project State Gate `35795142060`, Python QEMU VM Lab `35795143552`, and Independent Ethernet Interoperability `35795145092`. CTERM READ CHARACTERISTICS coverage now expands from four fields to a practical terminal profile: input/output speed, character size, parity state/type, mode-writing, attributes, VT200 terminal type, input/output flow controls, geometry, wrap, ignore-input, normal echo, escape-sequence recognition, and input-count state. Pipe-mode proof expects the default 9600-baud, 8-bit, VT200, 80x24 profile; TTY geometry and echo remain live-derived. Next: exact-SHA fast acceptance, then add asynchronous OOB input handling and readahead state.


Exact-SHA fast acceptance for practical CTERM characteristics candidate `f8af214c61c28210b1d2bccdb6a48ae6c893c44c` is green: Repository Policy `35796124281`, Build Bootstrap `35796161226`, Project State Gate `35796162960`, Python QEMU VM Lab `35796164502`, and Independent Ethernet Interoperability `35796165961`. Native `dnlogin` now adds asynchronous CTERM OOB input and readahead state. The session loop polls both NSP and stdin; Ctrl-C/Ctrl-O/Ctrl-Y are emitted as CTERM OOB records, ordinary input is buffered for a later START READ, INPUT STATE announces the empty-to-nonempty transition, CHECK INPUT includes buffered bytes, and CLEAR INPUT also clears readahead. The independent PyDECnet object-42 proof now feeds Ctrl-C before `phase7<CR>`, validates OOB plus INPUT STATE, then confirms the original seven-byte read path is preserved. Next: exact-SHA fast acceptance, then CTERM set-characteristics/input-state transitions and remaining remote-login polish.


Exact-SHA fast acceptance for asynchronous CTERM OOB/readahead candidate `c6030301354092e6718ea2d1e820044b76e37dd9` is green: Repository Policy `35798274150`, Build Bootstrap `35798305531`, Project State Gate `35798307037`, Python QEMU VM Lab `35798308553`, and Independent Ethernet Interoperability `35798310234`. Native `dnlogin` now accepts host CHARACTERISTICS updates for handler normal-echo and input-count-state, persists those values, reflects them through READ CHARACTERISTICS, and suppresses asynchronous INPUT STATE notifications when input-count-state is disabled. The independent PyDECnet object-42 proof sets both fields and reads them back before START READ. Next: exact-SHA fast acceptance, then remaining CTERM control/characteristic polish and standard Phase 7 tools beyond remote login.


Exact-SHA fast acceptance for CTERM characteristic-update candidate `4cae3302a4d62a3e37d089f2b56f16192d0245a6` is green: Repository Policy `35799031108`, Build Bootstrap `35799068199`, Project State Gate `35799069960`, Python QEMU VM Lab `35799071809`, and Independent Ethernet Interoperability `35799073943`. Phase 7 remote login now exposes the traditional `sethost` command name as a symlinked frontend to the native `dnlogin` implementation. Candidate images install `/usr/local/bin/sethost`, the userspace Makefile produces the alias, and the live PyDECnet object-42 proof accepts an additional independent `sethost --probe` session before the interactive login session. Next: exact-SHA fast acceptance, then move into DAP/FAL file-access tooling while retaining CTERM polish as needed.


Exact-SHA fast acceptance for native `sethost` frontend candidate `bbf077041445514a1cf6a839dbfda81541ec22c1` is green: Repository Policy `35799912396`, Build Bootstrap `35799982550`, Project State Gate `35799984153`, Python QEMU VM Lab `35799985680`, and Independent Ethernet Interoperability `35799987790`. Phase 7 now starts DAP/FAL tooling with a native `dncopy` foundation. `dncopy --probe AREA.NODE` opens standard FAL object 17 over native AF_DECnet, emits a DAP 4.1 CONFIG record with a bounded 1024-byte buffer and Linux/user-file-system identity, validates the peer CONFIG response, and has a local codec selftest. Candidate images install the tool, and the pinned PyDECnet interoperability harness independently accepts object 17 and validates the live CONFIG exchange before the CTERM interactive session. Next: exact-SHA fast acceptance, then add DAP ACCESS/ATTRIBUTES/CONTROL sequencing for file retrieval.


Repository Policy run `35801436510` rejected initial DAP/FAL candidate `d556b0216a38bfe70b5852a4501a26547d1c32f7` before acceptance dispatch because the two new `userspace/dncopy` files did not carry the repository's exact canonical license header. This follow-up adds the canonical headers and aligns the DECnet UAPI include with the existing native userspace tools; DAP behavior is unchanged. Next: exact-SHA fast acceptance, then DAP ACCESS/ATTRIBUTES/CONTROL retrieval sequencing.


Fast acceptance for DAP/FAL header-corrected candidate `78841f57c3be51278d88ca09a01345b5c8e3d9c7` passed Repository Policy `35801571366`, Build Bootstrap `35801608048`, and Project State Gate `35801610396`; x64 PyDECnet interoperability job `106993007326` in run `35801614613` then exposed a harness sequencing defect before DAP protocol validation. The object-17 PyDECnet API listener first delivers the Session Control connect event and requires explicit acceptance; the harness incorrectly expected the first event to be DAP CONFIG, crashed, and subsequent candidate retries received connection refused. This follow-up accepts the FAL connection before reading CONFIG. Candidate DAP behavior is unchanged. Next: exact-SHA fast acceptance, then DAP ACCESS/ATTRIBUTES/CONTROL retrieval sequencing.


Exact-SHA fast acceptance for corrected FAL connection sequencing candidate `90d3e7e83760ee9c55a14747fc6de10d11c57182` is green: Repository Policy `35801926888`, Build Bootstrap `35801958096`, Project State Gate `35801959726`, Python QEMU VM Lab `35801961500`, and Independent Ethernet Interoperability `35801963463`. Native `dncopy` now advances from CONFIG probing to a first complete DAP file-retrieval sequence. `dncopy --get AREA.NODE FILE` performs CONFIG, ACCESS OPEN, receives minimal ATTRIBUTES plus ACK, establishes a data stream with CONTROL CONNECT/ACK, starts sequential retrieval with CONTROL GET, emits DATA payloads to stdout until STATUS, then closes access with ACCOMP command/response. The PyDECnet object-17 harness validates the exact ACCESS/CONTROL/ACCOMP sequence and returns deterministic test content plus EOF status; the live smoke requires `DAP-PHASE7`. Next: exact-SHA fast acceptance, then add production-grade status decoding, access credentials, record/stream handling, and store/directory operations.


Exact-SHA fast acceptance for first complete DAP retrieval candidate `5c847e699efad8ac27094967d487c7e466d0ffc7` is green: Repository Policy `35802845531`, Build Bootstrap `35802876677`, Project State Gate `35802878403`, Python QEMU VM Lab `35802880344`, and Independent Ethernet Interoperability `35802881842`. Native `dncopy` now adds Session Control access-data options `-u/-p/-a`, strict EOF STATUS decoding for the accepted retrieval path, and binary-safe `--get-to AREA.NODE REMOTE LOCAL` output in addition to stdout retrieval. The live PyDECnet object-17 proof exercises a second file retrieval into a local file and validates its bytes. Next: exact-SHA fast acceptance, then record-format handling plus DAP store and directory operations.


Fast acceptance for DAP retrieval-hardening candidate `40788f9833c0ce6afc83360ff7122c2eaaa4a7cb` passed Repository Policy `35805723470`, but Build Bootstrap `35805754013` failed identically on x86_64 and ARM64 before protocol execution. GNU `getopt` treated command modes such as `--selftest` and `--probe` as unknown options, so the existing Makefile selftest exited with usage. This follow-up uses a small deterministic option parser that accepts optional `-u/-p/-a` fields followed by the established long-form mode token. DAP wire behavior is unchanged. Next: exact-SHA fast acceptance, then continue record/store/directory operations.


Exact-SHA fast acceptance for corrected DAP retrieval option parsing candidate `6b267ab29d8f3aebec2588e89d6af2987e199625` is green: Repository Policy `35805899980`, Build Bootstrap `35805925404`, Project State Gate `35805926856`, Python QEMU VM Lab `35805928277`, and Independent Ethernet Interoperability `35805929651`. Native `dncopy` now adds bounded DAP store with `--put LOCAL AREA.NODE REMOTE`: CONFIG, minimal ATTRIBUTES, ACCESS CREATE, CONTROL CONNECT, CONTROL PUT, DATA and ACCOMP close. The independent PyDECnet object-17 harness validates the complete upload transaction and deterministic payload. The smoke path also restores a unique PID-suffixed temporary retrieval filename. Next: exact-SHA fast acceptance, then DAP directory enumeration and richer record-format handling.


Fast acceptance for DAP store candidate `625eae851130d859a8cc10e011c62ed8a9cf9bf7` passed Repository Policy `35807279426`, Build Bootstrap `35807310043`, and Project State Gate `35807311334`; x64 PyDECnet interoperability job `107010918155` in run `35807313844` then exposed a smoke-harness placement defect. The new `dncopy --put` invocation was accidentally nested inside the failure-only branch for successful `--get-to` content validation, so the independent FAL server waited for the upload session while the candidate proceeded directly to CTERM and timed out. The temporary retrieval filename also retained a single literal dollar sign because JavaScript replacement interprets `$$` specially. This follow-up moves upload proof onto the normal success path and restores a real shell PID suffix. Candidate DAP behavior is unchanged. Next: exact-SHA fast acceptance, then directory enumeration and richer record-format handling.


Exact-SHA fast acceptance for corrected DAP store smoke candidate `ae20f824b4ab3618994f06f5ea01e7cfb4a46899` is green: Repository Policy `35807618127`, Build Bootstrap `35807647676`, Project State Gate `35807648956`, Python QEMU VM Lab `35807649403`, and Independent Ethernet Interoperability `35807649566`. Native `dncopy` now adds DAP directory enumeration with `--dir AREA.NODE SPEC`: CONFIG, ACCESS DIRECTORY, NAME parsing, optional attribute skipping, STATUS rejection, and ACCOMP response completion. The independent PyDECnet object-17 proof returns two deterministic NAME records and smoke validates the exact listing. Next: exact-SHA fast acceptance, then richer DAP record-format handling and larger/streaming stores.


Fast acceptance for DAP directory candidate `493f170dbb3a50d8b8de378f04e75822945ad303` passed Repository Policy `35810374107`, Build Bootstrap `35810398571`, and Project State Gate `35810400385`; x64 PyDECnet interoperability job `107020482696` in run `35810403589` exposed only a smoke-harness placement defect. The new directory proof was accidentally nested inside the `dncopy --put` failure branch, so successful upload skipped the expected directory session and the PyDECnet harness waited there while the candidate advanced to CTERM. This follow-up places directory proof after successful upload. Candidate DAP behavior is unchanged. Next: exact-SHA fast acceptance, then richer DAP record-format handling and larger/streaming stores.


Exact-SHA fast acceptance for corrected DAP directory smoke candidate `8f3c981313e215e56f48b757f1409e77afc58b2a` is green: Repository Policy `35810684691`, Build Bootstrap `35810710060`, Project State Gate `35810711708`, Python QEMU VM Lab `35810713855`, and Independent Ethernet Interoperability `35810715684`. Native `dncopy --put` now streams local input in repeated 1024-byte DAP DATA records instead of imposing the prior 1536-byte whole-file bound. The independent PyDECnet FAL proof uploads a deterministic 3072-byte file, verifies multiple DATA records and exact aggregate bytes, then completes ACCOMP normally. Next: exact-SHA fast acceptance, then DAP record-format negotiation and text/stream translation.


Exact-SHA fast acceptance for streamed DAP store candidate `f10633b16670e542f42dd28e09a506772faaf8b9` is green: Repository Policy `35811313228`, Build Bootstrap `35811337616`, Project State Gate `35811338813`, Python QEMU VM Lab `35811339812`, and Independent Ethernet Interoperability `35811340935`. Native `dncopy` now adds record-format-aware text retrieval with `--get-text AREA.NODE FILE`. It parses returned DAP ATTRIBUTES RFM and normalizes STM/SCR CR or CRLF to local LF while preserving raw retrieval; VAR DATA records receive local newline boundaries. The pinned PyDECnet object-17 proof advertises STM and supplies two CRLF DATA records, with smoke validating normalized output. The upload fixture also restores a real PID-suffixed temporary path. Next: exact-SHA fast acceptance, then text-mode store negotiation and broader DAP status/attribute support.


Exact-SHA fast acceptance for DAP text retrieval candidate `163fb10500e8a92e2a7b79a77ee774105f06efec` is green: Repository Policy `35812936186`, Build Bootstrap `35812964320`, Project State Gate `35812965550`, Python QEMU VM Lab `35812966848`, and Independent Ethernet Interoperability `35812968076`. Native `dncopy` now adds text-mode store with `--put-text LOCAL AREA.NODE REMOTE`: it negotiates ASCII sequential variable records with implied carriage return, emits one DAP DATA record per local line with line terminators removed, and retains raw streaming `--put` separately. The independent PyDECnet FAL proof validates the ATTRIBUTES negotiation and exact ALPHA/BETA record boundaries; directory proof includes the uploaded text file. Next: exact-SHA fast acceptance, then broaden DAP ATTRIBUTES/status decoding and file operations.


Fast acceptance for DAP text-store candidate `7c22bd1dcc00cc77be5762607773e6af12513300` passed Repository Policy `35813623747`, Build Bootstrap `35813648840`, and Project State Gate `35813649969`; x64 PyDECnet interoperability job `107030435681` in run `35813653874` exposed a candidate-side store negotiation defect. After CREATE, `dncopy` still required the returned ATTRIBUTES menu to be empty, so the valid PyDECnet proof reply advertising RFM=VAR caused the client to close before CONTROL CONNECT. This follow-up validates the returned store ATTRIBUTES through the same RFM parser used by retrieval. Next: exact-SHA fast acceptance, then broaden DAP status/attribute support and file operations.


Exact-SHA fast acceptance for store-ATTRIBUTES candidate `49566744d031079ff7a3c6a8c2804fe6e7013465` is green: Repository Policy `35813943088`, Build Bootstrap `35813969004`, Project State Gate `35813970337`, Python QEMU VM Lab `35813971859`, and Independent Ethernet Interoperability `35813973289`. Native `dncopy` now decodes DAP STATUS records into the standard major/minor fields and names recognized major status classes while retaining the exact accepted EOF status semantics; malformed and non-EOF status diagnostics remain bounded. The userspace selftest covers the wire split. Next: exact-SHA fast acceptance, then broaden DAP ATTRIBUTES parsing and file operations.


Exact-SHA fast acceptance for DAP STATUS decoding candidate `380df7aa6cd9e41ab1220c89a054ef37d68ac91c` is green: Repository Policy `35815598787`, Build Bootstrap `35815625211`, Project State Gate `35815626791`, Python QEMU VM Lab `35815628252`, and Independent Ethernet Interoperability `35815629681`. Native `dncopy` now parses the DAP ATTRIBUTES extensible menu sufficiently to locate RFM when DATATYPE and ORG precede it, accepts standard default FIX format when RFM is absent, and rejects malformed overlong extensible menus. This broadens peer attribute tolerance without changing transfer sequencing. Next: exact-SHA fast acceptance, then add further DAP file-operation coverage.


Exact-SHA fast acceptance for broadened DAP ATTRIBUTES parser candidate `e943e8fcee4090d735e27d488e7fed95364132c3` is green: Repository Policy `35816217152`, Build Bootstrap `35816242470`, Project State Gate `35816244067`, Python QEMU VM Lab `35816245753`, and Independent Ethernet Interoperability `35816247876`. Native `dncopy` now adds DAP ERASE as `--delete AREA.NODE FILE`: it performs CONFIG, emits ACCESS ERASE, requires a successful ACCESS COMPLETE response, and reports returned STATUS failures through the decoded major/minor status path. The independent PyDECnet API harness validates the exact ERASE request and the following directory proof confirms the deleted entry is absent. Next: exact-SHA fast acceptance, then continue DAP file operations and Phase 7 tooling.


Exact-SHA fast acceptance for DAP ERASE candidate `0a18bf123efa1d0ed06cc747453bea06dffc33b7` is green: Repository Policy `35825062349`, Build Bootstrap `35825115573`, Project State Gate `35825116989`, Python QEMU VM Lab `35825118569`, and Independent Ethernet Interoperability `35825120235`. The standard DECnet/Linux `dntype` and `dndir` command names now front the native DAP client: `dntype AREA.NODE FILE` performs text retrieval to stdout, while `dndir AREA.NODE [SPEC]` performs directory enumeration with a VMS-style default wildcard. Candidate images install both as symlinked frontends to `dncopy`, and independent PyDECnet API sessions prove both names on the live stack. Next: exact-SHA fast acceptance, then expand the classic DAP command surface and file-spec handling.


Exact-SHA fast acceptance for standard DAP frontends candidate `77d8ce4a65da4dba9229065323f088fbaff02243` is green: Repository Policy `35825927914`, Build Bootstrap `35825958975`, Project State Gate `35825961047`, Python QEMU VM Lab `35825963480`, and Independent Ethernet Interoperability `35825965774`. The classic DECnet/Linux `dndel` command name now fronts the already-proven native DAP ERASE operation, alongside `dntype` and `dndir`. Candidate images install all three aliases to the clean native `dncopy` implementation, and live interoperability now performs the ERASE proof through `dndel`. Next: exact-SHA fast acceptance, then add transparent DECnet file-spec parsing for the classic DAP frontends.


Exact-SHA fast acceptance for `dndel` frontend candidate `5f0b3d5ac59a8c308ceca69a69d426bf8e6d190a` is green: Repository Policy `35826619325`, Build Bootstrap `35826648140`, Project State Gate `35826650079`, Python QEMU VM Lab `35826652129`, and Independent Ethernet Interoperability `35826654034`. The classic DAP frontends now accept transparent DECnet file specifications in the familiar numeric-node form `AREA.NODE::FILE`, including optional quoted `USER PASSWORD ACCOUNT` access fields; explicit `-u/-p/-a` values take precedence. `dndir AREA.NODE::` supplies the VMS-style default wildcard. Live interoperability now exercises `dntype`, `dndir`, and `dndel` through transparent file specs. Next: exact-SHA fast acceptance, then extend transparent file-spec support into general `dncopy` source/destination forms.


Build Bootstrap `35827472079` for transparent file-spec candidate `1ef017422ca5d213d2931013a3cd12a81d56b5f7` failed identically on x86_64 and ARM64 before protocol execution because the new selftest variables were inserted into `report_status` rather than `selftest`; `-Werror` caught the unused declarations and the selftest then referenced undeclared names. This follow-up moves only those declarations to their intended scope. Transparent file-spec parsing and wire behavior are unchanged. Next: exact-SHA fast acceptance, then extend transparent file-spec support into general `dncopy` source/destination forms.


Exact-SHA full acceptance for transparent-spec selftest corrective candidate `76bb169e5e8bdeb60c0236d33236c38d6fdb9a91` is green: Repository Policy `35846489657`, Build Bootstrap `35846529342`, Project State Gate `35846532188`, External Reference Baselines `35846535125`, E1-E4 Python QEMU VM Lab `35846538034`/`35846540969`/`35846543549`/`35846546556`, and Independent Ethernet Interoperability `35846549630`. Native `dncopy` now accepts the classic two-operand form when exactly one operand is a transparent DECnet specification: remote-to-local retrieval and local-to-remote text store use the same DAP engine and credential parser as `dntype`, `dndir`, and `dndel`. A local operand of `-` maps to stdout/stdin. The independent PyDECnet FAL harness validates both directions. Next: exact-SHA fast acceptance, then add transfer-mode controls and richer classic file-copy behavior.


Exact-SHA fast acceptance for classic two-operand `dncopy` candidate `3a4f57bedb96c7cc161d382ae2403f547cbecabf` is green: Repository Policy `35868434280`, Build Bootstrap `35868492309`, Project State Gate `35868495727`, Python QEMU VM Lab `35868499014`, and Independent Ethernet Interoperability `35868502890`. The classic DAP client now accepts `-m record` and `-m block`; record mode remains the default and preserves line/record semantics, while block mode carries raw payload bytes in both transparent remote-to-local and local-to-remote copies. The independent PyDECnet FAL proof covers exact four-byte binary retrieval/store alongside the existing record-mode proof. Next: exact-SHA fast acceptance, then add record-format/carriage-control controls and richer DAP metadata.


Fast acceptance of transfer-mode candidate `6ce1e137c337af98f77dbdfe91dbd3ec7d178621` exposed a harness-only timeout in PyDECnet L1 interoperability run `35869695563`, job `107210236236`: the expanded FAL proof remained active when the fixed 30-second host `pydecnet-cterm.py` bound expired, after which the candidate's later interactive CTERM connection timed out because the API driver had been killed. The candidate had already completed routing/NICE and DAP CONFIG successfully; no protocol fault is indicated. The bounded host application-proof allowance is raised to 60 seconds to cover the added DAP record/block sessions. Product behavior is unchanged. Next: exact-SHA fast acceptance.


The first 60-second PyDECnet application-proof rerun on `99d5d59cca67d88fb688aa5546563b98045a93cb` still failed in run `35870362895`, job `107212544983`, at the same harness boundary: `pydecnet-cterm.py` printed READY, serviced the expanded DAP sequence, and was terminated exactly 60 seconds later just as the candidate advanced to interactive CTERM, producing a later connection timeout. Routing/NICE and DAP CONFIG remained healthy. The bounded application-proof allowance is raised to 90 seconds; no candidate protocol behavior changes. Next: exact-SHA fast acceptance.


The 90-second rerun on `9e10a51c64990761ae4ce4f052ecbf6d6d20ea75` again expired exactly at the host application-proof boundary in PyDECnet L1 run `35871034412`, job `107214878945`. The candidate had completed all fourteen DAP client sessions and was advancing to interactive CTERM; the guest progressed only about thirteen seconds during the ninety wall-clock seconds, so this is hosted TCG slowdown rather than a protocol stall. The application proof remains bounded but is raised to 180 seconds, still well below the existing 420-second x64 interoperability bound. Product behavior is unchanged. Next: exact-SHA fast acceptance.


Fast acceptance of bounded-app-proof candidate `c1c944423cce45a0bd766f63f656a495937d3a19` passed Repository Policy `35871986136`, Build Bootstrap `35872066870`, Project State Gate `35872070129`, and dual-architecture Python QEMU VM Lab `35872073034`; x64 PyDECnet L1 interoperability run `35872075685`, job `107218455172`, then hit the host application-proof timeout exactly 180 seconds after `pydecnet-cterm: ready`. Candidate serial evidence shows routing/NICE and the expanded DAP sequence were healthy and the guest had reached interactive CTERM when the host driver was killed; Route20 L1 passed. This is the same hosted-TCG wall-clock dilation previously isolated, not a protocol failure. Raise only the bounded PyDECnet application-proof allowance from 180 to 300 seconds, leaving candidate protocol behavior and the independent proof sequence unchanged. Next: exact-SHA fast acceptance.


Fast acceptance of 300-second application-proof candidate `72ec230bd3b4fa4f807016fc2095851cfe976f1f` again passed Repository Policy `35873169142`, Build Bootstrap `35873231446`, Project State Gate `35873234607`, and dual-architecture Python QEMU VM Lab `35873238036`; x64 PyDECnet L1 interoperability run `35873241564`, job `107222476736`, expired exactly 300 seconds after `pydecnet-cterm: ready`. Candidate serial had advanced through routing/NICE and all expanded DAP work to the interactive CTERM connect at guest uptime about 61 seconds when the host driver was killed; Route20 L1 passed. This confirms hosted TCG dilation rather than a protocol stall. Bind the PyDECnet application-proof timeout to the existing architecture-aware interoperability bound (`timeout_seconds`: 420 seconds x64, 720 seconds aarch64) instead of maintaining a separate shorter wall-clock limit. Product behavior and proof ordering are unchanged. Next: exact-SHA fast acceptance.


Fast acceptance of architecture-bound app-proof candidate `e50a79166efca677f346e23cff72a76b9a52d2cd` passed Repository Policy `35874358479`, Build Bootstrap `35874413658`, Project State Gate `35874416559`, and dual-architecture Python QEMU VM Lab `35874419894`; x64 PyDECnet L1 interoperability run `35874422887`, job `107226538001`, again ended at the host proof bound while Route20 L1 passed. Reinspection found the concrete harness defect: the new block-mode get/put smoke commands were accidentally nested inside the failure branch of the preceding transparent store. On success the candidate skipped both block sessions, while `pydecnet-cterm.py` waited forever for them and therefore never accepted the following CTERM session. This supersedes the earlier TCG-timeout diagnosis. Move the block proofs onto the normal success path and restore the bounded host application proof to 60 seconds; candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance.


Corrective follow-up: the block-proof placement fix exposed two shell temporary-name literals as a single trailing `$` rather than `$$`. Restore PID-scoped temporary paths for both transparent-copy and block-mode smoke files; proof ordering and product protocol behavior are unchanged. Next: exact-SHA fast acceptance.


Fast acceptance of PID-scoped smoke-path candidate `938b2d05866c7db187f989193407056a612b69df` passed Repository Policy `35877811072`, Build Bootstrap `35877882308`, Project State Gate `35877886743`, and the x64 E1 VM job, but PyDECnet L1 interoperability run `35877894504`, job `107238479329`, failed the new block-mode retrieval content check. The independent harness sent only payload bytes `01 02 03` because its DAP DATA test vector omitted the leading `00` byte while the candidate correctly stripped the three-byte DAP header. Align the reference vector with the already-required four-byte block-put payload `00 01 02 03`; product behavior is unchanged. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `775ba89d6600c87907ba6e3f595053b4cc257710` is green: Repository Policy `35878749694`, Build Bootstrap `35878802427`, Project State Gate `35878806829`, Python QEMU VM Lab `35878810843` (x86_64 and aarch64), and Independent Ethernet Interoperability `35878814565` (Route20 and PyDECnet L1). Follow-up inspection found the intended `$$` PID suffix correction in the smoke script had remained a single literal `$`; correct both temporary paths now. No protocol behavior changes. Next: exact-SHA fast acceptance, then continue Phase 7 DAP metadata controls.


Exact-SHA fast acceptance for `b140a0a6599e0946451ca499bacb5c43d6903d40` is green: Build Bootstrap `35880198198`, Project State Gate `35880201861`, Python QEMU VM Lab `35880205540` (x86_64 and aarch64), and Independent Ethernet Interoperability `35880208989` (Route20 and PyDECnet L1). Continue Phase 7 DAP usability by adding explicit upload record-format and carriage-control metadata controls to `dncopy`: `-r fix|var|vfc|stm|stmlf|stmcr` and `-c none|ftn|cr|prn`. Defaults preserve current behavior (VAR with implied CR for record-mode uploads; empty attributes for block mode), while explicit metadata also works with block transfers. The independent PyDECnet application proof now validates a VFC/PRN upload end-to-end. Option/value semantics were checked against pinned `tuklusan/LinuxDECnet` `ff39eef045d1e4b7b72a3d40111e89c07a473398`; implementation remains native to this repository. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `b1d9b48ce037613ea826d4df46fd6a89d83f6faf` is green: Build Bootstrap `35881602099`, Project State Gate `35881606713`, Python QEMU VM Lab `35881610675` (x86_64 and aarch64), and Independent Ethernet Interoperability `35881614434` (Route20 and PyDECnet L1). Continue Phase 7 DAP command compatibility with `DNCOPY_OPTIONS` defaults for transfer mode, record format and carriage control, plus classic attached spellings such as `-mblock`, `-rvfc` and `-cnone`. Command-line options are applied after the environment and therefore override defaults. The environment parser is deliberately limited to supported transfer-shape options; credentials remain command-line/transparent-spec inputs. Selftest covers valid overrides and malformed values. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `6e5cbec86dd76e3b5e4e94ed104502e02339d2c1` is green: Build Bootstrap `35884322136`, Project State Gate `35884325148`, Python QEMU VM Lab `35884328107` (x86_64 and aarch64), and Independent Ethernet Interoperability `35884331017` (Route20 and PyDECnet L1). Continue Phase 7 with a native `dntask` client for named DECnet task objects. The tool supports transparent numeric-node specs with optional access fields, default `TASK` object selection, binary or record-oriented output, interactive stdin/network relay, and bounded connect/inactivity timeouts. It is built and installed in candidate images and independently proven against a pinned PyDECnet named `TASKTEST` object returning two application records. LinuxDECnet `dntask` behavior and object-name limit were used only as the pinned compatibility reference; implementation is native to this repository. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `acdc835ef4a2f07d738c161a8524153065fec44b` is green: Build Bootstrap `35885464576`, Project State Gate `35885467590`, Python QEMU VM Lab `35885471207` (x86_64 and aarch64), and Independent Ethernet Interoperability `35885474258` (Route20 and PyDECnet L1). Continue Phase 7 DAP tooling with native `dnsubmit`, implemented as a `dncopy` sibling command using DAP ACCESS function SUBMIT against object 17. It accepts existing numeric/transparent DECnet file specifications and access data, is installed in candidate images, and has an independent PyDECnet FAL proof that validates CONFIG, empty ATTRIBUTES, ACCESS function 8 and the remote job file specification. LinuxDECnet `dnsubmit` at the pinned reference SHA was used for compatibility behavior only. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `336ddc0d43326c709c9bb970d66da629fd25fda7` is green: Build Bootstrap `35886641148`, Project State Gate `35886643847`, Python QEMU VM Lab `35886646935` (x86_64 and aarch64), and Independent Ethernet Interoperability `35886649876` (Route20 and PyDECnet L1). Continue Phase 7 DAP tooling with native `dnprint`, sharing the `dncopy` parser and FAL connection path. It opens the remote file through DAP object 17 and closes it with FOP spool bit 20, matching the pinned LinuxDECnet compatibility behavior. Candidate images install the sibling command and the PyDECnet FAL proof validates CONFIG, OPEN, returned attributes/ACK, and the spool-bearing ACCOMP close. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `9a360f71e97d92d1ab1c3f479d61a69db356163b` is green: Repository Policy `35887723851`, Build Bootstrap `35887839937`, Project State Gate `35887842516`, Python QEMU VM Lab `35887844943` (x86_64 and aarch64), and Independent Ethernet Interoperability `35887847701` (Route20 and PyDECnet L1). Continue Phase 7 daemon work with the native FAL object-17 listener foundation `dnfald`. The first bounded server boundary registers numeric object 17, accepts Session/NSP links, validates a DAP 4.1 CONFIG request, returns the native Linux CONFIG response and supports bounded one-shot proof mode. Candidate images install the daemon, and pinned PyDECnet now independently opens object 17 and validates the full CONFIG exchange. File access functions remain next; this commit deliberately does not pretend CONFIG-only FAL is a complete file server.


Exact-SHA fast acceptance for `ff0049797b508ebc81bc012bbe6d07fd52658a3a` is green: Build Bootstrap `35892006533`, Project State Gate `35892010184`, Python QEMU VM Lab `35892014050` (x86_64 and aarch64), and Independent Ethernet Interoperability `35892017065` (Route20 and PyDECnet L1). Extend the native FAL foundation with a bounded read path rooted at an explicit server directory. Object 17 now accepts a safe single-component DAP OPEN, returns file attributes/ACK, handles CONNECT and sequential GET, emits DAP DATA plus EOF STATUS, and completes ACCOMP close. Path traversal and wildcard/device syntax are rejected at this boundary. The pinned PyDECnet client proof retrieves `SERVER.TXT` and validates the complete application payload and close exchange. Next: exact-SHA fast acceptance, then add CREATE/PUT before directory/erase.


Exact-SHA fast acceptance for `911cbef2d44f310b2dce7f6ad136c15d43e5e903` is green: Repository Policy `35893224106`, Build Bootstrap `35893278157`, Project State Gate `35893280869`, Python QEMU VM Lab `35893283798` (x86_64 and aarch64), and Independent Ethernet Interoperability `35893287252` (Route20 and PyDECnet L1). Extend native FAL with CREATE/PUT: object 17 now accepts optional incoming ATTRIBUTES, safe CREATE access under the configured root, CONNECT/PUT, record-number-prefixed DATA, and ACCOMP close/response. The daemon gains a bounded `--sessions N` proof mode so independent PyDECnet can validate GET and PUT in separate logical links, while the candidate checks the exact written bytes. Also restore PID-scoped `$$` temporary paths for three smoke files that had regressed to literal single-dollar names; protocol behavior is unchanged. Next: exact-SHA fast acceptance, then directory/erase and richer FAL metadata.


Exact-SHA fast acceptance for `e73f50582fc2c75531d60a42d038383ae9d6c872` is green: Repository Policy `35895235244`, Build Bootstrap `35895276803`, Project State Gate `35895279279`, Python QEMU VM Lab `35895281708` (x86_64 and aarch64), and Independent Ethernet Interoperability `35895284406` (Route20 and PyDECnet L1). Extend native FAL with bounded directory and erase operations under the configured root. DIRECTORY accepts an empty or simple wildcard pattern without path/device syntax, emits DAP NAME records and ACCOMP completion; ERASE accepts only the existing safe single-component filespec and returns ACCOMP after unlink. The pinned PyDECnet proof now exercises GET, CREATE/PUT, directory enumeration and erase across four independent object-17 links and verifies the expected directory membership and final deletion. Next: exact-SHA fast acceptance, then FAL record metadata/authentication and remaining Phase 7 applications.


Fast acceptance of FAL directory/erase candidate `2a7e14fe758e4e244584d619abfe73b84ca042fb` passed Repository Policy `35896496329`, Build Bootstrap `35896540200` and Project State Gate `35896542540`; x64 VM acceptance also passed. PyDECnet L1 interoperability run `35896547465`, job `107301783769`, stopped before the new FAL exchange because `tests/lab/pydecnet-fal.py` had a harness-only Python syntax defect: the final connector cleanup lost its `finally:` clause while appending directory/erase sessions. Restore that clause only. Native FAL behavior is unchanged. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `09b1314e56302f732f3785f3f725d804a2904aae` is green: Repository Policy `35897135787`, Build Bootstrap `35897194681`, Project State Gate `35897197257`, Python QEMU VM Lab `35897200120` (x86_64 and aarch64), and Independent Ethernet Interoperability `35897202835` (Route20 and PyDECnet L1). Extend native FAL record metadata across CREATE and later OPEN using filesystem extended attributes under the configured server root. The bounded ATTRIBUTES parser accepts the current one-byte DATATYPE/ORG/RFM/RAT shape, validates RFM/RAT ranges, persists RFM and RAT as `user.decnet.*` xattrs, echoes them on CREATE, and restores them on subsequent OPEN. Independent PyDECnet now creates `UPLOAD.BIN` as VFC/PRN, opens it in a fresh object-17 session, validates returned metadata and file bytes, then continues directory/erase coverage. Next: exact-SHA fast acceptance, then inbound FAL access-control policy and remaining Phase 7 applications.


Fast acceptance of record-metadata candidate `3c3b649ce2217cf45467433309afb6c5a49e70da` passed Repository Policy `35899995777`, Build Bootstrap `35900051640` and Project State Gate `35900056236`; x64 VM acceptance passed, while PyDECnet L1 interoperability run `35900063225`, job `107313644001`, disconnected immediately after OPEN because metadata-less `SERVER.TXT` changed from the established compact fixed-record ATTRIBUTES reply to the new expanded DATATYPE/ORG/RFM/RAT form. Preserve the compact `{RFM=FIX}` encoding for files without stored metadata and use the expanded encoding only when non-default metadata exists. The logged `ENODATA` text was stale errno from successful missing-xattr fallback, not the root failure. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `8e6f40c1da576f4d54c73defc3912ffcc4792db0` is green: Repository Policy `35900586088`, Build Bootstrap `35900635427`, Project State Gate `35900638045`, Python QEMU VM Lab `35900640774` (x86_64 and aarch64), and Independent Ethernet Interoperability `35900643443` (Route20 and PyDECnet L1). Continue DAP/FAL completeness with native RENAME. The FAL server accepts ACCESS function 3 followed by a FILESPEC NAME record, applies the existing safe single-component root policy, uses the host rename primitive (preserving xattrs), and returns ACCOMP response. A new `dnrename` multicall command emits the matching client exchange and is installed in candidate images. Independent PyDECnet renames the metadata-bearing upload before directory enumeration and erase, proving server-side interoperability and metadata-preserving filesystem semantics. Next: exact-SHA fast acceptance, then remaining FAL operations/applications.


Exact-SHA fast acceptance for `5e6208fb0b95fc12b82ab967eeef6ec516d09be8` is green: Repository Policy `35902239694`, Build Bootstrap `35902297081`, Project State Gate `35902299625`, Python QEMU VM Lab `35902302367` (x86_64 and aarch64), and Independent Ethernet Interoperability `35902305247` (Route20 and PyDECnet L1). Add the Phase 7 DECnet-native static web-server foundation `dnhttpd`. It binds named object `HTTP`, accepts bounded HTTP/1.0 GET requests over NSP, serves one safe root-level static file per request, maps `/` to `index.html`, rejects traversal/subdirectory syntax, and returns explicit 400/404 responses. Candidate images install the daemon, and independent PyDECnet opens object `HTTP`, sends a GET request and validates the status line and fixture body. Next: exact-SHA fast acceptance, then standard PHONE/mail applications and remaining FAL completeness.


Repository-policy run `35910274283` for `1bb6cf35c2e43bf7ec5a385b125b50b1e7502a1e` stopped before acceptance because the three newly added web-server files did not carry the repository's canonical project header. Restore only the canonical headers in `userspace/dnhttpd/dnhttpd.c`, `userspace/dnhttpd/Makefile`, and `tests/lab/pydecnet-http.py`; web-server behavior is unchanged. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `47f0af19c20a8f4df0efd50fe5454043edfa36f7` is green: Repository Policy `35910500749`, Build Bootstrap `35910559345`, Project State Gate `35910562098`, Python QEMU VM Lab `35910565370` (x86_64 and aarch64), and Independent Ethernet Interoperability `35910568633` (Route20 and PyDECnet L1). Begin the standard PHONE application with native object-29 daemon `dnphoned`. The first bounded server path accepts a configured local user, decodes the classic PHONE CONNECT framing, returns REPLYOK/REPLYNOUSER, accepts the subsequent DIAL record and acknowledges it. Command numbers and object 29 were checked against pinned `tuklusan/LinuxDECnet` `ff39eef045d1e4b7b72a3d40111e89c07a473398`; implementation is native. Independent PyDECnet proves CONNECT and DIAL against the candidate. Next: exact-SHA fast acceptance, then add the user-facing PHONE client/data path.


Fast acceptance for PHONE foundation `d076a342404307f5e07694b08e15f970d0c2feb7` reached Repository Policy `35911714003` and Project State Gate `35911764756`, but Build Bootstrap `35911761794`, VM Lab `35911767668`, and interoperability `35911770533` all stopped at the same compile-only defect: strict C11 does not expose `strnlen` without a feature-test macro. Replace that call with a small repository-local bounded string-length helper; PHONE protocol behavior is unchanged. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `0ebfe3e14b6266dea77bf8b952bfc0aca5f4dd3f` is green: Repository Policy `35913344807`, Build Bootstrap `35913395218`, Project State Gate `35913397796`, Python QEMU VM Lab `35913400249` (x86_64 and aarch64), and Independent Ethernet Interoperability `35913403689` (Route20 and PyDECnet L1). Add the first user-facing PHONE client/data path. New `phone` connects to numeric `AREA.NODE::USER` object 29, performs CONNECT and DIAL handshakes, and sends one classic PHONE DATA record; `-s NODE::USER` selects the presented local name. `dnphoned` now accepts one DATA record after DIAL, validates its source terminator and emits the delivered text. Candidate images install the client, while independent PyDECnet extends its object-29 proof through DATA delivery. Next: exact-SHA fast acceptance, then expand PHONE session controls/directory and proceed to mail.


Exact-SHA fast acceptance for `435afa74ffa0e73025768f3fd63778e3b9326950` is green: Repository Policy `35916298840`, Build Bootstrap `35916344282`, Project State Gate `35916347035`, Python QEMU VM Lab `35916350805` (x86_64 and aarch64), and Independent Ethernet Interoperability `35916353911` (Route20 and PyDECnet L1). Begin the standard mail application with native MAIL-11 object 27 client/server foundations. `dnmail` sends the classic sender, recipient, recipient-ack, address terminator, full address, subject, body-records and final terminator sequence; `dnmaild` accepts the same sequence, acknowledges recipient and delivery, and appends a bounded local spool record under an explicit root. Object number and record ordering were checked against pinned `tuklusan/LinuxDECnet` `ff39eef045d1e4b7b72a3d40111e89c07a473398`; implementation remains native. Candidate images install both tools, and independent PyDECnet validates object-27 delivery plus exact spool content. SMTP/sendmail bridging, MAIL-11 v3 connect data and multiple recipients remain subsequent work.


Exact-SHA fast acceptance for `1310c66d3d1e623d764fc5bb03bc58a2e402581e` is green: Repository Policy `35917696092`, Build Bootstrap `35917747732`, Project State Gate `35917751680`, Python QEMU VM Lab `35917754440` (x86_64 and aarch64), and Independent Ethernet Interoperability `35917757138` (Route20 and PyDECnet L1). Extend native MAIL-11 recipient handling from one addressee to the protocol's repeated-recipient sequence: object 27 now accepts and acknowledges each recipient until the lone-NUL terminator, rejects an empty recipient set, bounds the assembled address list, and records all recipients in the spool envelope. Independent PyDECnet now proves two recipient acknowledgements and exact combined spool content. Next: exact-SHA fast acceptance, then MAIL-11 v3/connect-data and local delivery bridging.


Exact-SHA fast acceptance for `c99dac1001b69de8fe268e8bcc1df7c3051ae10f` is green: Repository Policy `35920122893`, Build Bootstrap `35920162849`, Project State Gate `35920165176`, Python QEMU VM Lab `35920167156` (x86_64 and aarch64), and Independent Ethernet Interoperability `35920169346` (Route20 and PyDECnet L1). Complete the first MAIL-11 multiple-recipient path on both sides. Harden the server's bounded recipient-list assembly at the exact buffer edge, and teach `dnmail` to split comma-separated recipient names, send each recipient as its own MAIL-11 record, require an acknowledgement for every addressee, then emit the lone-NUL terminator. The full-address record remains the original user-facing destination string. Next: exact-SHA fast acceptance, then MAIL-11 v3/connect-data and delivery bridging.


Fast acceptance for `4705936c5518f3465380a3837438328de302e15e` exposed one strict-C11 portability defect before promotion: Build Bootstrap `35921268597` failed on x86_64 and aarch64 because `strtok_r` is not declared without a POSIX feature-test macro. Replace the recipient splitter with a small C11-only comma scanner; MAIL-11 wire behavior is unchanged. VM/interoperability runs for the same candidate continued independently, but this compile failure is sufficient to reject that candidate. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `cf7a1573d368545c7604ed243a307c682117d3e4` is green: Repository Policy `35922127402`, Build Bootstrap `35922180265`, Project State Gate `35922182899`, Python QEMU VM Lab `35922186141` (x86_64 and aarch64), and Independent Ethernet Interoperability `35922188788` (Route20 and PyDECnet L1). Add native MAIL-11 v3 Session Control optional-data negotiation. `dnmaild` now uses deferred accept on object 27, accepts legacy zero-length connect data unchanged, validates 16-byte v3 requests and returns the 16-byte v3 capability block before entering the existing mail record exchange. `dnmail -3` sends the pinned LinuxDECnet-compatible v3 request and requires a v3 response. Independent PyDECnet supplies the same connect data and validates the returned accept data before the two-recipient delivery proof. Next: exact-SHA fast acceptance, then local sendmail/SMTP bridge and remaining Phase 7 completeness.


Fast acceptance for MAIL-11 v3 candidate `23cad17208a85542aa92f23cc5c75798ac87b7e7` stopped in Build Bootstrap `35923545809` on the same compile-only defect on both architectures: `dnmaild.c` added `uint16_t` endian helpers without including `<stdint.h>`. Add the missing standard header only; protocol behavior is unchanged. VM/interoperability jobs for the rejected candidate may continue independently, but promotion is blocked by the build failure. Next: exact-SHA fast acceptance.


MAIL-11 v3 exposed a native deferred-accept socket ABI contradiction before promotion of `bf6df486ac4dde1318c35204bcad51331f0cc24b`: request-dependent accept data must be set on the accepted child, but `DSO_CONDATA` rejected every socket that already had a local link even though `DSO_CONACCEPT` consumes that child's `conndata_out`. Allow `DSO_CONDATA` only for an `ACC_DEFER` accepted child while its NSP connection remains in CR, retaining `EISCONN` everywhere else. The inbound socket proof now deliberately leaves listener accept data empty and sets `linux-accept` on the deferred child immediately before `DSO_CONACCEPT`, covering the exact MAIL-11 requirement. Next: exact-SHA fast acceptance, then resume local sendmail/SMTP delivery bridging.


Repository tree integrity was immediately restored after the preceding low-level Git tree write omitted the base tree. This follow-up rebuilds the exact parent tree and reapplies only the deferred-accept socket/test/document deltas; no additional protocol behavior is changed. Next: exact-SHA fast acceptance, then resume local sendmail/SMTP delivery bridging.


Fast acceptance of deferred-accept candidate `2e913c4718eef308044cc0bb1930bd6284bb2c23` passed Repository Policy `35927160893`, Build Bootstrap `35927206787`, Project State Gate `35927209224` and Python QEMU VM Lab `35927211443` (x86_64 and aarch64). Route20 L1 interoperability also passed, but the PyDECnet L1 job in run `35927213490` hung immediately after MAIL readiness because the independent proof passed Session connect data under the connector-internal name `conndata`; pinned PyDECnet's public dictionary API names that argument `data`. Change only the harness keyword to the pinned API's actual public interface. Candidate MAIL-11/socket behavior is unchanged. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `e827d970ace0e943fbc39bb9579373ac94ace362` is green: Repository Policy `35937635557`, Build Bootstrap `35937674753`, Project State Gate `35937676694`, Python QEMU VM Lab `35937678600` (x86_64 and aarch64), and Independent Ethernet Interoperability `35937680551` (Route20 and PyDECnet L1). Continue MAIL-11 local-delivery integration with an explicit `dnmaild --sendmail PATH` bridge. The daemon feeds one RFC-style message to a directly executed sendmail-compatible program using fixed `-i -t` arguments, never a shell; delivery is bounded to 30 seconds and the MAIL-11 final ACK is withheld if the local MTA fails. The independent PyDECnet proof now drives the MAIL-11 v3/two-recipient exchange through a fake sendmail executable and validates both invocation arguments and delivered headers/body in addition to the spool copy. Next: exact-SHA fast acceptance, then add the optional SMTP delivery path.


Exact-SHA fast acceptance for `2df6fdbe5ac1723d79a0b07cbe65db757ad7e8c6` is green: Repository Policy `35938488605`, Build Bootstrap `35938520254`, Project State Gate `35938522061`, Python QEMU VM Lab `35938523731` (x86_64 and aarch64), and Independent Ethernet Interoperability `35938525546` (Route20 and PyDECnet L1). Complete the first MAIL-11 local-delivery bridge set with optional direct SMTP delivery: `dnmaild --smtp HOST [--smtp-port PORT] [--smtp-from ADDRESS]` performs bounded HELO/MAIL/RCPT/DATA/QUIT exchange, dot-stuffs record lines, rejects embedded CR/LF/NUL in SMTP-mode body records, and withholds the MAIL-11 completion ACK unless SMTP accepts the message. SMTP and sendmail delivery modes are mutually exclusive. Candidate-image acceptance now includes a tiny native loopback SMTP oracle; pinned PyDECnet drives a second v3/two-recipient MAIL-11 delivery and the candidate verifies the delivered SMTP message end-to-end. Next: exact-SHA fast acceptance, then continue remaining Phase 7 PHONE/FAL/application completeness.


Repository Policy run `35939519450` rejected SMTP candidate `d1e37a2c6d0166272d832227294e7f8f972c48cb` before acceptance because the low-level tree write changed executable modes on `tests/lab/prepare-candidate-image.sh` and `tests/lab/run-interop.sh` from `100755` to `100644`. Restore only those parent-tree modes; SMTP behavior is unchanged. Next: exact-SHA fast acceptance.


Repository Policy `35939667948` rejected `793697e8730195962146a618f4b1990da0d96eb1` because the new SMTP resolver implementation used reserved repository-policy vocabulary in standard resolver field names. Replace that resolver path with the pinned compatibility reference's IPv4 host lookup shape; SMTP protocol behavior is unchanged. Next: exact-SHA fast acceptance.


Repository Policy `35941142358` confirmed the previous rename was insufficient because standard `struct addrinfo` field names also collide with reserved repository-policy vocabulary. Use the pinned LinuxDECnet-compatible IPv4 resolver shape with `gethostbyname` and `sockaddr_in`; delivery semantics remain unchanged. Next: exact-SHA fast acceptance.


Exact-SHA fast acceptance for `b30eefc296104ce83cf06dd36c3953e27b378b82` is green: Repository Policy `35941303768`, Build Bootstrap `35941337499`, Project State Gate `35941339515`, Python QEMU VM Lab `35941341577` (x86_64 and aarch64), and Independent Ethernet Interoperability `35941343707` (Route20 and PyDECnet L1). Continue Phase 7 PHONE completeness with the classic object-29 DIRECTORY operation. `dnphoned` now answers a directory request with a bounded entry for its configured local user and gains bounded `--sessions N` proof mode; the independent PyDECnet PHONE proof validates DIRECTORY on a second logical link after CONNECT/DIAL/DATA. Command 0x0f and directory behavior were checked against pinned LinuxDECnet `ff39eef045d1e4b7b72a3d40111e89c07a473398`. Next: exact-SHA fast acceptance, then PHONE session controls and remaining FAL/application completeness.


Exact-SHA fast acceptance for `35b88bc790ffce0194446e1d489e8c1508fb27d1` is green: Repository Policy `35942366517`, Build Bootstrap `35942397717`, Project State Gate `35942399184`, Python QEMU VM Lab `35942400641` (x86_64 and aarch64), and Independent Ethernet Interoperability `35942402005` (Route20 and PyDECnet L1). Extend native PHONE session control on object 29 with HOLD, UNHOLD, HANGUP and GOODBYE handling. The one-shot `phone` client now closes a sent message with classic HANGUP/GOODBYE instead of simply dropping the NSP link, while independent PyDECnet exercises HOLD/UNHOLD plus clean teardown before the already-proven DIRECTORY session. Command values are checked against pinned LinuxDECnet `ff39eef045d1e4b7b72a3d40111e89c07a473398`. Next: exact-SHA fast acceptance, then remaining PHONE answer/reject boundary and FAL/application completeness.


Exact-SHA fast acceptance for `6014f06d8fd8f1eb2970d4624d9fb5d4d116a6ef` is green: Repository Policy `35943049219`, Build Bootstrap `35943079184`, Project State Gate `35943081035`, Python QEMU VM Lab `35943082766` (x86_64 and aarch64), and Independent Ethernet Interoperability `35943084505` (Route20 and PyDECnet L1). Continue Phase 7 FAL completeness with inbound Session Control access-data policy. `dnfald` now optionally requires configured user/password/account fields via the native `DSO_CONACCESS` socket option before any DAP exchange; absent policy preserves the prior open behavior. The independent PyDECnet FAL matrix now authenticates all six object-17 sessions with explicit credentials, without logging them. Next: exact-SHA fast acceptance, then negative authentication proof and remaining FAL/application completeness.


Exact-SHA fast acceptance for `5a8f02e12cbdc25ac4304df094cd4364ea7b22c2` is green: Repository Policy `35944285938`, Build Bootstrap `35944322355`, Project State Gate `35944324717`, Python QEMU VM Lab `35944326648` (x86_64 and aarch64), and Independent Ethernet Interoperability `35944328402` (Route20 and PyDECnet L1). Add the negative half of FAL access-control proof. A credential mismatch now closes only that object-17 session, records a generic access-denied diagnostic without credential values, and leaves the daemon available for later sessions. The independent PyDECnet proof first connects with a deliberately wrong password and requires a disconnect before DAP service, then runs all six authenticated positive sessions. Next: exact-SHA fast acceptance, then remaining FAL/application completeness.


Exact-SHA fast acceptance for `73c22353938a6bffccfca426c32faa2fd394edec` is green: Repository Policy `35944957588`, Build Bootstrap `35944986311`, Project State Gate `35944987990`, Python QEMU VM Lab `35944989606` (x86_64 and aarch64), and Independent Ethernet Interoperability `35944991262` (Route20 and PyDECnet L1). Begin the Phase 7 native compatibility-library surface with a clean `libdnet` address-conversion foundation. The repository now builds a native static `libdnet.a` plus `<netdnet/dn.h>` and `<netdnet/dnetdb.h>` compatibility headers implementing numeric `dnet_addr`, `dnet_ntoa`, `dnet_htoa`, `dnet_pton` and `dnet_ntop`; focused selftests cover valid/boundary/error conversion behavior. Pinned LinuxDECnet `ff39eef045d1e4b7b72a3d40111e89c07a473398` defines the compatibility signatures only; implementation is native. Next: exact-SHA fast acceptance, then add `dnet_conn` and independent MIRROR proof before expanding node/object database helpers.


Fast acceptance for libdnet foundation `a30a10f330c80522c553f386aa5bfa08eec372d3` stopped in Build Bootstrap `35945875789` on x86_64 and aarch64 because the new compatibility wrapper includes `<linux/dn.h>` but its Makefile exposed only the local compatibility include directory, not this repository's authoritative UAPI tree. Add `../../include/uapi` to libdnet's compile include path; library behavior is unchanged. Next: exact-SHA fast acceptance.

Exact-SHA fast acceptance for `66e6a6c742d4045d241e36d88c51b4b455fdd55c` is green: Repository Policy `35946022323`, Build Bootstrap `35946049131`, Project State Gate `35946050842`, Python QEMU VM Lab `35946052526` (x86_64 and aarch64), and Independent Ethernet Interoperability `35946053858` (Route20 and PyDECnet L1). The native libdnet address-conversion foundation is therefore accepted.

Extend the native libdnet compatibility surface with `dnet_conn`, retaining the pinned LinuxDECnet call signature while translating its raw optional-data arguments to this stack's structured `DSO_CONDATA` UAPI. The first native implementation supports numeric nodes, numeric or named objects, `NODE/USER/PASS/ACCOUNT` access data, SOCK_SEQPACKET/SOCK_STREAM selection, bounded socket timeouts and returned connect data. Candidate images now install the static library and compatibility headers, and a native libdnet client connects outbound to pinned PyDECnet MIRROR object 25, validates its two-byte accept data and proves positive plus malformed-record loopback behavior. Named-node lookup remains deliberately deferred to the database-helper increment. Next: exact-SHA fast acceptance, then add node/object database helpers.

Exact-SHA fast acceptance for `684d517da489bba71843fd4f97d3167c78abd0c4` is green: Repository Policy `35947425023`, Build Bootstrap `35947495941`, Project State Gate `35947497489`, Python QEMU VM Lab `35947499214` (x86_64 and aarch64), and Independent Ethernet Interoperability `35947500876` (Route20 and PyDECnet L1). Native `dnet_conn` and its independent outbound MIRROR proof are therefore accepted.

Expand libdnet with native node/object database helpers. The compatibility header now exposes `nodeent`, `getnodebyname`, `getnodebyaddr`, `getnodeadd`, `getexecdev`, node iteration, the classic built-in object catalog, object number/name lookup and high-object-number controls. Node lookup uses the established `/etc/decnet.conf` compatibility format with bounded parsing; `dnet_conn` now falls back from numeric text to that node database. The independent PyDECnet proof creates a two-entry executor/peer database in the disposable candidate, verifies forward/reverse/executor/device/iteration lookups and MIRROR object lookup, then connects by node name and named object rather than numeric selectors. Signatures and database format were checked against pinned LinuxDECnet `ff39eef045d1e4b7b72a3d40111e89c07a473398`; implementation remains native. Next: exact-SHA fast acceptance, then complete the remaining core libdnet compatibility helpers.

Exact-SHA fast acceptance for `5fc7e06d081460d8b883f60b8a2a68a51162eff7` is green: Repository Policy `35949364623`, Build Bootstrap `35949398010`, Project State Gate `35949399448`, Python QEMU VM Lab `35949400851` (x86_64 and aarch64), and Independent Ethernet Interoperability `35949402435` (Route20 and PyDECnet L1). Native libdnet node/object database lookup and named-node/named-object MIRROR interoperability are therefore accepted.

Complete the next core libdnet compatibility slice with `dnet_recv`, `dnet_eof`, `getnodename` and a shared-library build. `dnet_recv(..., MSG_EOR)` uses native record-boundary reporting, `dnet_eof` maps native link state through `DSO_LINKINFO`, and `getnodename` resolves the configured executor from the node database. The library now builds both `libdnet.a` and versioned `libdnet.so.1.0` with the `libdnet.so.1`/ `libdnet.so` compatibility links. Candidate images install both forms, while the independent MIRROR proof consumes the new receive/EOF/node-name helpers. Next: exact-SHA fast acceptance, then add daemon/listener compatibility helpers and close the Phase 7 library surface.

Exact-SHA fast acceptance for `d7ccc7ec4a6924aceb8a42bb7150980bcffc71d7` is green: Repository Policy `35950232116`, Build Bootstrap `35950262455`, Project State Gate `35950264387`, Python QEMU VM Lab `35950266378` (x86_64 and aarch64), and Independent Ethernet Interoperability `35950268212` (Route20 and PyDECnet L1). The shared/static libdnet build plus `dnet_recv`, `dnet_eof` and `getnodename` are therefore accepted.

Add the native daemon/listener compatibility library surface: `libdnet_daemon` now provides `dnet_daemon`, `dnet_accept`, `dnet_reject`, `dnet_set_optdata` and `dnet_daemon_name`, using deferred native AF_DECnet listeners and the existing Session Control socket options rather than legacy kernel assumptions. Static and versioned shared daemon libraries are installed in candidate images. A dedicated compatibility server binds named object `LIBMIRROR`, accepts through the library API and serves the MIRROR record protocol; pinned PyDECnet independently connects to that named object and validates accept data, positive records and malformed-record failure. Next: exact-SHA fast acceptance, then audit remaining Phase 7 application/library gaps against the roadmap and close only those with concrete missing behavior.

Fast acceptance for daemon-compatibility candidate `a701a50812176209fb5473ce853a61578dce433a` passed Repository Policy `35951083403` and Project State Gate `35951117088`, but Build Bootstrap `35951115392` failed on both architectures and VM Lab `35951118830` failed at candidate build because strict warnings reject the deliberately ignored `chdir("/")` return value in `daemon.c`. Check that return value and propagate failure; daemon protocol behavior is unchanged. The candidate is rejected on this compile-only defect. Next: exact-SHA fast acceptance.

Exact-SHA fast acceptance for `d7388754c5f62256eaadde280c9bc85eb18378a8` is green: Repository Policy `35951279432`, Build Bootstrap `35951308329`, Project State Gate `35951309501`, Python QEMU VM Lab `35951310725` (x86_64 and aarch64), and Independent Ethernet Interoperability `35951311984` (Route20 and PyDECnet L1). The native `libdnet_daemon` compatibility surface and independent named-object server proof are therefore accepted.

The Phase 7 gap audit identifies a concrete missing diagnostics command from the standard DECnet/Linux environment: `dnping`. Add a native MIRROR-object diagnostic client with bounded count, frame size, interval and timeout controls plus quiet/verbose output. It resolves numeric or `/etc/decnet.conf` node names through native libdnet, validates every returned MIRROR record byte-for-byte and reports packet/round-trip statistics. Candidate images install the tool, and the pinned PyDECnet interoperability path now requires three 128-byte MIRROR loops through `dnping`. Behavior and command purpose were checked against pinned LinuxDECnet `ff39eef045d1e4b7b72a3d40111e89c07a473398`; implementation remains native. Next: exact-SHA fast acceptance, then continue the Phase 7 gap audit with generic object-service dispatch and remaining administration tooling.

Exact-SHA fast acceptance for `c73dfda8aa8c66362c8bf2f70be9db950c84f848` is green: Repository Policy `35952268719`, Build Bootstrap `35952298813`, Project State Gate `35952300239`, Python QEMU VM Lab `35952302132` (x86_64 and aarch64), and Independent Ethernet Interoperability `35952303714` (Route20 and PyDECnet L1). Native `dnping` is therefore accepted.

A boundary audit found one local diagnostics defect outside the exercised 128-byte interoperability case: a one-byte `dnping -s 1` request reused byte zero for the per-packet sequence marker, overwriting the MIRROR request opcode. Preserve byte zero as the required MIRROR opcode and vary byte one only when present. No kernel or wire behavior outside that one-byte client boundary changes. Next: exact-SHA fast acceptance, then continue Phase 7 with generic object-service dispatch.

Exact-SHA fast acceptance for `264b9f322ff9aa21fd7da47e502ef3650f5c40bb` is green: Repository Policy `35952905898`, Build Bootstrap `35952939658`, Project State Gate `35952941153`, Python QEMU VM Lab `35952943100` (x86_64 and aarch64), and Independent Ethernet Interoperability `35952944778` (Route20 and PyDECnet L1). The one-byte `dnping` boundary correction is therefore accepted.

Continue Phase 7 with a native generic object super-server, `dnetd`. The first bounded compatibility slice reads the classic five-field `dnetd.conf` shape, creates one native deferred AF_DECnet listener per configured numbered or named object, rejects unsupported wildcard and authentication modes rather than silently weakening them, supports classic auto-accept/reject control, optionally drops to an explicit local account, and execs configured programs directly with the accepted DECnet socket on stdin/stdout without a shell. Candidate images install `dnetd`; the independent PyDECnet proof connects to a configured named `DNETDTEST` object whose child accepts through native `libdnet_daemon` and serves the MIRROR protocol. Configuration purpose and execution model were checked against pinned LinuxDECnet `ff39eef045d1e4b7b72a3d40111e89c07a473398`; implementation remains native. Authentication/proxy and wildcard task dispatch remain explicit later increments, not false claims. Next: exact-SHA fast acceptance, then add authenticated/proxy object policy or close Phase 7 if the remaining gap audit finds no release-blocking standard surface.

Fast acceptance for dnetd candidate `5f4f4b05f8d777ee5814d0d753bf45b082164ad3` passed Repository Policy `35953676180`, while Build Bootstrap `35953701646` failed on x86_64 and aarch64 because the new super-server used the legacy-only `DNSTAT_REJECTED` convenience constant, which is intentionally not part of this repository's native UAPI. The reject policy does not require disconnect data: issue native `DSO_CONREJECT` directly and remove the undeclared compatibility constant. Product behavior remains a clean Session rejection. Next: exact-SHA fast acceptance.

Exact-SHA full promotion acceptance for `a967af2787638ab49bf1a929a27ef9f7ab8564e9` is green: Repository Policy `35954451490`, Build Bootstrap `35954483756`, Project State Gate `35954485477`, External Reference Baselines `35954487274`, x86_64/aarch64 E1-E4 VM runs `35954489283`, `35954491199`, `35954492983`, `35954494765`, and full 14-job Independent Ethernet Interoperability `35954497878`. Phase 7 is complete. The accepted userspace environment includes native NCP/NICE management, sethost/dnlogin/CTERM, DAP/FAL file tools and access policy, task/object access, PHONE, MAIL-11 with local sendmail and SMTP delivery bridges, the DECnet-native web server, MIRROR diagnostics, static/shared libdnet plus libdnet_daemon compatibility, and generic dnetd object dispatch, all on the native kernel stack and proven against pinned independent peers.

Begin Phase 8. Local/rootless VDE2 remains independently green at run `35440502997`, and local MULTINET TCP framing/reconnect remains independently green at run `35440787624`. The next work is to strengthen both transport proofs with restart/stress/multi-peer coverage, add the repository-tracked secret-safe Area-31 workflow and reusable Linux/VAX checks, and prove an actual cross-runner VDE2 switch-to-switch join before distributed scale depends on it. External Area-31 success remains supplemental and never substitutes for exact-SHA local acceptance.

Fast acceptance for Phase 7 closure commit `24ec051d25d4074555a4fc5af3f559b44f87bc29` is green: Repository Policy `35958370764`, Build Bootstrap `35958416991`, Project State Gate `35958419134`, Python QEMU VM Lab `35958421050` (x86_64 and aarch64), and Independent Ethernet Interoperability `35958423109` (Route20 and PyDECnet L1).

Strengthen the first Phase 8 VDE2 transport gate without conflating it with MULTINET. The local/rootless proof now requires a three-endpoint native libvdeplug multicast stress run with 256 content-checked frames, an endpoint close/reopen during the run, explicit failure to open a nonexistent switch, complete VDE switch teardown/recreation followed by a second 256-frame stress run, and Route20/PyDECnet adjacency recovery after the PyDECnet peer is stopped long enough for the adjacency to expire and then restarted. This upgrades the local VDE2 gate from a one-frame/one-adjacency sanity proof to positive, negative, multi-peer, restart and bounded stress coverage. Next: run the exact VDE2 workflow and fast acceptance; then strengthen MULTINET separately before introducing the Area-31 remote workflow.

Exact VDE2 workflow `35959075714` is green for `7331e36c243cd8cacd97b3ba4fd2d14c11ffd528`: the enhanced local/rootless gate passed three-endpoint frame stress, endpoint restart, nonexistent-endpoint rejection, full switch restart and independent Route20/PyDECnet adjacency restart. Exact-SHA fast acceptance is also green: Repository Policy `35959092344`, Build Bootstrap `35959125604`, Project State Gate `35959127790`, Python QEMU VM Lab `35959129713` (x86_64 and aarch64), and Independent Ethernet Interoperability `35959131714` (Route20 and PyDECnet L1).

Strengthen MULTINET independently. The live proof now uses one two-circuit L2 listener router and two simultaneous L1 connector routers, first confirms that a connector aimed at an unopened TCP port never reports Circuit up, then proves both live circuits, churns one connector through five complete down/reconnect cycles while the other remains alive, and finally restarts the central listener and requires both connectors to recover. The pinned PyDECnet MULTINET module suite still runs first, covering its fragmentation/coalescing and protocol mechanics. Next: run the exact MULTINET workflow and fast acceptance; then add the secret-safe Area-31 workflow on top of these independently proven local transports.

MULTINET workflow `35959801944` rejected `9eb7eedc03d0da17b50c64454e763ec2c4d6925b` before live transport execution. All 40 pinned PyDECnet MULTINET module tests passed, but the new live three-router configuration used circuit labels `MUL-B` and `MUL-C`; the pinned parser permits alphabetic prefixes followed only by hyphen/digits. Rename the central circuits to `MUL-1`/`MUL-2` and connector circuits to `MUL-0`. No MULTINET protocol behavior changes. Next: rerun the exact MULTINET workflow and fast acceptance.

MULTINET retry workflow `35959966978` accepted the corrected three-router configuration and formed both simultaneous live circuits, but the churn harness then false-failed waiting for a specific `Circuit down` event before reconnect. The transport's successful initial two-circuit state is present in the retained log; the pinned module suite had already passed all 40 cases, including local/remote restart and close detection. Make the live restart discriminator depend on the stronger externally useful invariant: after each killed connector or central listener is restarted, a new `Circuit up` must be observed while the unaffected peer remains alive. This removes an unnecessary event-timing dependency without weakening reconnect proof. Next: rerun the exact MULTINET workflow and fast acceptance.

Light documentation/code sanity pass after `92e5f56293e9a0747b7116bcf4fbb0476b22c5ec`: enhanced MULTINET workflow `35960257018` is green on that exact commit, followed by fast acceptance Build Bootstrap `35960311447`, Project State Gate `35960313483`, Python QEMU VM Lab `35960315522` (x86_64 and aarch64) and Independent Ethernet Interoperability `35960317698`. A later scheduled full reproof of the same exact commit is also green through Build Bootstrap `35984721554`, Project State Gate `35984724951`, External Reference Baselines `35984728083`, x86_64/aarch64 E1-E4 runs `35984731413`, `35984734808`, `35984737886`, `35984740897`, and full interoperability `35984744413`.

The sanity pass found one concrete code drift and several documentation drifts. `dnmultinet.py` still selected only Ethernet adjacency events 4.15/4.16 even though its WAN circuit is MULTINET point-to-point, whose useful circuit down/up events are 4.8/4.10; generate both point-to-point and Ethernet event classes and lock the generated configuration in its Makefile tests. Refresh Phase 8 status, enhanced VDE2/MULTINET run IDs, the current PyDECnet live pin and the three-router MULTINET acceptance description across the lab/pre-production documents. No kernel, routing, NSP or Session behavior changes. Next: exact-SHA fast acceptance, then implement the secret-safe Area-31 workflow and reusable Linux/VAX checks; cross-runner VDE2 remains separately outstanding.

Documentation sanity follow-up: remove the stale NSP-foundation paragraph that had drifted under the Phase 7 heading; NSP remains fully recorded in the Phase 5 chronology below. No implementation or acceptance requirement changes. Fast acceptance of the preceding sanity commit `e0da821f6ba10f8b080d8615463af77889052236` is green: Repository Policy `35991295675`, Build Bootstrap `35991336543`, Project State Gate `35991338731`, Python QEMU VM Lab `35991340917` (x86_64 and aarch64), and Independent Ethernet Interoperability `35991343247` (Route20 and PyDECnet L1).

Begin the repository-tracked Area-31 integration boundary. Add a manual-only Area-31 workflow whose first executable step verifies all five required runtime secret names before checkout or remote lab activity, masks populated values, and requires an explicitly assigned disposable Area-31 gateway address/name for each run. Extend `dnmultinet.py` with a secret-safe runtime-peer mode that reads the remote host/port from environment variables rather than process arguments, refuses to print runtime peer configuration in dry-run mode, and can expose a local PyDECnet API socket without persisting its generated configuration. Add a reusable Area-31 driver and NICE probe that start a rootless VDE-backed L2 MULTINET gateway, keep gateway output discarded, and require executor summary/status/counter replies from the VAX router through the remote path without printing the VAX address or credentials. `VAX_USERNAME` and `VAX_PASSWORD` are preflighted but intentionally unused until credential-bearing Linux/VAX pairs are added. This establishes the secret-safe remote workflow foundation only; it is not yet Phase 8 exit evidence because a native DECnet-IV-Linux VM has not traversed the VDE-to-MULTINET path and cross-runner VDE2 remains unproven. Next: exact-SHA fast acceptance, then manually execute the Area-31 workflow with an assigned gateway identity and, once remote routing is green, put a native Linux VM on the VDE side for routing/NICE/NSP/Session pairs.

Fast acceptance rejected Area-31 workflow candidate `86bfafde35e0123328e6a3c5ce6eadca8c4089cb` in Build Bootstrap `35996149827`: the new peer-host validator accidentally double-escaped the host character class and rejected the existing valid `127.0.0.1` selftest before any remote workflow execution. Replace that over-constrained expression with the actual configuration invariant needed here: peer hosts must be nonempty and contain no whitespace. This accepts ordinary hostnames plus IPv4/IPv6 forms while still preventing config-token injection. No transport or protocol behavior changes. Next: exact-SHA fast acceptance.

Exact-SHA fast acceptance for Area-31 workflow fix `0b1c34f5163c5cfe444b64e2b353327d77ec5171` is green: Repository Policy `35996298162`, Build Bootstrap `35996345148`, Project State Gate `35996348444`, Python QEMU VM Lab `35996351412` (x86_64 and aarch64), and Independent Ethernet Interoperability `35996354404` (Route20 and PyDECnet L1). Strengthen the new secret-safety boundary with build-time regressions: compile the Area-31 NICE helper, syntax-check the driver, require a complete dummy preflight to pass, require an omitted password to fail by secret name only, and require secret-backed `dnmultinet --dry-run` to fail without echoing the peer value. No remote Area-31 traffic is generated by these local checks. Next: exact-SHA fast acceptance, then remote execution when an Area-31 gateway identity is explicitly assigned; meanwhile continue the independent cross-runner VDE2 path without inventing rendezvous infrastructure.

Fast acceptance rejected secret-preflight regression candidate `2afd4b71152d3160e0f0b2d29b01091363e9c442` in Build Bootstrap `35997309449` on both architectures. The product preflight itself correctly passed with all five dummy values; the new negative Makefile checks were defective because shell command substitution and the captured variable used single-dollar syntax, so Make consumed them before the shell ran and falsely reported that a missing password was accepted. Escape both constructs for the recipe shell. Product and secret-validation behavior are unchanged. Next: exact-SHA fast acceptance.

Exact-SHA fast acceptance for secret-preflight regression fix `a9bd1594ad6a287d971ae23cb61be33fca6e226b` is green: Repository Policy `35997600349`, Build Bootstrap `35997647871`, Project State Gate `35997650495`, Python QEMU VM Lab `35997652948` (x86_64 and aarch64), and Independent Ethernet Interoperability `35997655595` (Route20 and PyDECnet L1).

Continue the independent cross-runner VDE2 path without assuming hosted runners are directly reachable. Add a manual two-runner proof that uses an explicitly supplied existing SSH bastion only as rendezvous: the server runner exposes a restricted ephemeral SSH endpoint through an assigned reverse-forward port, each runner owns its own `vde_switch`, and the client joins the switches with the documented `dpipe vde_plug = ssh ... vde_plug` transport. A shared native libvdeplug probe requires marked frame request/response delivery in both directions; Route20 on the server and pinned PyDECnet on the client must form an adjacency, recover after the SSH/VDE bridge is disconnected/reconnected, then recover again after the client VDE switch and PyDECnet are restarted. The SSH key and pinned bastion host-key material remain runtime-only and the inner ephemeral SSH account is command-restricted to the VDE plug/readiness/completion operations. Extend the local VDE2 gate to compile and exercise the same marked-frame helper, and add build-time syntax/preflight negatives for the cross-runner driver. This commit establishes an executable cross-runner gate but does not claim the cross-runner exit criterion until the manual two-host workflow itself is green. Next: run exact local VDE2 plus fast acceptance; then execute the cross-runner workflow when the pre-existing SSH rendezvous secrets and an unused reverse-forward port are available, while Area-31 remote execution remains separately gated by its assigned DECnet identity.

Repository Policy `36000237236` rejected cross-runner VDE2 candidate `40b851733736bae71281292606998a095170d228` before acceptance because the new C helper used a block-comment rendering of the license text instead of the repository's canonical C/C++ line-comment header. Convert only that header to the canonical form; cross-runner transport/test behavior is unchanged. Next: rerun exact local VDE2 proof and fast acceptance.

The first exact acceptance of cross-runner gate header-fix candidate `cb38a24a17cad09dc9072b4068d50aab2d5354f5` passed Repository Policy and exact local VDE2 workflow `36000393096`, but Build Bootstrap `36000449054` rejected both architectures in the new cross-runner negative preflight recipe. As with the earlier Area-31 regression, Make consumed single-dollar command-substitution/output references before the recipe shell ran, making the missing-key negative appear to succeed. Escape only those cross-runner recipe dollars; the driver preflight itself is correct and VDE2 transport behavior is unchanged. Next: exact local VDE2 plus fast acceptance.

Exact local VDE2 proof and fast acceptance for cross-runner preflight fix `4810091fd200000fbab2d1a24b143530e8b412c3` are green. VDE2 Transport Proof `36000880150` passed the enhanced local/rootless transport gate; Repository Policy `36000880200`, Build Bootstrap `36000960787`, Project State Gate `36000963485`, Python QEMU VM Lab `36000966262` (x86_64 and aarch64), and Independent Ethernet Interoperability `36000968595` (Route20 and PyDECnet L1) all passed on the same exact commit. The repository-tracked cross-runner gate is therefore locally accepted but remains external evidence until a real two-runner rendezvous run is green. Area-31 remote execution also remains gated by runtime secrets and an explicitly assigned disposable DECnet identity. Next: add the native DECnet-IV-Linux VM side of the Area-31 VDE-to-MULTINET path and reusable routing/NICE/NSP/Session checks without weakening the external-execution prerequisites.

Exact-SHA fast acceptance for continuity commit `533fc5c65dbc0a31cea96e3abf1c11d8f51352f2` is green: Repository Policy `36002783262`, Build Bootstrap `36002830764`, Project State Gate `36002833276`, Python QEMU VM Lab `36002835692` (x86_64 and aarch64), and Independent Ethernet Interoperability `36002838297` (Route20 and PyDECnet L1). A direct sanity review of the still-unexecuted cross-runner path found a concrete generated-wrapper defect: the unquoted heredoc expanded `SSH_ORIGINAL_COMMAND` while the server driver was constructing its forced-command wrapper, so every later inner-SSH command would have been compared against an empty string. Generate the wrapper through one helper that preserves the runtime variable, and add a build-time wrapper selftest that proves the allowed readiness command dispatches while an unlisted command returns 126. Cross-runner external evidence remains unclaimed until a real two-runner rendezvous succeeds. Next: exact local VDE2 plus fast acceptance, then continue the native Linux-through-Area-31 gateway path while external executions remain prerequisite-gated.

Exact local VDE2 proof and fast acceptance for cross-runner forced-command fix `3b1099e507e00adbb2a81541e25bf73e8bd1d159` are green: VDE2 Transport Proof `36003884984`, Repository Policy `36003891747`, Build Bootstrap `36003972241`, Project State Gate `36003974854`, Python QEMU VM Lab `36003977418` (x86_64 and aarch64), and Independent Ethernet Interoperability `36003980287` (Route20 and PyDECnet L1). Extend the Area-31 gate from a host-side PyDECnet reachability probe to an exact native DECnet-IV-Linux candidate VM on the gateway VDE segment. The remote workflow now builds the exact amd64 candidate from the source-independent foundation, supplies the assigned Linux/gateway/VAX identities through a private read-only control disk rather than process arguments, requires native adjacency to the gateway, executes native NML summary/status/counters and three content-checked MIRROR exchanges to the VAX router, and reports only generic pass/failure markers. No runtime credential value is placed on the VM command line or retained as evidence. This is repository coverage only until the external Area-31 workflow executes with assigned identities and secrets. Next: exact-SHA fast acceptance; then add credential-bearing Linux/VAX application pairs without exposing credentials, while actual Area-31 and cross-runner runs remain external prerequisites.

Exact-SHA fast acceptance for native Area-31 candidate-path commit `3e539d0e5069f361926b07293469fd6938e2baef` is green: Repository Policy `36005649942`, Build Bootstrap `36005705657`, Project State Gate `36005708328`, Python QEMU VM Lab `36005711252` (x86_64 and aarch64), and Independent Ethernet Interoperability `36005713886` (Route20 and PyDECnet L1). Extend the still-unexecuted Area-31 external gate with the first credential-bearing native Linux/VAX application pairs. `dnlogin` and `dncopy` now accept `DNACCESS_USER`/`DNACCESS_PASSWORD`/`DNACCESS_ACCOUNT` defaults so runtime access data does not need to appear in application arguments; explicit command-line options still override those defaults. The disposable Area-31 control image carries the VAX user/password only in private files, and the native VM must pass an authenticated CTERM/Foundation probe plus an authenticated FAL/DAP CONFIG exchange after the already-required NICE and MIRROR proofs. Secret values are neither logged nor retained as artifacts. Next: exact-SHA fast acceptance, then continue repository-side Area-31 application coverage while actual Area-31 and cross-runner VDE2 execution remain gated by their external runtime prerequisites.

Fast acceptance rejected credential-probe candidate `393a22c70a4d08696df1464849e486ce55ae8b43` in Build Bootstrap `36015960885` on both x86_64 and aarch64. The new VAX credential preflight was rendered with malformed Bash newline-pattern syntax and failed syntax validation before any Area-31 network activity. Repair the bounded credential validation without changing credential transport or protocol behavior. Next: exact-SHA fast acceptance.

Fast acceptance rejected credential-preflight repair candidate `de1e5d44bc0824390028b6f45e78d9b9b9481a92` in Build Bootstrap `36016477522`. The first repair preserved a stale malformed fragment at the end of `prove-area31.sh` and over-escaped the CR/LF patterns. Remove the stale tail and restore literal Bash ANSI-C newline/carriage-return patterns. No product or wire behavior changes. Next: exact-SHA fast acceptance.

Exact-SHA fast acceptance for credential-bearing Area-31 probe candidate `994ecb0818d6b1e2039b3d50d8a64117ca0500b9` is green: Repository Policy `36016773860`, Build Bootstrap `36016833963`, Project State Gate `36016837287`, Python QEMU VM Lab `36016841176` (x86_64 and aarch64), and Independent Ethernet Interoperability `36016845386` (Route20 and PyDECnet L1). Extend the native VAX-side FAL proof from authenticated CONFIG only to a read-only authenticated directory operation for the login account's default directory using `*.*;*`; output remains discarded so external filenames are not retained as evidence. Also normalize the smoke-script command continuations. Next: exact-SHA fast acceptance, then continue repository-side Area-31 application coverage without inventing unprepared remote services; actual Area-31 and cross-runner VDE2 executions remain external prerequisites.

Exact-SHA fast acceptance for Area-31 FAL directory candidate `b8a08be77afe3fe4d16f4d75f57fb4308a9c48dc` is green: Repository Policy `36018731565`, Build Bootstrap `36018790020`, Project State Gate `36018793939`, Python QEMU VM Lab `36018797490` (x86_64 and aarch64), and Independent Ethernet Interoperability `36018802274` (Route20 and PyDECnet L1). Add native `dnlynx`, a bounded HTTP/1.0-over-DECnet client for named object `HTTP`, with optional Session access data supplied by the existing `DNACCESS_*` environment defaults. Install it into exact candidate images and add the reverse application-direction proof missing from the existing `dnhttpd` coverage: the native Linux candidate must fetch content from a dynamically bound independent PyDECnet HTTP object. This makes HTTP application proof bidirectional and reusable on x86_64/aarch64 full interoperability runs. QCOCAL is a worthwhile later real-VAX target once the Area-31 external gate is actually live; do not assume a historical node address or mutate a remote VAX before runtime reachability and current identity are proven. Next: exact-SHA fast acceptance, then full x86_64/aarch64 independent HTTP-client proof at the next consolidated/full gate while Area-31 and cross-runner executions remain external prerequisites.

Exact current-head full acceptance for native HTTP client candidate `3e7928335807cb0ff63f9dce1078b638c4ab798d` is green through Build Bootstrap `36021867668`, Project State Gate `36021871497`, External Reference Baselines `36021875243`, the full x86_64/aarch64 Python VM matrix `36021878906`/`36021882933`/`36021887119`/`36021890820`, and full independent interoperability `36021894313`. Add the real-VAX QCOCAL HTTP experiment without guessing its node address: the manual Area-31 workflow accepts an optional explicit `qcocal_node`; when present, the exact native Linux VM uses the existing runtime VAX credentials to upload a temporary OpenVMS zero-object `HTTP.COM` through FAL, fetches it with native `dnlynx`, requires the fixed response marker, and deletes the remote procedure. The OpenVMS command procedure uses the documented named zero-object/SYS$NET activation model. This remains repository-prepared coverage, not claimed external evidence, until the Area-31 workflow actually runs with a current QCOCAL address and succeeds. Next: exact-SHA fast acceptance, then execute Area-31 when its explicit runtime identities are available; cross-runner VDE2 remains independently external-gated.

Repository Policy `36024926392` rejected QCOCAL candidate `9d6267e260390a6d9e3a902906ac322a6cb13a85` before acceptance because the checked-in OpenVMS `.COM` procedure necessarily used DCL `$!` comments while LICENSE-MONKEY classifies generic text suffixes under the canonical hash-comment header. Do not weaken policy or inject non-DCL header lines into a remote command procedure. Replace the checked-in `HTTP.COM` with a canonical Python generator that creates the ephemeral DCL procedure only inside the disposable control image. QCOCAL protocol behavior is unchanged. Next: exact-SHA fast acceptance.

Exact-SHA fast acceptance for runtime-generated QCOCAL object candidate `bfa7eb73dbe94f5eab51006fa3fffd0a8a3d5682` is green: Repository Policy `36025276435`, Build Bootstrap `36025337871`, Project State Gate `36025341706`, Python QEMU VM Lab `36025345852` (x86_64 and aarch64), and Independent Ethernet Interoperability `36025349071` (Route20 and PyDECnet L1). Generalize the still-manual Area-31 external gate so the exact native proof runs sequentially on both amd64 and arm64 hosted runners using the corresponding immutable foundation and native QEMU machine. The QCOCAL deployment remains serialized, so its temporary `HTTP.COM` cannot race between architectures. Add build-time selftest of the DCL generator plus amd64/arm64 Area-31 preflight checks. No external Area-31 success is claimed until the manual workflow executes with assigned gateway/Linux identities and, for QCOCAL, its current explicit Area-31 address. Next: exact-SHA fast acceptance; then execute the dual-architecture Area-31 gate when runtime identities are available.

Exact-SHA fast acceptance for dual-architecture Area-31 candidate `e49e28bb8a748eb9645eddad418cec9ecaaa6099` is green: Repository Policy `36026736544`, Build Bootstrap `36026788201`, Project State Gate `36026791357`, Python QEMU VM Lab `36026794470` (x86_64 and aarch64), and Independent Ethernet Interoperability `36026797274` (Route20 and PyDECnet L1). Remove the need to know QCOCAL's numeric node address in advance: after MULTINET routing and the configured VAX NML endpoint are live, query its NICE known-node summary, resolve the exact node name `QCOCAL`, and feed the discovered Area-31 address into the existing temporary DCL/FAL/`dnlynx` proof. An explicit `qcocal_node` remains a validated override. Discovery failure leaves the optional QCOCAL subtest disabled rather than weakening the required VAX router proof. Next: exact-SHA fast acceptance; then execute the manual dual-architecture Area-31 workflow when disposable gateway/Linux node identities are available.

Add a local syntax regression for the new NICE QCOCAL discovery helper so build acceptance cannot leave that manual Area-31 path unparsed until the first external run. This is test-only; live discovery behavior is unchanged. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Harden QCOCAL NICE discovery: first query the volatile SHOW-known-node view, then fall back to the permanent LIST-known-node database when the live table does not carry the requested name. This avoids guessing an address and preserves the explicit override. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Make the QCOCAL experiment non-destructive. Native `dnlynx` now accepts a bounded `-o OBJECT` named-object override while retaining `HTTP` as the default. The Area-31 VAX proof uses the project-specific zero-object name `DNIVHT` and temporary `DNIVHT.COM` instead of overwriting/deleting a potentially pre-existing user `HTTP.COM`. Object names are restricted to 1..16 alphanumeric characters beginning with a letter and covered by the existing native selftest. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Prove the new `dnlynx -o OBJECT` path before relying on QCOCAL. Extend the pinned PyDECnet HTTP server oracle to bind a caller-selected named object and response marker, and add a second independent client-direction exchange using object `DNIVHT` after the existing default `HTTP` exchange. This exercises the exact object override used by the non-destructive VAX experiment under normal x86_64/aarch64 independent interoperability. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Fail closed around the temporary QCOCAL object file. Before installing `DNIVHT.COM`, the native VM performs an authenticated FAL directory query for `DNIVHT.COM;*`; any query failure aborts, and any pre-existing version aborts without modification. Only a proven-empty name is uploaded, exercised through `dnlynx -o DNIVHT`, and removed. This prevents the external experiment from overwriting an existing VAX account file. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Extend the optional QCOCAL pair with generic task/object access. `dntask` now consumes `DNACCESS_USER`/`DNACCESS_PASSWORD`/`DNACCESS_ACCOUNT` defaults when access fields are not embedded in the task specification, keeping credentials off command lines while preserving explicit transparent syntax precedence. The Area-31 control image generates a second temporary VMS zero-object procedure, `DNIVTK.COM`; QCOCAL preflight fails closed if either project-specific object file already exists, then the native VM uploads `DNIVTK.COM`, invokes `QCOCAL::DNIVTK` through `dntask`, requires `QCOCAL-DECNET-TASK-PASS`, and removes the procedure. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Fail closed on oversized `dntask` environment-supplied access data. `DNACCESS_USER`, `DNACCESS_PASSWORD`, and `DNACCESS_ACCOUNT` now produce a bounded validation error rather than silently falling back to an unauthenticated task connection when an environment field exceeds the classic DECnet access-data limit. Explicit task-spec access fields remain authoritative. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Bind an explicit QCOCAL override back to live NICE information before any remote file mutation. The Area-31 driver now always attempts the VAX manager's QCOCAL name lookup; when both an explicit `qcocal_node` and a live NICE mapping exist, they must match exactly or the proof stops. A manager with no QCOCAL mapping may still use the explicit override, preserving the escape hatch for sparse VMS databases. Next: exact-SHA fast acceptance, then manual dual-architecture Area-31 execution when disposable identities are available.

Exact-SHA fast acceptance for QCOCAL NICE cross-check candidate `8e25ef0ac72993f02efbd38cf34aba499864efc8` is green: Repository Policy `36032464953`, Build Bootstrap `36032524591`, Project State Gate `36032527137`, Python QEMU VM Lab `36032529528` (x86_64 and aarch64), and Independent Ethernet Interoperability `36032532035` (Route20 and PyDECnet L1). Repository-side Phase 8 preparation is now at the external-execution boundary. Remaining Phase 8 exit evidence requires (1) a real dual-architecture Area-31 workflow run using explicitly assigned disposable gateway/Linux node identities and the configured runtime secrets, and (2) a real two-runner VDE2 workflow run using an existing SSH rendezvous and assigned reverse-forward port. Do not fabricate either result or substitute local acceptance. Next: execute and record those external gates when their runtime inputs are available; close Phase 8 only if both are green.

Fast acceptance of continuity commit `56e80b84a7b2266dc3b98cedd2064225b62d1d9c` passed Repository Policy `36034862042`, Build Bootstrap `36034917777`, Project State Gate `36034920798`, x86_64 E1 and Independent Ethernet Interoperability `36034926627`, but Python QEMU VM Lab `36034923551` false-failed the arm64 E1 controller bound. The guest emitted `DNIV-E1-PASS` at uptime 378.213 seconds, just after the configured 375-second arm64 controller budget; protocol recovery had already completed successfully. Raise only the bounded arm64 VM-lab controller allowance to 420 seconds, preserving all protocol and convergence assertions. Next: exact-SHA fast acceptance, then return to the Phase 8 external-execution boundary.

Exact-SHA fast acceptance for arm64 E1 controller-bound candidate `c36dcea810dd10d82bac26e64421b9638410cd9e` is green: Repository Policy `36036073070`, Build Bootstrap `36036128169`, Project State Gate `36036131759`, Python QEMU VM Lab `36036134865` (x86_64 and aarch64), and Independent Ethernet Interoperability `36036138366` (Route20 and PyDECnet L1). The bounded arm64 allowance change therefore clears its intended false-timeout without weakening protocol assertions. Phase 8 is back at the external-execution boundary: execute and record the real dual-architecture Area-31 workflow and the real two-runner VDE2-over-SSH workflow when their external runtime inputs are available; close Phase 8 only if both are green.

Automate Phase 8 execution. Consolidated/full acceptance dispatches local VDE2 and MULTINET proofs; full acceptance dispatches cross-runner VDE2-over-SSH and dual-architecture Area-31/MULTINET interoperability. No human-entered DECnet identity or reverse port is required. The cross-runner port is exact-SHA-derived. Area-31 uses only Area 31: it establishes the secret-backed MULTINET path, asks PYRTR at 31.3 for its known nodes through NML, chooses two high-number Area-31 addresses absent from that list, then restarts the gateway on the selected identity before the native Linux/VAX proof. Secrets remain GitHub Actions secrets and are never logged. Child workflows bind checkout to the dispatcher-supplied exact SHA. Next: exact-SHA fast acceptance, then full acceptance to execute autonomous Phase 8 external gates.

Repository Policy `36041058603` rejected `dd51159bb8e441efb84c1034d0d35a4d6bd1053c` solely because new `tests/lab/area31-list-known.py` lacked the canonical blank line after the project license header. Restore the canonical Python header formatting; workflow/protocol behavior is unchanged. Next: exact-SHA fast acceptance.

Full Area-31 run `36043104934` exposed a host packaging defect on x86_64: Ubuntu's hosted QEMU reports no built-in `-netdev vde`, so the external proof stopped before any Area-31 traffic. Preserve VDE2 as the lab fabric but add a host TAP fallback: when QEMU lacks its optional VDE backend, create a disposable TAP owned by the runner, bridge it to the VDE switch with `vde_plug2tap`, attach QEMU through its standard TAP backend, and clean both bridge and TAP on exit. No DECnet protocol behavior changes. Next: exact-SHA fast acceptance, then rerun full acceptance and continue through the real Area-31 and cross-runner gates.

Full external run `36045020013` reached the autonomous Area-31 preflight but exposed a generated-name defect before network activity: high-number nodes such as 31.1021 produced seven-character names (`DNG1021`), exceeding the Phase IV six-character node-name limit. Keep the exact-SHA high-number allocation but use fixed six-character `DG%04d`/`DL%04d` names for gateway/Linux identities. No wire behavior changes. The same full run confirmed local VDE2 `36045011635`, local MULTINET `36045014203`, full VM coverage, references, and independent interoperability are green. Cross-runner VDE2 `36045016850` remains blocked because all configured `VDE_SSH_*` Actions secrets resolve empty; do not fabricate rendezvous credentials. Next: exact-SHA fast acceptance, then rerun full Area-31; keep cross-runner fail-closed until a real SSH rendezvous secret set exists.

Full Area-31 run `36051102753` now passes autonomous identity preflight but both architectures lose the user-space VDE/MULTINET gateway before VAX NML becomes reachable. The driver previously discarded the gateway stderr, hiding the concrete PyDECnet failure. Retain the gateway log only in the disposable job workspace and emit it on early gateway/NICE failure through explicit masking of every secret-backed host/port/VAX/access value. No protocol behavior changes. Cross-runner run `36051099600` still fails closed because all `VDE_SSH_*` rendezvous secrets are absent. Next: exact-SHA fast acceptance, then rerun full acceptance to capture and fix the concrete Area-31 gateway failure.

Masked diagnostics from x86_64 Area-31 run `36054313471` exposed the gateway failure: pinned PyDECnet rejects a single top-level config file containing both `routing` and `api` (`Exactly one of routing, bridge, or http and/or api required`). Fix `dnmultinet` to emit routing and API as separate PyDECnet top-level config files and pass both files to `decnet.main`; memory-backed configs remain the default and secret-backed peer data is still never printed or persisted. This preserves the local API socket strictly as control plumbing while PYRTR is queried through real NML over DECnet. Next: exact-SHA fast acceptance, then rerun full Area-31.

Tighten the `dnmultinet` split-config regression test: exercise `build_config()` and `build_api_config()` directly and require the routing config to contain no API component while the separate API config contains the requested socket. The earlier validate-only check returned before config generation and therefore did not cover the defect. Runtime behavior is unchanged.

Full Area-31 x86_64 run `36056177189` proved the MULTINET circuit itself is healthy: the gateway reached `Circuit up` with adjacent PYRTR at `31.3`, but the driver incorrectly waited for the separate VAX NICE endpoint before asking PYRTR for topology, creating a false ordering dependency and repeated local API disconnect noise. Reorder the proof around the actual network contract: wait for the real MULTINET adjacency to PYRTR, issue the standard NCP/NML equivalent of `TELL 31.3 SHOW KNOWN NODES`, choose the final disposable Area-31 gateway/Linux identities from addresses absent in that returned view, restart on the chosen identity, then require the VAX NICE proof. Remove the now-unneeded custom known-node API helper. Also make the split routing/API config regression test exercise generated configs directly and refresh stale HECnet lab documentation. No area other than 31 is used. Next: exact-SHA fast acceptance, then rerun full acceptance.

Fast Build Bootstrap `36058353726` rejected the new split-config regression on both architectures because the Makefile passed `\\n` to Python as a literal backslash-plus-n instead of a newline escape; the implementation itself already generates separate routing and API configs. Correct only the test literal escaping. Next: exact-SHA fast acceptance.

Full Area-31 run `36059574567` confirmed the new PYRTR NML query works on both architectures, but the allocator searched only 31.900-31.1023 and found no two addresses absent from PYRTR's known-node view. That high-number restriction was artificial. Scan the complete legal Area-31 node range 1..1023 in a deterministic SHA-seeded permutation, excluding PYRTR 31.3 and the configured VAX address, and choose the first two addresses not returned by `TELL 31.3 SHOW KNOWN NODES`. No other area is ever considered. Next: exact-SHA fast acceptance, then rerun full acceptance.

Full acceptance at exact main `259abc71d3c20f075b5032efd4719d47641b07af` completed Python QEMU VM Lab `36062371887` green on x86_64/aarch64 and Independent Ethernet Interoperability `36062374633` green across all 14 jobs. Cross-runner VDE2 run `36062382395` remains correctly fail-closed because the required `VDE_SSH_*` rendezvous secrets are absent. Area-31 run `36062384963` reached the real PYRTR NML query on both architectures but the allocator still rejected the entire legal Area-31 range. The concrete defect is whole-file address matching against NCP `SHOW KNOWN NODES`: anonymous `Unreachable` routing-table rows are placeholders rather than allocated/configured identities, yet their addresses were treated as occupied. Parse only anchored Area-31 node rows; treat named rows and rows whose state is not `Unreachable` as occupied, ignore anonymous `Unreachable` placeholders, require a structured PYRTR 31.3 row, and retain explicit PYRTR/VAX exclusions. Keep the standard `TELL 31.3 SHOW KNOWN NODES` NML query; do not substitute `LIST KNOWN NODES`, whose permanent flag does not by itself remove routing-layer rows in the pinned PyDECnet implementation. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 x86_64 run `36066609444` finally exposed the exact PYRTR NCP rendering. `TELL 31.3 SHOW KNOWN NODES` returns an executor banner `Executor node = 31.3 (PYRTR), State = On,` and omits 31.3 from the following node table. The Area-31 table itself contains named reachable/unreachable entries plus anonymous `Unreachable` routing slots throughout the legal range, confirming that whole-file address matching was the allocation defect. The first structured parser correctly distinguishes named/non-Unreachable rows from anonymous placeholders but incorrectly required PYRTR 31.3 to appear as a table row. Parse the executor banner as an occupied structured identity as well; the existing explicit 31.3 exclusion remains. No query or protocol behavior changes. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 run `36080601382` on exact candidate `fdc7a32b5c38dbdd104cbbfbba276d6c4a730974` proves the structured allocator fix: x86_64 selected 31.285 as absent from PYRTR's occupied-node view, restarted as DG0285, and re-established the real MULTINET adjacency to PYRTR 31.3. The next failure is later: VAX NICE did not converge. The retry loop discarded every helper diagnostic, so the run cannot distinguish missing routing from an NML Session rejection. Preserve the last `area31-nice.py` diagnostic and, on timeout, print only the configured VAX row from the local gateway's real NCP `SHOW KNOWN NODES` view before the already-masked gateway log. This is diagnostic-only and does not change DECnet protocol behavior, identities, credentials or queries. Cross-runner VDE2 remains separately fail-closed on the absent SSH rendezvous secrets. Next: exact-SHA fast acceptance, then rerun full acceptance and fix only the newly exposed concrete Area-31 cause.

Full Area-31 run `36082276468` on exact candidate `97aaa18718cfeb99e377b74fb2c6deeb3f031173` exposes the next concrete x86_64 failure. After allocating and restarting, the local gateway's own NCP known-node view reports the configured VAX reachable through `WAN-0` via PYRTR 31.3, so routing convergence is not the blocker. The NML Session connect also succeeds and returns a data message; `area31-nice.py` then raises `NICE summary failed` because its hard-coded positive-reply signature rejects the real VAX response. Preserve the response type and a bounded 64-byte hex prefix on failure so the real VMS NICE framing can be implemented exactly rather than guessed. Tighten the route diagnostic to match the VAX address as the first table field so a short address such as 31.1 does not also print 31.10..31.199. No protocol behavior changes yet. Next: exact-SHA fast acceptance, then rerun full acceptance to capture the real VAX NICE response bytes and fix only that parser defect.

Full Area-31 x86_64 run `36083581481` on exact candidate `0213583bc30f2cfd139f1ba3cf3031d426b60ba5` captured the real VMS NICE framing: the first executor reply is `02ffff00`, the standard retcode-2 multiple-response header. The prior helper incorrectly required its first frame to be a retcode-1 item and therefore rejected a valid VAX NML session. Mirror the pinned PyDECnet NCP response iterator: consume retcode 2 as list start, validate positive node-item replies, require retcode -128 list termination, and reject negative/error or malformed sequences. Build the executor requests with the pinned NICE classes rather than raw literals; this also corrects the previously mislabeled summary request from information selector 2 (characteristics) to selector 0 (summary), while retaining status 1 and counters 3. Add syntax coverage for the helper. Candidate kernel and wire protocol behavior are unchanged. Next: exact-SHA fast acceptance, then full acceptance and continue only from concrete external results.

Full Area-31 x86_64 run `36084834122` on exact candidate `3581114467ddf8dd2cba531bb5d095efce792aef` proves the VMS NICE framing correction: executor summary, status and counters all pass, and QCOCAL is discovered through NICE. The run then exposes two host-lab defects before the native VM can execute. The DECnet MAC arithmetic at `prove-area31.sh` is missing the final arithmetic-expansion parenthesis, so Bash interprets `<< 2` as a here-document at runtime and corrupts the generated MAC. Separately, the QEMU-without-VDE fallback gives the same TAP device to both `vde_plug2tap` and QEMU; the first process owns the TAP queue and QEMU fails with `Device or resource busy`. Correct the MAC expression and use two TAP devices joined by a disposable Linux bridge: one TAP is owned by `vde_plug2tap`, the other by QEMU. Extend cleanup for both TAPs/bridge and emit a secret-masked native VM log on later failure. No DECnet candidate protocol behavior changes. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete Area-31 result.

Full Area-31 x86_64 job `107918164560` in run `36086078999` on exact candidate `12b5313f142b49786da6a5d1a07ac70a3e237bbb` proves the host TAP fallback and Python/VMS NICE path are now healthy: disposable identities are allocated, the gateway reaches PYRTR 31.3, executor summary/status/counters pass, and QCOCAL is discovered through NICE. The native Linux VM then boots and fails specifically at `area31-native: NICE proof failed`. Its C probe still assumes a single retcode-1 NICE reply, while the same real VMS endpoint has already proven the standard retcode-2 multiple-response header followed by positive node item(s) and retcode -128 termination. Make the native probe consume that exact sequence, validate each positive executor node reply with the repository NICE parser and require the configured target address. Add fast syntax coverage for `area31-native.c`. Candidate kernel and wire behavior are unchanged. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete Area-31 result.

Full Area-31 x86_64 job `107924875358` in run `36088269646` on exact candidate `861e0f671618692da899fe0eaa7b851e421e3578` proves the native VMS NICE correction: native executor summary/status/counters now pass. The next concrete failure is MIRROR. The Area-31 C probe requests the string object name `MIRROR`, while the repository's independent-reference helper identifies the standard DECnet MIRROR endpoint as numbered object 25 and uses numeric object 25 by default. Use explicit `#25` for the real VMS peer so Session Control selects the standard numbered object rather than requiring a named-object registration. MIRROR payload semantics are unchanged. Cross-runner VDE2 remains fail-closed solely on the absent `VDE_SSH_*` rendezvous secrets. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete Area-31 result.

Full Area-31 x86_64 job `107928474130` in run `36089449271` on exact candidate `762ad596b27e3e00be4999eb0726ee0566228f59` again reaches PYRTR, passes the host-side VAX NICE checks and discovers QCOCAL, but the freshly booted native node intermittently fails its first NICE operation. This is a readiness race in the Area-31 smoke harness: it waits only for the local adjacency to the disposable gateway, then immediately opens a Session connection to the remote VAX. Adjacency-up does not prove that the VAX L1 route has arrived. Require the native kernel route table to contain the exact VAX destination via the selected gateway before starting NICE/MIRROR. This changes only lab convergence gating, not candidate routing or Session behavior. The numeric MIRROR object-25 correction remains in place and will be exercised once route convergence is explicit. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 x86_64 job `107932753210` in run `36090844216` on exact candidate `8819b622cf4c01f93d1003ba7cb7accd560d85b1` proves that explicit VAX route convergence alone does not remove the intermittent native NICE failure: the native route is present before the probe, yet `area31-native` still reports only the aggregate NICE failure. Earlier exact-head runs have shown the same native NICE code succeeding, so changing protocol behavior without the failing wire/stage evidence would be guesswork. Add bounded failure-only diagnostics to the native probe: identify the NICE information selector and socket stage, report errno, and dump at most the first 32 reply bytes when framing/retcode/entity validation fails. No credentials, endpoints or successful-session payloads are logged, and candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance to capture the concrete failing native NICE stage/frame.

Full Area-31 x86_64 job `107935998822` in run `36091932114` on exact candidate `dcdaadedece5e447b42c3286bf2b9259ba53fd37` reaches the newly instrumented native probe without emitting a NICE failure diagnostic and proceeds to the same later failure already seen on arm64: `DNIV-AREA31-NATIVE-FAIL reason=qcocal-http-preflight`. This proves the current blocker is QCOCAL preflight, not native NICE. The preflight asks FAL to list only `DNIV*.COM;*`; a clean QCOCAL with no matching temporary objects can legitimately return a no-match status, which the generic directory client reports as failure. Gate on the exact QCOCAL route, prove FAL Session/access independently with `dncopy --probe`, then list the normal broad `*.*;*` directory and reject only if DNIVHT/DNIVTK already exist. Also reduce failure-only NICE hex evidence from 32 bytes to the four-byte NICE reply header so endpoint-derived entity bytes cannot leak through logs. No candidate protocol behavior changes. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 x86_64 job `107950032334` in run `36096590771` on exact candidate `7cc9ca1f35239795c1c43a2e9b49f68077b2fb94` passes PYRTR allocation, VAX NICE, QCOCAL NICE discovery, native NICE/MIRROR, VAX CTERM/FAL, QCOCAL route/FAL access and QCOCAL DCL upload, then fails specifically at `DNIV-AREA31-NATIVE-FAIL reason=qcocal-http-client`. The current smoke discards all `dnlynx` diagnostics, so the evidence does not yet distinguish Session/object rejection, HTTP parse failure or a non-2xx response. Preserve only failure-safe evidence: capture `dnlynx -i` output, emit its first generic stderr line and/or the HTTP status line on failure, never emit response body or credentials, and retain cleanup of the ephemeral DNIVHT.COM object. No candidate protocol behavior changes. Cross-runner VDE2 run `36096589135` remains correctly fail-closed because the five `VDE_SSH_*` rendezvous secrets are absent. Next: exact-SHA fast acceptance, then full acceptance to expose the concrete QCOCAL HTTP cause.

Full acceptance on exact candidate `5f463888d6201dbef68f15d799859ec4aafba9f5` exposed two independent non-candidate failures. Independent Ethernet Interoperability run `36122701896` failed only three arm64 jobs while all corresponding x86_64 jobs and eleven other matrix jobs were green; each failed before any interoperability execution because Ubuntu's arm64 mirror returned transient 404s for libpcap packages immediately after apt metadata refresh. Make the host dependency step retry mirror acquisition once after clearing stale lists, with apt Acquire::Retries enabled. Area-31 run `36122710927` reaches QCOCAL on both architectures and still fails only at the HTTP client; Session connect itself succeeds, but the prior failure-safe logging still emits only the aggregate `dnlynx: HTTP exchange failed`. Add stage-only HTTP diagnostics (request send, response recv, header framing/status, output/flush) without logging response bytes, endpoints or credentials. No DECnet candidate protocol behavior changes. Cross-runner VDE2 remains correctly fail-closed on the absent `VDE_SSH_*` rendezvous secrets. Next: exact-SHA fast acceptance, then full acceptance and fix only the concrete QCOCAL HTTP stage exposed there.

Full Area-31 x86_64 job `108056565374` in run `36130547028` on exact candidate `f9c57291b5e99ae9b7d5638f6698abadb973e456` exposes the native NICE intermittency precisely: after local adjacency and the exact VAX L1 route are present, the first executor-summary Session connect can still time out with `errno=110`. This is external convergence/readiness, not a NICE framing failure; previous exact-head runs have reached and passed native NICE/MIRROR over the same path. Make the Area-31 smoke retry the complete native NICE/MIRROR proof for up to 60 seconds after route appearance, retaining only the final bounded diagnostic if convergence never occurs. Candidate routing, NSP and Session behavior are unchanged. The same full run also exercises the arm64 apt mirror retry hardening independently. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 run `36131929932` on exact candidate `b1d7f0ab35ba003469680231ce72faf930691df5` gets through PYRTR allocation, VAX NICE and QCOCAL discovery, then both architectures fail only at the ephemeral QCOCAL HTTP object. The failure-safe client diagnostic is now concrete: `dnlynx: HTTP stage=no-header`, meaning the named Session object accepts and closes without delivering a parseable HTTP header. The generated DCL object is the only Phase-8 HTTP component using multi-line DCL expression continuation; `SET NOON` then masks any command-procedure parse error and yields exactly this empty-close symptom. Replace the continued response expression with short independent `WRITE SYS$NET` records, each carrying explicit CR/LF bytes; `dnlynx` already concatenates DECnet records while framing HTTP. Add a selftest guard forbidding the fragile continuation form. This changes only the ephemeral reference-side DCL test object, not candidate protocol behavior. Cross-runner VDE2 remains separately fail-closed because the five `VDE_SSH_*` rendezvous secrets are absent. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete Area-31 result.

Full Area-31 x86_64 job `108072650188` in run `36135552061` on exact candidate `67fa2aebf37bea26cfc6827a949701c7ab571f0b` still reaches the QCOCAL HTTP object but returns `dnlynx: HTTP stage=no-header` after the prior DCL response split. VSI's DCL WRITE definition is explicit: `WRITE logical-name expression[,...]`, and comma-separated expressions are concatenated into one output record. The generated object incorrectly used shell-like `+` operators directly in WRITE commands; with `SET NOON`, those command errors are suppressed and the object closes with no response, exactly matching the wire symptom. Emit each HTTP record using canonical comma-separated DCL WRITE operands and add a generator selftest guard rejecting `+` in WRITE lines. This changes only the ephemeral QCOCAL reference object; candidate DECnet and HTTP client behavior are unchanged. Fast acceptance for `67fa2aebf37bea26cfc6827a949701c7ab571f0b` was fully green. Cross-runner VDE2 remains fail-closed on missing `VDE_SSH_*` secrets. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 x86_64 job `108078584179` in run `36137373745` on exact candidate `e906b900d88fe0e2c43b1d1bbf271ccb8cc3dc88` still reaches PYRTR allocation, VAX NICE, QCOCAL discovery and the native QCOCAL Session object, but the object closes with `dnlynx: HTTP stage=no-header`. The remaining generated DCL defect is concrete: OpenVMS DCL has no `F$CHAR` lexical function. The procedure used `F$CHAR(13)` and `F$CHAR(10)` under `SET NOON`, so those assignment errors were suppressed before the HTTP WRITE commands. Construct CR and LF using DCL's documented binary-overlay assignment (`cr[0,8] = 13`, `lf[0,8] = 10`) and add a generator selftest guard forbidding `F$CHAR`. This changes only the ephemeral QCOCAL reference-side DCL object; candidate DECnet/HTTP behavior is unchanged. Cross-runner VDE2 remains fail-closed because the required `VDE_SSH_*` rendezvous secrets are absent. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete Area-31 result.

Exact candidate `5cf52c584e077d841a8a240fc7cf326b1d0b3f6b` is the first dual-architecture real Area-31 green state. Fast acceptance is fully green: Repository Policy `36138401401`, Build Bootstrap `36138441856`, Project State Gate `36138444274`, Python QEMU VM Lab `36138447794` (x86_64/aarch64), and Independent Ethernet Interoperability `36138450607`. Full acceptance is green through Repository Policy `36139253300`, Build Bootstrap `36139306038`, Project State Gate `36139308529`, External Reference Baselines `36139310791`, all four x86_64/aarch64 VM runs `36139313854`/`36139316663`/`36139319254`/`36139321679`, all 14 independent interoperability jobs in `36139324159`, local VDE2 `36139326492`, local MULTINET `36139328804`, and dual-architecture Area-31 `36139333328`. The DCL binary-overlay correction therefore closes the Area-31 external evidence gap, including the routed native Linux, VAX NICE/MIRROR/login/FAL, and discovered-QCOCAL HTTP/task checks. The only non-green full gate is cross-runner VDE2 `36139331212`, which fails correctly before network activity because all five required `VDE_SSH_*` rendezvous secrets are absent. Phase 8 remains open solely on that external dependency; do not enter Phase 9 or fabricate/manualize the rendezvous. Next action is the automatically dispatched two-runner VDE2 proof after the real SSH rendezvous secrets are configured.

Area-31 expansion starts while Phase 8 is otherwise blocked on the external VDE2 rendezvous secrets. PYRTR's real NML `SHOW KNOWN NODES` output is now retained as a structured occupied-node manifest rather than serving allocation only. The native candidate consumes that manifest after the strict VAX/QCOCAL proof and performs a bounded capability survey of every currently reachable Area-31 identity: NICE executor summary first, then MIRROR object 25 when NICE succeeds. Object absence/unavailability is recorded, not promoted to a candidate failure; malformed required VAX behavior remains strict. The required VAX MIRROR proof is broadened from three 128-byte records to a 1/127/128/255/256/511/512-byte matrix and NICE counters are read again after the traffic. This directly exercises heterogeneous real Area-31 peers through the candidate kernel/NSP/Session path without credentials or destructive operations. Next: exact-SHA fast acceptance, then full acceptance to prove the expanded dual-architecture Area-31 corpus. Cross-runner VDE2 remains fail-closed until the five real `VDE_SSH_*` secrets exist; Phase 9 remains blocked.

Cross-runner VDE2 is no longer blocked on absent secrets. Full run `36142699326` on exact candidate `e2d330016224c6f1083e5a889037412ddc8ebad0` passed SSH prerequisite validation on both GitHub runners and entered the real rendezvous. The server's reverse-SSH process stayed alive but no completion marker arrived; it failed after its 900-second marker wait. The client remained inside `Join remote VDE switch and prove recovery` until GitHub cancelled the 25-minute job, leaving two SSH processes. This is a harness observability/bounding defect: readiness SSH calls have no command-level timeout, inner SSH has no ConnectTimeout, and merely observing a live background reverse-SSH PID does not prove the reverse endpoint is usable. Bound outer and inner SSH connects, wrap remote readiness/completion commands in `timeout`, shorten the server marker wait to 600 seconds, and print only the final bounded readiness error. No endpoint/credential value is logged and no VDE/DECnet protocol behavior changes. Next: exact-SHA fast acceptance, then full acceptance; fix only the concrete bounded rendezvous error exposed next. Phase 9 remains blocked until cross-runner VDE2 is green.

Cross-runner VDE2 full run `36147917346` on exact candidate `90679867da8fd3426b812d90db845eeda013a19f` converts the previous hang into a bounded concrete failure: the client reports `Connection timed out during banner exchange` and then `server rendezvous did not become ready`. Both runners passed secret/key preflight and the server kept its reverse-SSH session alive, so the remaining uncertainty is between the server's ephemeral inner sshd and the bastion reverse-forward endpoint. Add a server-local authenticated inner-SSH selfcheck before publishing the reverse tunnel, then make the client require a raw `SSH-2.0-` banner through the bastion before attempting inner authentication. Probe-only proxy commands now have kill-after bounds; the long-lived VDE bridge keeps its unbounded proxy after the banner is proven. No DECnet, VDE2 or candidate protocol behavior changes. Next: exact-SHA fast acceptance, then full acceptance and fix only the newly isolated rendezvous stage. Phase 9 remains blocked until the real cross-runner VDE2 gate is green.


Cross-runner VDE2 full run `36151198844` on exact candidate `5d1f1a6e40961d86b540eabde9bcaf6559e863bd` isolates the failure to the server runner itself: the new authenticated loopback selfcheck of the ephemeral inner sshd fails before the reverse tunnel is published, while secret/key preflight succeeds. GitHub-hosted Ubuntu uses a locked password entry for the `runner` account; OpenSSH with `UsePAM no` rejects that account before public-key authentication. Run the private inner sshd with `UsePAM yes` while retaining public-key-only authentication, the forced-command `authorized_keys` restriction, disabled TTY/agent/X11/TCP forwarding, and the loopback-only listener. Preserve only bounded local selfcheck diagnostics on failure. This changes only the disposable two-runner test harness, not VDE2 or DECnet protocol behavior. Next: exact-SHA fast acceptance, then full acceptance and continue only from the next concrete cross-runner result. Phase 9 remains blocked until the real two-runner gate is green.

Fast Build Bootstrap run `36152059542` on exact candidate `46c1340433ee085294c5e0995026de4283ff726e` failed identically on x86_64 and arm64 before any protocol test because `tests/lab/prove-vde2-cross-runner.sh` lost its executable mode in the preceding tree update; the test invocation returned `Permission denied`. Restore only the repository mode to `100755`; script contents and protocol behavior are unchanged. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36153674513` on exact candidate `775b23c2e36803c0af2fd6c03a174dbfb5c85382` proves the reverse path is not yet relevant: the server fails its authenticated loopback inner-SSH selfcheck after the executable-mode repair, even with PAM enabled. The bounded client receives no useful stderr, so the remaining defect is inside the disposable inner sshd/auth/forced-command path. Enable verbose logging only for that loopback sshd and print only the final bounded client/server diagnostic tails on selfcheck failure. The log contains no rendezvous endpoint or credential values. No candidate, VDE2, routing or Session behavior changes. Next: exact-SHA fast acceptance, then full acceptance and fix only the concrete local-SSH cause exposed there.

Full acceptance on exact candidate `db6c0c978a3ccd231539446840efbfcdaad3990c` exposed scheduler starvation rather than a transport defect. Cross-runner VDE2 run `36155492527` started its client at 15:38:52 UTC, but the server job did not receive a hosted runner until 15:43:43 UTC, 13 seconds after the client had already exhausted its bounded reverse-banner wait and exited. The server then advanced beyond the previously failing loopback inner-SSH selfcheck, proving the PAM correction. Dispatch the two-runner VDE2 workflow before the VM/interoperability fan-out in full acceptance so both halves enter the Actions queue ahead of the large matrix. No manual dispatch, protocol behavior, credentials, or rendezvous semantics change. Next: exact-SHA fast acceptance, then full acceptance; inspect the real cross-runner result before any further change.

Acceptance dispatcher observability is now self-contained: every automatically dispatched child workflow is discovered immediately by exact workflow, event, branch and candidate SHA, excluding the pre-dispatch run-ID set, and its numeric run ID is persisted in the dispatcher scratch artifact. This removes dependence on UI-only run discovery while preserving automatic dispatch and exact-SHA lineage. Next: exact-SHA fast acceptance, then full acceptance; inspect the recorded cross-runner VDE2 child directly and fix only concrete failures.

Dispatcher run-ID evidence is now also posted back to the owner-created `DNIV acceptance gates` issue after dispatch completes. The comment contains only the exact candidate SHA/profile/scope and child numeric run IDs, so connector-only automation can recover the lineage without Actions run-list access. No credentials, endpoints or protocol behavior are exposed. Next: exact-SHA fast acceptance, then full acceptance.

FAST dispatcher run `36158748964` proved child run-ID discovery works, but the final issue-comment publish failed with `Resource not accessible by integration` because the dispatcher job had `actions: write` and `contents: read` only. Grant `issues: write` to that job so the already-owner-gated `DNIV acceptance gates` event can publish non-secret child run IDs. Child workflows themselves dispatched successfully; candidate protocol behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36160103171` on exact candidate `40c18823de61f168080e7dd4d98963f8e5430fca` removes scheduler starvation from the failure: server and client received runners at 16:20:21/16:20:22 UTC and entered the proof together. The server passed the authenticated loopback inner-SSH selfcheck and kept its reverse SSH process alive; the client nevertheless received no `SSH-2.0-` banner through the bastion reverse endpoint and failed at 16:24:59 UTC. Add a bounded server-side self-probe through the actual bastion reverse listener before publishing readiness, keep verbose reverse-SSH evidence private, and emit only a sanitized classification distinguishing bastion direct-forward denial, listener refusal, a delivered `forwarded-tcpip` channel with no inner banner, or no delivered forwarding channel. No DECnet/VDE2 protocol behavior or credentials change. Next: exact-SHA fast acceptance, then full acceptance and fix only the classified rendezvous defect.

Cross-runner run `36162729374` on exact candidate `4c1646cb2e1f4fcab1c07464d9b7558beec911bb` proves the server can traverse the published reverse listener and read the inner `SSH-2.0-` banner, while the client still exhausts every eight-second banner probe with no stderr. The generated bastion SSH config used a ten-second `ConnectTimeout`, so the outer `timeout -k 2 8` killed client probes before OpenSSH could report an outer-connect failure. Reduce only the outer bastion connect bound to five seconds, below every eight-second probe wrapper, so the next run either succeeds or emits the concrete SSH failure instead of a silent timeout. Candidate, VDE2 and credential behavior are unchanged. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36165678044` on exact candidate `29e4ce7fc7a88652c7bf17da79ee5a5f084280c3` makes the remaining failure concrete: the server runner publishes and self-probes the reverse endpoint successfully, but the separate x86_64 client runner cannot establish even the outer SSH connection to the bastion (`Connection timed out`) across repeated bounded attempts. This is runner-egress reachability, not the reverse tunnel or inner sshd. Move only the client half to GitHub's native `ubuntu-24.04-arm` pool, retaining the x86_64 server half, so the gate both diversifies hosted-runner egress and strengthens the proof into a real mixed-architecture two-host VDE2 join. No credentials, rendezvous endpoint, candidate protocol behavior or manual dispatch changes. Next: exact-SHA fast acceptance, then full acceptance and inspect the real mixed-architecture cross-runner result.

Mixed-architecture full cross-runner run `36167545491` on exact candidate `6b518e1f3eb2ff3d2874d2770ab2d9f9efb21ee2` reached the real reverse endpoint from the arm64 client, proving the runner-egress problem is avoided. The next concrete failure is local to the arm64 client bridge launch: `setsid dpipe ...` exits immediately with `Err: cannot create pgrp`. `dpipe` creates its own process group; wrapping it in `setsid` makes that group creation fail on this runner. Remove only the redundant outer `setsid`; existing cleanup still targets the dpipe-created process group. No rendezvous, VDE2, DECnet or credential behavior changes. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36169051957` on exact candidate `8d4e99b6fdfdac28933c23965b066b89b5b0097a` advances past the arm64 process-group failure: the mixed-architecture client reaches/authenticates through the rendezvous and starts the bridge, but the first raw Ethernet echo times out. Normalize both bridge-side `vde_plug` endpoints to explicit `vde://` URLs, matching the already-green Route20/echo VDE URI form and avoiding path-form parser differences across libvdeplug builds. The forced-command whitelist is updated to the exact same URI command. No DECnet protocol behavior, credentials, endpoint, or manual dispatch changes. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36170889385` on exact candidate `a4926cb4a2b6c92253315ccbbd0bebba317d8491` advances past outer reachability and raw reverse-banner checks, then fails the authenticated ready probe with `Connection timed out during banner exchange`. The probe ProxyCommand itself was wrapped in an eight-second `timeout`; unlike the one-shot raw banner probe, a ProxyCommand must remain alive for the entire nested SSH handshake and command. Reuse the normal bounded SSH proxy there and keep the existing outer `timeout -k 2 8 ssh ...` around the complete ready/completion probe. No protocol, credential, endpoint, or manual-dispatch behavior changes. Next: exact-SHA fast acceptance, then full acceptance.

Full Area-31 run `36173485243` on exact candidate `eba626563a9d17b389078dcff81ffaa8ceb8ce80` passed x86_64 but the arm64 native proof was killed by the harness after the fixed 360-second guest wait while its expanded Area-31 survey was still making forward progress (through at least node 31.39 at about 358 guest seconds). This is a harness budget defect, not a protocol failure. Keep x86_64 at 360 seconds and raise only the arm64 proof wait to 540 seconds to cover TCG/host variance plus the deliberately expanded peer survey. Cross-runner VDE2 in the same full run failed independently because the arm64 client runner could not open TCP to the rendezvous host; server-side reverse selfcheck passed. Next: exact-SHA fast acceptance, then full acceptance; treat any repeated client outer-connect timeout as external rendezvous reachability rather than candidate behavior.

Full cross-runner run `36176419640` on exact candidate `5ad20f30a7e0fa518de922c91b87c2d52f844f37` reaches the inner SSH service and starts the stream bridge, but the first raw Ethernet echo still times out. Replace the dpipe composition with vde_plug4's documented native `vde_plug URL = command ...` bidirectional stream mode, using the same pinned Ubuntu vdeplug implementation already installed by the workflow. This removes the extra dpipe process/framing layer while preserving the exact SSH forced-command boundary and VDE URLs. Bridge cleanup now targets the vde_plug process directly. No DECnet protocol, credential, endpoint, or manual-dispatch behavior changes. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36178045997` on exact candidate `7e50a8735df05bf6f677176eb231fee79a70aaea` did not exercise the new native bridge because the arm64 client never established the outer SSH connection to the bastion; after bounded retries it reported `Connection timed out`. This repeats the already-classified hosted-runner egress failure, while the server remained in its bounded rendezvous wait. Classify only that exact outer-connect timeout with exit status 75 and automatically retry the client half once on an x86_64 hosted runner; the primary arm64 egress probe is shortened to twelve attempts so the bounded fallback still fits inside the existing server window. Inner-SSH, VDE bridge, frame, adjacency and recovery failures remain hard failures and never trigger fallback. No credentials, endpoint values, DECnet/VDE2 behavior or manual workflow dispatch changes. Next: exact-SHA fast acceptance, then full acceptance; inspect the native bridge only once a client reaches the rendezvous. Phase 9 remains blocked until the two-runner proof is green.

Full cross-runner run `36180046635` on exact candidate `39887ef5560354db9d1f06f5a5e536643aed488b` repeated the arm64 outer bastion timeout, but the automatic fallback did not start because Actions masks both the host and port as `***`; the classifier incorrectly required a numeric port and therefore returned the normal hard-failure status. Match the OpenSSH outer-connect diagnostic without assuming the masked port is numeric. The classification still applies only to the raw bastion banner probe's private stderr and only to `ssh: connect to host ...: Connection timed out`; all inner-SSH/VDE/frame/adjacency failures remain hard failures. Next: exact-SHA fast acceptance, then full acceptance and exercise the automatic alternate-runner path. Phase 9 remains blocked until cross-runner VDE2 is green.

Repair the timeout-classifier edit itself and lock it down with a permanent unit regression. The previous text replacement malformed the shell line before acceptance could validate it. The repaired script factors classification into `is_outer_connect_timeout`; its selftest accepts GitHub's masked outer-connect diagnostic and rejects an inner banner-exchange timeout, and the root unit gate runs that test on every build. No protocol or rendezvous behavior changes beyond the intended automatic outer-egress fallback. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36188286348` on exact candidate `f960b2226d00628fc43ca0c6cdf3d6661456441b` reached the reverse endpoint from the arm64 client but then exhausted the authenticated ready probe with `Connection timed out during banner exchange`; the automatic alternate-runner fallback correctly stayed disabled because this was no longer the raw outer-connect signature. The nested SSH ProxyCommand opens a fresh bastion connection for every inner probe and bridge SSH session, so an outer egress stall after the successful raw banner is surfaced misleadingly as an inner banner timeout. After the raw reverse-banner discriminator succeeds, establish one bounded persistent client-side SSH local forward through the bastion and run all authenticated inner SSH commands plus the VDE bridge through that localhost forward. This preserves the same server reverse listener and forced-command boundary while removing repeated outer bastion connects from the inner protocol path. The persistent outer forward is killed during normal cleanup; an explicit outer-connect timeout while creating it remains eligible for the automatic alternate-runner fallback, while inner SSH/VDE/frame/adjacency failures remain hard failures. No credentials, endpoint values or DECnet/VDE2 protocol behavior change. Next: exact-SHA fast acceptance, then full acceptance and inspect the first native-bridge result reached through the persistent tunnel. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36189654229` on exact candidate `aeb9ef081a956a8a1670637c49d86e706d80cea5` reaches the server through the persistent client-side bastion forward and passes the authenticated ready probe, so repeated bastion connections are no longer the blocker. The native vdeplug4 bridge then exits immediately with `vde_plug: invalid option -- 'F'`: GNU option permutation makes the inner SSH `-F /dev/null` argument look like a `vde_plug` option. Terminate vde_plug option parsing explicitly with `--` before the local VDE URL, preserving the documented `vde_plug URL = command [args]` stream form and all SSH arguments unchanged. No endpoint, credential or DECnet/VDE2 protocol behavior changes. Next: exact-SHA fast acceptance, then full acceptance and inspect the first frame result. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36192721038` on exact candidate `a6c461cf474d35cb348a50d4ffae3dfe7d29dfdc` is the first run to carry real Ethernet and DECnet traffic through the native vdeplug4/SSH bridge: the initial marked frame succeeds and pinned PyDECnet forms adjacency to Route20. The remaining failure is the bridge-reconnect assertion: after the first adjacency-up event, the harness disconnects the bridge for only eight seconds and then waits for a second adjacency-up event, but the Ethernet circuit itself remains locally up and the remote adjacency has not yet aged out, so no down/up transition occurs. Make the fault proof wait explicitly for PyDECnet event 4.16 (`Adjacency down`) with a bounded 120-second expiry window before restarting the bridge, then require the next 4.15 adjacency-up event as before. This strengthens the disconnect/reconnect proof rather than weakening it; frame, switch-restart and independent-peer requirements remain unchanged. No candidate protocol behavior, credentials or endpoint values change. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete cross-runner result. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36196720805` on exact candidate `a1cb8c532d1efd2eee44b72d758002c81503f1c0` still never observed PyDECnet adjacency-down after the bridge was stopped. The client log retained the initial successful adjacency for the entire new 120-second expiry window, proving the stop operation killed only the native `vde_plug` parent while its spawned SSH stream remained alive and continued carrying Route20 hellos. Start the native bridge in its own session/process group with `setsid` and stop the entire bridge group, including the spawned SSH command, before waiting for adjacency expiry. This is the native-vdeplug equivalent of the earlier dpipe process-group cleanup and is deliberately applied only after dpipe has been removed; the prior dpipe/setsid incompatibility is therefore not reintroduced. Frame delivery, peer behavior, credentials and endpoint values are unchanged. Next: exact-SHA fast acceptance, then full acceptance and require bridge disconnect, adjacency expiry, reconnect, client-switch restart and all three marked frame exchanges to pass. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36198514542` on exact candidate `3036cddba1d5a2b26e276442b5261f0f9513ed13` again carried the first marked Ethernet frame and formed the independent PyDECnet/Route20 adjacency, but the bridge fault still did not age the adjacency down within the bounded 120-second window. The process-group stop is therefore not sufficient for vdeplug4's spawned command path. Record the complete descendant PID tree before teardown, terminate both the bridge group and every recorded descendant (including any SSH stream in its own process group), and require a marked raw-frame negative immediately after stop before waiting for adjacency expiry. A root unit selftest creates a child in a separate session and proves the tree-stop helper kills it, preventing this cleanup regression from escaping build acceptance. No candidate protocol, credentials or endpoint values change. Next: exact-SHA fast acceptance, then full acceptance; the cross-runner gate must show frame-path loss, adjacency expiry, bridge reconnect, switch restart and all three marked frame exchanges before Phase 8 can close.

Fast Build Bootstrap run `36199014015` on exact candidate `4f6ce601764767ffeaf28afc3a2ca463d28109d8` rejected the descendant-stop increment identically on x86_64 and arm64 before protocol execution because the scripted edit itself malformed the timeout-classifier line, and `bash -n` correctly reported an unmatched quote. Rebuild the script from the last syntax-valid parent while retaining the intended process-tree teardown, bridge-stop selftest and post-stop marked-frame negative. This is a harness source repair only; no rendezvous, VDE2 or DECnet behavior changes. Next: exact-SHA fast acceptance, then full acceptance.

Full cross-runner run `36199792553` on exact candidate `a80823bdd3f428601a96b66b074365f9fb795a21` again carried the first marked Ethernet frame and formed the pinned PyDECnet/Route20 adjacency. The repaired descendant teardown then made the marked request/response path fail as required, but PyDECnet still did not emit adjacency-down during the 120-second bound. Treat the reusable client-side bastion local forward as the hard transport fault boundary as well as stopping the native bridge: close both before the negative frame and adjacency-expiry checks, recreate the forward before bridge reconnect, and repeat the same complete transport cut around the client-switch restart. This guarantees that no one-way or detached inner SSH stream can continue carrying Route20 hellos after the bridge process tree is stopped. Outer-connect timeout classification/fallback remains unchanged; all frame, adjacency, reconnect and switch-restart requirements remain strict. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and require the real cross-runner fault/recovery sequence to pass before Phase 8 closes.

Full cross-runner run `36200905966` on exact candidate `6ef9a9114a4ff7adcf56acd738a92d163ebc6ce6` failed before the bridge stage at the new reusable client-side bastion forward. The helper treated a still-running outer SSH process as ready, but every authenticated inner probe then received `ssh: connect to host 127.0.0.1 ...: Connection refused`, so the local listener/full reverse path had never become usable. Make forward readiness application-backed: after each `ssh -NT -L` launch, require a bounded raw `SSH-2.0-` banner through the localhost forward before accepting it; otherwise terminate that attempt and retry. Only a classified outer bastion timeout remains eligible for alternate-runner fallback. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and continue only from the next concrete cross-runner result.

Full cross-runner run `36202093416` on exact candidate `a2da9dd54616345870ef01ab1f2af5a0a19b3bf4` failed at the new application-backed persistent client forward, but the failure was self-inflicted by the readiness loop: each `ssh -NT -L` process was killed after a fixed one-second sleep if the local banner was not already available, shorter than the configured five-second outer SSH connect bound. The repeated localhost `ConnectionRefusedError` diagnostics therefore did not prove the forward was unusable; they proved it had not been allowed to finish connecting. Keep each forward attempt alive for a bounded eight-second readiness window, probe every 0.5 seconds, suppress transient localhost probe tracebacks, and only then terminate/retry the attempt. The raw outer-connect classifier and all inner SSH/VDE/frame/adjacency requirements remain unchanged. Next: exact-SHA fast acceptance, then full acceptance and continue only from the next concrete cross-runner result.

Full cross-runner run `36202993942` on exact candidate `b4b8324c6c5c73d1de5be5d5d006bdbf27cf7bf1` reached the native bridge, carried the initial marked Ethernet frame and formed the pinned PyDECnet/Route20 adjacency. The transport cut then made the marked frame path fail, but the harness waited for a log string that it had filtered out: pinned PyDECnet defines routing event 4.15 as Adjacency up, 4.16 as Adjacency rejected, and 4.18 as Adjacency down. The test configuration subscribed to `4.15,4.16`, so a genuine listener-timeout down event could never satisfy `wait_downs`. Subscribe to `4.15,4.18` and keep the existing strict transport-cut, expiry, reconnect, switch-restart and marked-frame requirements unchanged. This fixes only independent-reference harness observability; no candidate protocol, credentials or endpoint values change. Next: exact-SHA fast acceptance, then full acceptance and require the complete real cross-runner fault/recovery sequence to pass before Phase 8 closes.

Full cross-runner run `36204411612` on exact candidate `312640e3b4863ab4f49c55f24ff87aab348e2370` failed before the native bridge because the arm64 client could not make the reusable `ssh -L` bastion forward pass its local SSH-banner readiness probe, despite the preceding one-shot `ssh -W` reverse-endpoint banner probe succeeding. Replace the persistent local-port forward with an OpenSSH ControlMaster connection and reuse the already-proven `-W 127.0.0.1:<reverse-port>` channel through that master for every nested inner-SSH command and the native VDE bridge. Stopping the master remains the hard transport-cut boundary for expiry/reconnect testing. Outer-connect timeout fallback remains unchanged; inner SSH, frame, adjacency and recovery failures remain hard failures. No credentials, endpoint values or DECnet/VDE2 protocol behavior change. Next: exact-SHA fast acceptance, then full acceptance and continue from the first concrete result through the multiplexed persistent bastion path. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36205905304` on exact candidate `f750012592ff32a094a49bcaa8df8740ecf93500` again passed the one-shot raw reverse-endpoint banner check, but the new ControlMaster path could not pass the same banner using multiplexed `ssh -S ... -W`; the fallback correctly stayed disabled because the outer connection itself had succeeded. Keep the ControlMaster only as the persistent outer transport, create the client localhost forward explicitly over that established master with `ssh -O forward -L`, and application-probe the resulting local listener for the inner `SSH-2.0-` banner before any authenticated command or VDE bridge starts. All inner SSH, frame, adjacency, transport-cut, reconnect and switch-restart failures remain hard failures; only a classified outer master-connect timeout is fallback-eligible. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and continue from the first concrete result through the master-backed local forward. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36206877235` on exact candidate `625e70921274313bfb0ea3eb652dac94906f36af` passed the arm64 client's initial one-shot reverse-endpoint banner discriminator, but the same runner then failed to establish an application-usable persistent bastion transport before any authenticated inner SSH, VDE frame or adjacency test ran. Treat failure of this pre-inner-SSH persistent transport bootstrap as the same hosted-runner transport class as the already-supported raw outer-connect timeout and invoke the automatic x86_64 client fallback. The fallback reruns the entire client proof from scratch; no inner SSH, VDE frame, adjacency, reconnect or switch-restart failure is fallback-eligible. This does not weaken the Phase 8 gate: one real two-host client path still must pass the complete cross-runner sequence. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and inspect the fallback's first concrete result if the arm64 bootstrap repeats. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36208438211` on exact candidate `93691c452133a840a550b938db17d15600ff5761` exercised the new automatic alternate-runner path: the arm64 primary classified its persistent bastion transport bootstrap as fallback-eligible, then the x86_64 fallback independently failed at the same pre-inner-SSH persistent transport bootstrap after its raw prerequisites passed. The bootstrap currently suppresses the ControlMaster/local-forward failure detail, so the next exact increment adds a secret-safe stage discriminator: master creation, forward request, local-listen conflict, forwarding denial, or forwarded-banner readiness. No inner SSH, VDE frame, adjacency or recovery failure becomes fallback-eligible, and no endpoint or credential value is emitted. Next: exact-SHA fast acceptance, then full acceptance and fix only the concrete persistent-transport stage reported by the two-runner run. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36209384967` on exact candidate `46f205fb22a05a53a30d83e090bc209ddca1a4bb` reports the same secret-safe concrete failure on the x86_64 fallback as on the arm64 primary: persistent transport bootstrap fails at `stage=master`, before any local-forward request, inner SSH, VDE frame or adjacency operation. The ControlMaster path is therefore the defect. Restore the earlier direct `ssh -NT -L` persistent forward that already carried a marked Ethernet frame and formed PyDECnet/Route20 adjacency in run `36202993942`, retaining the eight-second application-backed banner readiness, transport-cut teardown, alternate-runner classification and strict downstream frame/adjacency/recovery checks. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and continue from the first concrete result on the previously proven forward mechanism. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36211600829` on exact candidate `e3bf465983d57353d1dcf9672c536b73f3377451` repeated the persistent-forward defect on both client architectures: the arm64 primary fell back automatically and the x86_64 fallback failed at the same secret-safe `stage=forward-banner`, while the one-shot `ssh -W` reverse-endpoint banner discriminator had already succeeded. Remove the unreliable extra `ssh -L` layer and return inner SSH plus the native VDE bridge to the direct `ProxyCommand=ssh ... -W` path that previously carried a marked Ethernet frame and formed Route20/PyDECnet adjacency. The hardened bridge process-tree teardown remains the transport-cut boundary; a marked raw-frame negative must prove the path is dead before the now-correct 4.18 adjacency-down wait. Only the initial raw outer-connect discriminator remains fallback-eligible; every inner SSH, frame, adjacency and recovery failure is hard. No credentials, endpoint values or candidate DECnet/VDE2 behavior change. Next: exact-SHA fast acceptance, then full acceptance and require the complete real cross-runner fault/recovery sequence before Phase 8 closes.

Full cross-runner run `36213069416` on exact candidate `a9582c577cdf9282b4b02ea1aa06ae50d7c53a7a` passed the raw reverse-endpoint banner and authenticated ready probe, then the native bridge process stayed alive but the first marked Ethernet request timed out. The same native VDE bridge shape previously carried a marked frame when its right-hand command was a simple SSH invocation through localhost; the new direct ProxyCommand adds a multi-token OpenSSH option directly to vde_plug4's command side. Keep the proven direct bastion ProxyCommand for authenticated probes, but place the complete inner SSH bridge command in a private executable wrapper and give vde_plug only that single wrapper pathname as its command operand. This preserves the exact forced remote VDE command and hardened process-tree teardown while removing command-argument interpretation from the vde_plug boundary. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and continue only from the next concrete cross-runner result. Phase 9 remains blocked until the complete frame/adjacency/fault/recovery sequence is green.

Full cross-runner run `36213729581` on exact candidate `7696e769b2531e691540e2ff9e9307d1966b83d0` again passed the raw reverse-endpoint banner and authenticated ready probe, launched the direct native VDE-over-SSH bridge, and then timed out only on the first marked Ethernet request. The direct bridge now has an extra outer+inner SSH startup path compared with the earlier localhost-forward run that carried the same frame, while the harness treated a live bridge PID after one second as data-plane readiness. Make initial bridge readiness application-backed: retry the marked frame exchange up to three bounded attempts while requiring the bridge process to remain alive, and accept the bridge only after an actual response crosses both switches. The frame proof remains mandatory; this only removes a one-shot startup race before any adjacency exists to provide natural retry traffic. No credentials, endpoint values or DECnet/VDE2 candidate behavior change. Next: exact-SHA fast acceptance, then full acceptance and continue from the next concrete cross-runner result. Phase 9 remains blocked until frame delivery, adjacency expiry/recovery, reconnect and switch restart are all green.

Full cross-runner run `36214476374` on exact candidate `9dcee4fafc23c12b78e201439b2bf66d96c82fb8` passed the one-shot raw reverse-endpoint banner discriminator, but a later fresh ProxyCommand connection for the authenticated ready probe stalled and surfaced only as inner `Connection timed out during banner exchange`. This is the ambiguity already identified in the direct path: each nested SSH opens a new bastion connection, and the inner client alone cannot distinguish outer egress failure from an inner banner defect. Move the direct ProxyCommand into a private wrapper that records only its own stderr; before each authenticated ready attempt, clear that private outer diagnostic and classify fallback only when the wrapper itself reports the exact outer bastion connect timeout. Genuine inner banner/authentication failures remain hard failures, as do every VDE frame, adjacency and recovery stage. No credentials, endpoint values or candidate DECnet/VDE2 behavior change. Next: exact-SHA fast acceptance, then full acceptance and inspect the complete alternate-runner path if the hosted arm64 outer connection repeats. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36215246642` on exact candidate `3a7aab8881cc122de59b64fbaa70abceb2380401` passed the one-shot reverse-endpoint banner and authenticated ready probe, then the native bridge opened a fresh ProxyCommand path whose outer bastion connection timed out; the inner SSH consequently reported `Connection timed out during banner exchange`, and the bridge exited before the first marked frame. The private ProxyCommand wrapper already records the decisive outer diagnostic. During only the initial bridge bootstrap, classify that exact wrapper-level outer timeout as fallback-eligible so the automatic x86_64 client reruns the complete proof. Disable that classification permanently after the first marked frame succeeds; all later bridge reconnect, adjacency and recovery failures remain hard failures. No credentials, endpoint values or candidate DECnet/VDE2 behavior change. Next: exact-SHA fast acceptance, then full acceptance and inspect the complete fallback path if the arm64 bridge bootstrap repeats. Phase 9 remains blocked until cross-runner VDE2 is green.

Full cross-runner run `36216016155` on exact candidate `e8de6b100c3f6ab9f666acd525b2df353279fe24` again proved the first raw `ssh -W` reverse-endpoint banner path, then failed every authenticated ready probe with `Connection timed out during banner exchange`; the private ProxyCommand log did not contain an outer-connect timeout, so fallback correctly stayed disabled. The repeated pattern is that the first bastion connection is usable and the next fresh connection is not. Make the first client bastion connection the persistent transport instead of throwing it away: establish `ssh -NT -L` as the initial rendezvous operation, require the local listener to return the inner `SSH-2.0-` banner, and reuse that one outer connection for authenticated commands and the native VDE bridge. This is the transport form that already carried a marked frame and formed Route20/PyDECnet adjacency in run `36202993942`. The hard transport-cut now stops both bridge and persistent forward, requires marked-frame failure and adjacency-down, then establishes a fresh forward for recovery; switch restart does the same. Only a classified persistent outer-connect timeout is fallback-eligible. No credentials, endpoint values or candidate DECnet/VDE2 behavior change. Next: exact-SHA fast acceptance, then full acceptance and require the complete frame/adjacency/reconnect/switch-restart sequence before Phase 8 closes.


Exact candidate `f58612166175cdf520e01bbc1afeb88f92bbb006` full acceptance isolated the next two Phase 8 harness corrections. Cross-runner VDE2 run `36218217564` passed the initial marked frame and PyDECnet/Route20 adjacency, the hard cut killed the frame path, and PyDECnet logged event 4.18 adjacency-down; recovery then failed before a second adjacency-up was observed. Require marked frame 2 immediately after recreating the persistent bastion forward/native bridge, before waiting for adjacency-up, so the recovered Ethernet path is proven independently and bridge diagnostics are surfaced at the correct stage. Area-31 run `36218288072` repeated `qcocal-object-existing`; the Area-31 matrix is already `max-parallel: 1`, so this is stale project-owned remote test state. Bound cleanup to DNIVHT.COM and DNIVTK.COM only, re-list after each DAP erase, and fail if cleanup cannot make those names absent before the strict HTTP/task install/use/delete proof. Candidate DECnet behavior is unchanged. Next: fast acceptance, then full acceptance; do not enter Phase 9 until both external Phase 8 gates are green on the same exact head.


Full acceptance on exact candidate `bc95d47b44a8ce9ba43411a4e674add2ea0b5e31` requalified Area-31 successfully on both architectures in run `36219353713`; the bounded DNIVHT/DNIVTK stale-state cleanup therefore resolves the remote QCOCAL lifecycle defect without changing candidate protocol behavior. Cross-runner VDE2 run `36219283281` still failed at the initial persistent client forward: the server proved the reverse-endpoint SSH banner, but the arm64 client exhausted its direct `ssh -NT -L` attempts without receiving the inner banner and the x86_64 fallback was not selected. For this direct bastion process, both the TCP-connect timeout form and OpenSSH's `Connection timed out during banner exchange` form are outer-connection failures; classify exactly those two private-log forms as fallback-eligible, while forwarded-target, authentication, host-key, inner-SSH, frame, adjacency and recovery failures remain hard failures. Add secret-safe stage diagnostics for non-timeout forward failures. Next: exact-SHA fast acceptance, then full acceptance; Phase 9 remains blocked until the complete cross-runner frame/adjacency/fault/recovery sequence is green.


Fast Build Bootstrap run `36221757765` on exact candidate `46967371212e81c48254f7ec5abf2528d2e2ddbb` rejected the VDE harness before execution because the scripted classifier edit malformed the shell source and also lost the executable file mode. Rebuild `prove-vde2-cross-runner.sh` from the last syntax-valid parent `bc95d47b44a8ce9ba43411a4e674add2ea0b5e31`, retain only the intended direct-bastion timeout classification and secret-safe diagnostics, and restore mode 0755. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; Phase 9 remains blocked on cross-runner VDE2.


Full cross-runner run `36222378434` on exact candidate `ee394d9b1b577624843662343bee7bbd6ba07b09` failed during initial persistent-client-forward readiness before inner authentication, frames or adjacency. The helper still violated the intended first-connection reuse model by killing an otherwise live `ssh -NT -L` transport after only eight seconds and opening up to twelve fresh bastion connections; the final private log contained none of the classified outer-timeout, authentication, host-key or forwarded-target errors, so fallback correctly remained disabled. Keep exactly one first client bastion connection alive for the whole bounded banner-readiness window, record private verbose OpenSSH state only for secret-safe classification, and distinguish outer authentication, local-listener, reverse-endpoint and live-without-banner failures without printing endpoint values. A classified direct outer timeout remains the only fallback-eligible failure. Candidate DECnet/VDE2 behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance and require the complete three-frame/adjacency/fault/recovery sequence before Phase 8 closes.


Bastion VDE inventory diagnostics are wired into every cross-runner role. Each role independently tests SSH command login, records visible `vde_switch` command lines, discovers VDE control sockets below `/tmp`, and attempts `vde_plug` attachment without exposing SSH secret values. The probe is intentionally non-gating while the current reverse-forward proof remains authoritative. Next: exact-SHA fast acceptance, then full acceptance; inspect probe output and use the persistent bastion switch directly if both hosted-runner architectures can attach.


Full acceptance run `36223665078` on exact candidate `43c35e4c8ac73abbd9f8360e445c741c7a8ba663` reached the new bastion inventory probe from both the server and primary arm64 client. Both roles authenticated to the SSH rendezvous, but the bastion account saw no running `vde_switch` process and no VDE `ctl` socket under `/tmp`; the existing reverse-forward path then failed before any cross-runner frame or adjacency. The probe is broadened to search `/tmp`, `/run`, `/var/run` and the bastion account home, report VDE binary availability, and, when no reusable switch is visible, create and attach to a disposable bastion-local VDE switch before cleaning it up. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; if the bastion can host the disposable switch, replace the fragile reverse-forward rendezvous with a direct shared bastion VDE fabric.


Fast acceptance on exact candidate `2416d122a07c5dd3891efa649df51da27f046965` completed green: Repository Policy `36223886697`, Build Bootstrap `36224024514`, Project State Gate `36224033410`, E1 VM Lab `36224041875`, and Independent Ethernet Interoperability `36224052012`. The bastion probe now also reports `socat`, `nc`, `netcat`, and `ncat` availability and resolved executable paths after successful SSH login. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance to capture the bastion tool inventory and VDE capability result.


Full acceptance on exact candidate `7511061bc0608e544b26bcc38eb25e6c041ec315` was green across every local/reference/Area-31 gate; only Cross-runner VDE2 run `36234122905` failed. The bastion inventory confirmed `socat` at `/usr/bin/socat`, `nc` and `netcat`, plus `vde_switch` and `vde_plug`; no persistent VDE control socket was visible, and the disposable bastion VDE attach probe failed. A new non-gating two-runner socat rendezvous probe now uses two independent SSH sessions joined by one bastion-local UNIX socket and exchanges marked bytes in both directions, avoiding reverse TCP forwarding entirely. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; if the socat rendezvous is green, replace the fragile reverse-forward/inner-SSH transport with the proven socat byte-stream rendezvous and carry VDE framing over it.


Cross-runner run `36239303917` on exact candidate `a4a967324fc2bebfef80abab42e1f3196866b346` showed the first socat rendezvous probe was still opening several short-lived bastion SSH sessions for capability checks, cleanup and socket-readiness polling; the server-side probe exited with SSH status 255 before the marked-byte exchange. The probe now uses one persistent outer SSH connection per runner for the actual socat UNIX-socket rendezvous, with no remote readiness polling and only one bounded client retry after a fixed server-start interval. Diagnostic stderr is emitted only on failure and contains no secret values. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; require marked-byte exchange through socat before converting the VDE transport to this rendezvous.


Pre-acceptance review of the single-connection socat probe found that the embedded Python marker strings contained a literal backslash-n instead of a newline, which could block `readline()` despite successful byte transport. The marker writes and reads are corrected before any acceptance claim is made. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance of the socat rendezvous probe.


Cross-runner run `36240111257` on exact candidate `48f70f09f2242494611e3ee700fe3c37f0bc49e4` proved the alternative transport directly: the x86_64 server and arm64 client each reported `socat-rendezvous: ... pass`, exchanging marked bytes bidirectionally through two independent bastion SSH sessions joined by a bastion-local UNIX socket. The legacy reverse-forward path failed immediately afterward, confirming the failure is in that forwarding design rather than basic runner-to-bastion transport. The authoritative cross-runner proof is now converted to carry the native VDE framed stream through the proven socat UNIX-socket rendezvous. The server accepts three sequential relay sessions for initial traffic, hard-cut recovery and client-switch restart; the client retains the marked-frame, Route20/PyDECnet adjacency, transport-loss, adjacency-expiry, reconnect and final-frame requirements. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; Phase 8 closes only if the complete socat-backed cross-runner sequence is green on the unchanged candidate.


Full cross-runner run `36241058051` on exact candidate `87f86be145753924d0ab674e163c54d28fef9ab9` reconfirmed the standalone socat rendezvous on both hosted-runner architectures, but the first authoritative Ethernet frame never crossed the initial relay. The server consumed all three raw vde_plug command sessions while the arm64 client timed out three marked-frame attempts, and Route20 then exited. The VDE stream boundary is corrected to the canonical `dpipe vde_plug ... = command` form documented by VDE2: each local switch is converted once to the bidirectional byte stream carried by the already-proven SSH/socat rendezvous. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; require all three marked frames plus adjacency loss/recovery and switch restart before Phase 8 closes.


Full cross-runner run `36241891430` on exact candidate `7757f2162f248fa73cd58b76b02b6e119ed8bcb3` reached the corrected dpipe framing path but both client and server hit `Err: cannot create pgrp` before VDE attachment. The pinned VDE2 dpipe implementation creates its own process group with `setpgrp()`; wrapping dpipe in `setsid` makes that call fail with EPERM. Remove only the redundant outer `setsid`. dpipe itself remains the process-group root, so the existing process-tree teardown still kills the complete VDE/SSH/socat transport at the hard-cut boundary. The project-owned `tuklusan/vde-2` fork is available for future VDE2 fixes and will be preferred if a defect is found below this harness boundary; this failure is not such a defect. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; require all marked frames, adjacency expiry/recovery and switch restart before Phase 8 closes.


Cross-runner run `36242541922` on exact candidate `e55181d8186bffa9b53fc4f972f201b5e2645f0d` removed the dpipe process-group error, but marked Ethernet frames still did not cross. The remaining ambiguity was implementation/invocation: the runner installed both distro VDE2 and VDEPLUG4 packages while the classic VDE2 cable contract requires `dpipe vde_plug SOCKETDIR = command`, not a `vde://` URL. Per project direction, cross-runner VDE2 now builds the project-owned `tuklusan/vde-2` fork from exact pin `7e7017b6308f3f81d5c922a32097137ceee13074` with classic vde_plug/dpipe enabled and uses those private binaries plus raw switch socket directories for the authoritative cable. This removes distro-command ambiguity and makes VDE2 itself patchable under a pinned fork if a real implementation defect appears. System libvdeplug4 remains only for Route20/PyDECnet and the frame helper. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance; require all marked frames, adjacency expiry/recovery and switch restart before Phase 8 closes.


Full acceptance run `36248497293` on exact candidate `737422efc284d3862c9e452819d9b171c48b9528` hit a hosted-arm64-to-bastion outer SSH timeout after the standalone socat probe also timed out. The cross-runner client had already returned from `start_bridge` while SSH was still connecting, so the later timeout was seen by frame readiness as a generic bridge exit and returned status 1 instead of the workflow's status-75 automatic x86_64 fallback signal. Frame readiness now reclassifies the completed bridge log with the same strict outer-timeout predicate and preserves status 75; forwarded-target timeouts remain excluded. The server also rejects its own outer SSH connect timeout explicitly instead of treating a long rc=255 session as a completed relay. No protocol behavior changed. The pinned `tuklusan/vde-2` build remains authoritative for the cable. Next: exact-SHA fast acceptance, then full acceptance; require three marked frames, adjacency loss/recovery and switch restart before Phase 8 closes.


Full cross-runner run `36254520875` on exact candidate `a7a4f72bee594ff1d467d456b3fd573aa06d6389` after clearing fail2ban proved bastion SSH login from both hosted architectures: the arm64 inventory probe reported `SSH login pass`, the amd64 inventory probe completed successfully, and the paired socat rendezvous probe passed. The authoritative arm64 cable then failed with `UNIX-CONNECT ... No such file or directory`: connectivity was healthy, but the client reached the bastion before the server had published its authoritative relay socket. The client bridge now keeps its single SSH session open while waiting up to 75 seconds for that exact relay socket and emits an internal readiness marker before frame testing. Redundant pre-proof bastion inventory/socat probes were removed from the authoritative workflow to reduce SSH login churn. Candidate DECnet and VDE2 protocol behavior are unchanged. Next: exact-SHA fast acceptance, then full cross-runner proof.


Fast acceptance build run `36254895411` exposed a repository-mode regression in `b21b0a7be536afdb71ba261144084e8765ab1965`: `tests/lab/prove-vde2-cross-runner.sh` had been written with mode 100644, so both x86_64 and arm64 userspace tests failed with `Permission denied` after `bash -n` passed. The script mode is restored to 100755 with content unchanged. Next: repeat exact-SHA fast acceptance before full cross-runner validation.


Full cross-runner run `36255664876` on exact candidate `e0d39671d268877cb99d8f03eb8341e9e77976a1` proved the authoritative cable end-to-end: the arm64 client passed `transport=socat frames=3 bridge_reconnect=1 switch_restart=1 adjacency_recoveries=2`, while the x86_64 server completed all three relay sessions with rc=0. The remaining server failure was a test-harness privilege error: Route20 is launched through `sudo`, so its daemon is root-owned, but the final liveness probe used unprivileged `kill -0`; an existing root-owned process therefore reports EPERM and was misclassified as exited. The liveness probe now uses `sudo kill -0`, matching the existing privileged cleanup path. Candidate DECnet, Route20, and VDE2 protocol behavior are unchanged. Next: exact-SHA fast acceptance, then repeat full acceptance to close Phase 8 if all gates remain green.


Full cross-runner run `36257502631` on exact candidate `e9f792d3bbb44d621fd53495928955a1d9749739` hit an orchestration delay rather than a transport failure: the arm64 client reached the bastion and held its SSH session, but the authoritative server relay socket was still absent after the previous 75-second publication window, producing `DNIV_RELAY_TIMEOUT`. Because hosted server/client jobs can start and provision at different times, relay publication tolerance is now 300 seconds (with a 315-second local readiness envelope) while preserving one SSH session and the existing bounded job timeout. No DECnet, Route20, or VDE2 protocol behavior changed. Next: exact-SHA fast acceptance, then full acceptance.


Phase 8 is closed on exact unchanged candidate `9b73e61bbd0f95b82410276f7b5dc3db94e219ba`. Repository Policy full-acceptance parent `36258507655` dispatched and completed a fully green Phase 8 matrix: Build Bootstrap `36258543209`, Project State Gate `36258553606`, Cross-runner VDE2 `36258564105`, External Reference Baselines `36258573270`, x86_64/aarch64 E1-E4 VM labs `36258584817`/`36258594905`/`36258607144`/`36258617814`, Independent Ethernet Interoperability `36258627216`, local VDE2 `36258635481`, MULTINET `36258643588`, and Area-31 `36258654316`. The authoritative cross-runner cable passed on both server/client jobs with three marked frames, hard transport cut, adjacency expiry and two recoveries, bridge reconnect and client-switch restart using exact `tuklusan/vde-2` pin `7e7017b6308f3f81d5c922a32097137ceee13074`. No one-off protocol patches or secret logging were required. Phase 9 is now active: scale/portability/real-peer/release-image qualification against `docs/PRE_PRODUCTION_TEST.md`.


Phase 9 scale work starts from Phase 8 closure commit `fa36d2ea46b63607c617320c2b6341016a67b50a`. A new exact-candidate controller, `tests/lab/dniv_scale.py`, defines 4/8/16 independent-VM routed topologies using the existing immutable architecture foundations and disposable candidate overlays. The 4-node mode proves three endpoints across a forced two-LAN router; 8/16 modes build a two-area L1/L2 topology and require host-PCAP forwarding source/visit evidence for every paired endpoint direction. `vm-lab.yml` exposes `scale4`/`scale8`/`scale16`; full acceptance initially dispatches only `scale4` on both architectures so scale expansion remains incremental and evidence-backed. Handover/Test Lab documentation is advanced to Phase 9 and stale Phase 8 'unproven' text is removed. Candidate DECnet behavior is unchanged. Next: exact-SHA fast acceptance, then full acceptance requiring the new dual-architecture 4-node scale gate; only after that promote 8 and 16 nodes.
